# План усиления ядра после `v0.4-robust`

> Презентационная упаковка отложена до последней недели. Эта итерация меняет
> только систему и измерительные инструменты; тег `v0.4-robust` остаётся точкой
> безопасного возврата.

## Gate итерации

1. Все 11 моделей проходят не только одиночный census, но и потоковый набор в
   трёх seed-ориентациях: 33/33 терминальных физических результатов.
2. Любой новый физический FAIL имеет сохранённые roster, terminal ledger,
   controller/perception log и позу; runner/timeout не выдаётся за misroute.
3. Пределы производительности меняются только A/B-прогоном. Новый режим обязан
   сохранить routing и safety, а не просто показать больше товаров в минуту.
4. Любой фикс проходит frozen frames, полный pytest/ruff/SDF, одиночный census,
   потоковую матрицу и occupied E-stop.

Официальные уточнения организаторов от 16.07 (`docs/md/organizer_faq_2026-07-16.md`)
добавляют два обязательных свойства: классификация должна переноситься на
приватные неизвестные модели, а Docker-запуск — фиксировать зависимости и
проверяемый CPU/headless сценарий. Запоминание 11 STL не считается улучшением.

## Этап 21 — потоковая матрица всех моделей

- [x] Добавить возобновляемый `run_stream_suite.sh`: два безопасных смешанных
  эпизода на ориентацию, timeout и долговечный status каждого эпизода.
- [x] Dry-run доказывает покрытие 11 уникальных моделей в каждой ориентации.
- [x] Прогнать ориентации 0..2 на тихом стенде и посчитать полный знаменатель.
- [x] Первый воспроизводимый отказ локализовать по физике, perception или control:
  Pouf→быстрый follower оказался транспортным сжатием зазора, а один Cylinder
  в плотном B-потоке создавал 3 ID из-за ложного split длинного силуэта.

Статус 16.07: шесть физических эпизодов `main@3f3a7ed` дали **33/33 terminal
PASS**, точный roster 33/33/33 между perception/classifier/controller и ноль
FAIL/TIMEOUT/JAM. Сырые каталоги: `runs/week4_suite_gate_3f3a7ed_seed0_oi{0,1,2}`.

## Этап 22 — плотный поток и предел механизма

- [x] Повторить потоковую матрицу с уменьшением разрешённых зазоров внутри одной
  зоны; отдельно проверить смены C↔D и divert→B.
- [x] Снять A/B `HOLD_S`/`FIRE_LEAD_S`: routing, импульс, cycle и cross-fire.
- [x] Зафиксировать безопасную рабочую точку и честный потолок шт/мин
  (рабочая точка reopened после красного stability-repeat; закрыта 17.07 —
  см. статус Stage 24).

Статус gap A/B 16.07: `SAME_ZONE_GAP_M=0.8` на orientation 0 дал B 6/6,
но mixed только 1/2 (Pouf физически не сошёл в C при полном roster), поэтому
универсальный baseline остаётся 1.0 м. Цепочка C→D→C→D→B при защитном gap
3.1 м повторена 2/2: 10/10 PASS, cross-fire нет.

Статус timing A/B 16.07: baseline A (`HOLD=2.5`, `LEAD=0.5`) сравнили с
B (`2.0/0.5`) и C (`2.5/0.4`) на двух динамических прогонах Pouf→C и двух
переходных эпизодах на вариант. Медианы cycle: 26.8/27.3/26.2 с; импульса:
6.93/5.43/5.46 Н·с. B и C дали по 10/10 PASS без cross-fire, но не ускорили
цикл или переходный темп 7–8 шт/мин; выборка динамики мала, а полный gate новых
таймингов не снят. Production-точка остаётся `HOLD=2.5`, `LEAD=0.5`, same-zone
gap 1.0 м, cross-zone/divert→B 3.1 м и отдельный Pouf→Pen 2.5 м. Верхняя
подтверждённая заявка — **18 шт/мин** как медиана пяти успешных коротких
трёхтоварных эпизодов; это не sustained all-model предел: безопасная матрица всех
моделей наблюдалась в диапазоне 3–12 шт/мин.

Уточнение Stage 24: точный повтор seed 0 на этой точке дал 31/33 из-за двух
исполнительных FAIL Pouf oi0/oi1. Поэтому параметры остаются текущим baseline,
но слово «безопасная» и новый тег отозваны до различающего replay и повторного
полного stream-gate; потолок 18 шт/мин по-прежнему не повышается.

## Этап 23 — сложные сцены perception

- [x] Добавить synthetic/real набор касаний, частичных перекрытий и входа/выхода
  из границы кадра; считать split/merge, phantom ID и category consistency.
- [x] Закрыть смену ID у кувыркающегося Helmet после временного выпадения из
  кадра: в повторе Pouf→Helmet оба маршрута физически PASS, но roster 2→3.
- [x] Не вводить нейросеть или новый порог без сравнения с текущим baseline.

Статус набора 16.07: `measure_hard_scenes.py` гоняет production baseline на
synthetic touch/occlusion/entry-exit и frozen real Gazebo visible/partial кадрах.
Baseline: split=0, merge=1, phantom=0, category=9/9; merge полной гранью —
зафиксированное ограничение top-view, а не скрытый PASS.

Статус 16.07: tracker 3 кадра/0.30 м принят после полного gate. Два одинаковых
Pouf→Helmet replay с безопасным C→B gap 3.1 м дали 4/4 физических PASS и roster
2/2/2; stream-suite 33/33, отдельный census 33/33 (`classification=0`,
`execution=0`), occupied E-stop и SDF 13/13 зелёные. Попытка gap 2.5 м была
отклонена planner до запуска Gazebo, как и требует защита от cross-fire.

## Этап 24 — длительная физическая устойчивость

- [x] Несколько seed-потоков и повторов на чистом стенде с полным denominator.
- [x] Проверка памяти/CPU, отсутствия роста ID/state и восстановления после E-stop.
- [x] При зелёном gate — новый тег; иначе остаёмся на `v0.4-robust`.

Статус 16.07: точный repeat seed 0, orientations 0..2 — **31/33**, 4/6
all-pass эпизодов; новый seed 1/orientation 0 — **11/11**, 2/2. Новый denominator
42/44, roster perception/classifier/controller 44/44/44, TIMEOUT/JAM/E-stop нет.
Оба FAIL — Pouf→C при oi0/oi1: категория и `FIRED` верны, финальные позы
`(3.219,0.808,0.494)` и `(3.035,0.730,0.207)` не прошли body-verdict; oi2 и
seed1/oi0 прошли. Прежний 33/33 не стирается, но больше не доказывает
повторяемость; routing-gate красный, новый тег заблокирован.

Статус 17.07: корень двух FAIL — исполнительный, не CV: на штатном трении
первый короб вставал у устья склиза C и Пуфик ложился на него вне зоны. Фикс
`424a0a2`: пол зон C/D `mu=0.05`, парный зазор Box400→Pouf 1.5 м, транспортный
Pouf→Pen 5.0 м, poll-бюджет 200. Gate: **два полных suite 33/33 + 33/33**
(roster 5/5/5 и 6/6/6, ноль FAIL/TIMEOUT/JAM), census 33/33 (helmet-хвост
добит после обрыва), triage 0/0, occupied E-stop PASS, ресурсный профиль mixed
oi1 (peak RSS 944 MiB, ≤4.4 ядра, плато без роста, ID без фантомов),
recovery-смок `smoke_estop_recover.sh` 2/2, host gate 388/3 + ruff. Рабочая
точка не менялась: `HOLD=2.5`, `LEAD=0.5`, same-zone 1.0 м, cross-zone 3.1 м;
потолок 18 шт/мин не пересматривался. **Gate GREEN — тег `v0.5-stable-stream`.**

## Этап 25 — переносимость на приватный набор и чистое развёртывание

- [x] Добавить процедурные неизвестные формы вокруг порогов размеров и K;
  сравнить production-правило с ожидаемой категорией без знания имени модели.
- [x] Зафиксировать версии Docker/Python-зависимостей и одной командой проверить
  headless CPU-сценарий на чистом checkout.
- [x] Свести массы товаров, трение ленты/товара и упрощения физики в отчёт;
  каждое число должно иметь статус «дано» или «инженерное допущение».

Этап добавлен по официальным ответам 16.07 и не заменяет красный gate Stage 24:
физический Pouf replay остаётся приоритетом, как только доступен Gazebo-стенд.

Статус 16.07: `measure_private_shapes.py` генерирует 12 depth-сцен без STL и
slug: B-призма и эллипс K≈0.77 в трёх yaw, крупная C-призма в трёх yaw,
допустимый круг D, тонкий 9 мм C и крупный круг C. Production-путь
`measure_items → classify_conservative` дал **12/12**, полный host gate 359/6.
Это проверка правил на аналитических формах, не заявка точности на неизвестном
распределении; реальные/приватные данные по-прежнему сильнее синтетики.

Статус развёртывания 17.07: база запинена по digest, python-зависимости
образа — `docker/pip-constraints.txt` (снят с фактического образа; numpy/scipy/
pytest — apt-сборки, dev-среды сознательно не пинятся — см. decisions.md).
`scripts/check_clean_deploy.sh` одной командой: чистый checkout `HEAD` →
сборка образа с нуля → headless CPU `ci_e2e.sh` → **PASS**. Первый прогон
поймал `.dockerignore`, не пропускавший lock в контекст сборки, — исправлено
красно-зелёным тестом (`tests/test_deploy_pins.py`). Все этапы 21–25 закрыты.

Статус физики 16.07: `docs/report/physics_assumptions.md` отделяет заданные
1 м/с, геометрию/рабочую область и чёрную матовую ленту от назначенных масс,
ODE-трения и упрощений rigid-body. Зафиксированы границы выводов: коэффициенты
не измерены на реальной ткани, деформируемая упаковка и реальный привод не
моделируются; перед изготовлением нужен DOE и повтор полного физического gate.
