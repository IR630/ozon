# Отчёт о развёртывании на новой машине — 02.08.2026

Машина: `Windows 10 Enterprise LTSC 2019`, build **17763.1490** (ReleaseId 1809), x64.
Репозиторий: `C:\Users\Professional\PycharmProjects\ozon`, ветка `main`, коммит `293e18f`, дерево чистое.
Что делалось: проверка развёртывания «с нуля» по инструкциям `README.md`.

> ## СТАТУС ДЕПЛОЯ: УСПЕШНО ЗАВЕРШЁН
>
> Финальная проверка: **02.08.2026, итерация 6**, коммит `4e9c4aa`.
> Обе половины решения развёрнуты из **чистого checkout** и подтверждены прогоном.
>
> | Половина | Команда | Где прогнано | Результат |
> |---|---|---|---|
> | Восприятие и классификация | `check_clean_deploy.sh --python-only` | **эта машина**, Win10 LTSC 2019 | **PASS, exit 0** |
> | Полный контур + ROS 2/Gazebo | `check_clean_deploy.sh` (полный) | машина автора, WSL Ubuntu-22.04, docker 29.6.1 | **PASS** (п. 10) |
>
> Обе проверки собирают код через `git archive HEAD`, то есть подтверждают
> **содержимое репозитория**, а не чьё-то рабочее дерево. Деплой проекта
> работоспособен: `PASS: clean checkout built the pinned image and the headless
> e2e smoke is green`.
>
> **Что при этом осталось открытым — и это не отказ деплоя.** Полный контур ни
> разу не прогонялся на **втором физическом железе**: у автора это своя связка
> WSL + docker, а на этой машине он неисполним по ограничению редакции Windows
> (п. 2). Независимое подтверждение с чужой машины есть только у Python-половины.
> Требование «вторая физическая среда» для ROS-половины остаётся человеко-задачей
> и в этом отчёте не закрывается.
>
> Журнал итераций — п. 9, встречный прогон автора — п. 10.

**Как читать этот документ.** Пункты 1–8 — то, что было найдено при первом
заходе, когда деплой ещё не проходил; они оставлены как есть, потому что
описывают реальные дефекты и их починку. Пункт 9 — журнал шести итераций,
пункт 10 — встречный прогон полного гейта автором, пункт 11 — итог. Итоговое
состояние определяется пунктом 11 и шапкой выше; если формулировка в ранних
пунктах расходится с ними, верна шапка.

**Что до сих пор верно из ранних пунктов:** на этой машине нет Docker, нет WSL2
и нет прав администратора, а Windows 10 LTSC 2019 (build 17763) до версии, где
появляется WSL2, не обновляется в принципе (п. 2). Поэтому полный гейт здесь
неисполним, и это ограничение ОС, а не кода. Половина, которая здесь
поддавалась проверке, проверена и зелёная; вторая половина подтверждена на
машине автора (п. 10).

---

## 1. Что реально заработало (проверено запуском)

Путь «Запуск Python-части (без ROS 2 / Gazebo)» из README отработал целиком.

| Шаг | Команда | Результат |
|---|---|---|
| venv | `python -m venv .venv` | OK, Python 3.14.6 |
| зависимости | `pip install -r requirements.txt` | OK, все колёса нашлись |
| линт | `ruff check .` | `All checks passed!` |
| тесты | `pytest -q` | **674 passed, 17 skipped**, 115 с |
| SDF-модели | `python scripts/build_item_models.py` | 11 моделей → `sim/models/items` |
| классификация | `python scripts/analyze_models.py docs/Stl/*.stl` | вывод **совпал с README построчно**, включая `Мешок K=0.800 → D` и `Ручка 9 мм → C` |
| валидация | `python scripts/measure_validation.py` | recall 8/8, категории 8/8, контракт 8/8, reject 4/4 |
| тяжёлые сцены | `python scripts/measure_hard_scenes.py` | `split=0 merge=1 phantom_id=0 category=9/9` |
| приватные формы | `python scripts/measure_private_shapes.py` | `20/20` |

Отдельно отмечу: на Python **3.14** с **numpy 2.5.1** всё зелёное, хотя README
заявляет только «3.11+». Это приятная новость, но см. риск в п. 6.

`python scripts/check_submission.py` вернул `BLOCKED: PLACEHOLDER: README.md:
<ссылка на облако организаторов>`. Это **не дефект** — README прямо описывает
такое поведение как человеко-задачу H6 (ссылка на видео появится после заливки
в облако). Просто фиксирую, что preflight ведёт себя ровно как задокументировано.

---

## 2. БЛОКЕР: ОС не тянет ни Docker, ни WSL2

Это главная проблема, и она не чинится настройкой проекта.

Установлено фактами с машины:

- Windows 10 **LTSC 2019, build 17763** (версия 1809).
- `docker` — не установлен, каталога `C:\Program Files\Docker` нет.
- `wsl` — команды нет вообще.
- Hyper-V — не включён.
- Запрос состояния компонентов Windows вернул `The requested operation requires
  elevation` → **прав администратора на машине нет**.

Почему это упирается в потолок:

- **WSL2 требует build 18362 (версия 1903) и выше.** LTSC 2019 зафиксирован на
  17763 и до 1903 не обновляется в принципе — это отдельная редакция с 10-летним
  циклом, а не отставшая сборка. То есть `wsl --import ... --version 2` и
  `systemd=true` из README здесь неисполнимы никакими правами.
- **Актуальный Docker Desktop требует Windows 10 21H2/22H2 и новее** — 17763
  ниже минимума. Теоретически остаётся легаси-путь: Docker Desktop 2.x на
  Hyper-V-бэкенде (редакция Enterprise это позволяет), но это снятая с поддержки
  версия, и включение Hyper-V всё равно требует администратора.

**Следствие:** команда `bash scripts/check_clean_deploy.sh` — та самая «проверка
развёртывания одной командой» — на этой машине не запускается: скрипт первым
делом делает `command -v docker` и выходит с `ABORT: docker is required`.

### Что из-за этого осталось непроверенным

Ни одна из этих команд здесь не выполнялась и об их состоянии я ничего сказать
не могу:

- `colcon build --packages-select ros_msgs`
- `bash scripts/check_sdf.sh`
- `bash scripts/run_skeleton.sh box_300x200x200 B`
- `bash scripts/run_stream.sh`
- `bash scripts/smoke_belt.sh`, `smoke_multi_item.sh`, `smoke_estop*.sh`
- `bash scripts/ci_e2e.sh`, `bash scripts/check_clean_deploy.sh`

Это весь контур камера → классификатор → дивёртер, то есть исполнительная часть
и интеграция. По критериям это самая тяжёлая часть баллов. **Зелёная Python-часть
её работоспособность не доказывает** — она доказывает только то, что ядро
восприятия и классификации считает правильные числа.

---

## 3. В README нет требований к версии Windows

Раздел «Требования» говорит: Docker, либо Ubuntu 22.04 «нативно или в WSL2»,
либо Python 3.11+ для Python-части. Нижняя граница версии Windows не названа
нигде. Из-за этого машина выглядела подходящей — «Windows 10, 64 бита, Enterprise» —
а на деле не тянет два пути из трёх.

Предложение: добавить в «Требования» строку вида «Windows 10 версии 21H2 (build
19044) и новее либо Windows 11; LTSC 2019 (build 17763) не подходит: WSL2
недоступен, Docker Desktop ниже минимальной версии».

---

## 4. Git: два дефекта конфигурации, оба блокируют обмен кодом

**4.1. `git fetch` и `git push` виснут намертво.** Первая же попытка
`git fetch --all` не завершилась за 2 минуты и была убита по таймауту.
При `GIT_TERMINAL_PROMPT=0` диагноз виден явно:

```
fatal: User cancelled dialog.
fatal: could not read Username for 'https://github.com': terminal prompts disabled
```

Причина: credential helper **настроен** (`credential.helper=manager` из
системного `C:/Program Files/Git/etc/gitconfig`, `git-credential-manager.exe` на
месте) — но на момент проверки сохранённой учётки для `github.com` ещё не было.
GCM пытался открыть графический диалог логина, а в неинтерактивной сессии этот
диалог показать некому, и он отменял сам себя.

Сеть при этом полностью рабочая — `pypi.org` и `github.com` отдают 200, пакеты
скачались. Дело было только в учётных данных.

**Закрыто 02.08 и проверено.** После однократного интерактивного входа GCM
положил учётку в Windows Credential Manager, причём постоянно:

```
Target: LegacyGeneric:target=git:https://github.com
User:   Ivana-banana
Local machine persistence
```

Ключ заведён на **хост**, а не на конкретный репозиторий (`useHttpPath` для
github не включён), поэтому одна запись покрывает и `origin`, и `true` — оба
живут на `github.com`. Повторный вход не требуется.

Проверено прямым прогоном из неинтерактивной сессии, без единого запроса пароля:
`git push true main` (ушёл в оба репозитория), `git ls-remote true main`,
`git ls-remote origin main`, `git fetch --all` — все `exit=0`.

Оговорка для того, кто будет это читать: пока учётки не было, `git pull` здесь
не падал с ошибкой, а **зависал** — это легко принять за медленную сеть. Правило
`CLAUDE.md` «проверку начинать с `git pull`» на неподготовленной машине выглядит
именно так.

**4.2. Ремоут `true` не настроен.** `GIT.md` требует пушить в оба репозитория и
описывает ремоут `true` с двумя push-URL (organizers `hackathonsrus/...` +
`IR630/ozon`). На этой машине в `.git/config` есть только:

```
[remote "origin"] url = https://github.com/IR630/ozon.git
```

То есть отправить что-либо организаторам отсюда было нельзя, а требование
«истории обязаны совпадать» не выполнимо и даже не проверяемо.

**Исправлено 02.08.** Ремоут добавлен по GIT.md, с двумя push-URL — обычный
`git push true main` теперь уходит сразу в оба репозитория:

```
git remote add true https://github.com/hackathonsrus/ozone-tech_ii_v_massy_883.git
git remote set-url --push true https://github.com/hackathonsrus/ozone-tech_ii_v_massy_883.git
git remote set-url --add --push true https://github.com/IR630/ozon.git
```

Итоговая конфигурация:

```
origin  https://github.com/IR630/ozon.git                                  (fetch)
origin  https://github.com/IR630/ozon.git                                  (push)
true    https://github.com/hackathonsrus/ozone-tech_ii_v_massy_883.git     (fetch)
true    https://github.com/hackathonsrus/ozone-tech_ii_v_massy_883.git     (push)
true    https://github.com/IR630/ozon.git                                  (push)
```

**Сверка выполнена.** Оба репозитория указывают на один коммит:

```
514904fc040d88a4435b230e21f07f41e43386f9   refs/heads/main   (true)
514904fc040d88a4435b230e21f07f41e43386f9   refs/heads/main   (origin)
```

Попутно выяснилось, что репозиторий организаторов **отставал на 4 коммита**
(`c369232..514904f`) — их и дослали обычным fast-forward, без `--force`, как и
предписывает GIT.md. Это ровно тот случай «пушнули из ветки с неполной
настройкой», от которого GIT.md и страхует: пока ремоута `true` не было,
push уходил только в зеркало.

**4.3. `user.name` / `user.email` не заданы.** Первый же `git commit` упал с
`Author identity unknown ... unable to auto-detect email address (got
'Ivan@PC5.(none)')`. Прописано локально для этого репозитория:

```
git config user.name "Alex Bessonov"
git config user.email "alexbessonov278@gmail.com"
```

Если машина будет использоваться дальше — имеет смысл сделать это через
`--global`, иначе то же самое всплывёт в следующем checkout.

**4.4. Мелочь: `git` не виден из PowerShell.** Бинарь есть (`C:\Program Files\Git`,
version 2.45.1), но в `PATH` PowerShell его нет — доступен только внутри Git Bash.
README в разделе про WSL даёт `git worktree add ...` именно как PowerShell-команду,
и она здесь не выполнится. Лечится дописыванием `C:\Program Files\Git\cmd` в PATH.

---

## 5. Не хватает инструментов для сборки видео

`ffmpeg` не найден, `playwright`/chromium не установлены. Значит
`scripts/build_defence_video.py` и `scripts/build_census_reel.py` здесь не
соберутся.

**Отдельно про `gh` — это расхождение с CLAUDE.md.** Там написано, что «gh стоит
в `%LOCALAPPDATA%\Programs\gh`». На этой машине такого каталога **нет вообще**,
и рекурсивный поиск `gh.exe` по `%LOCALAPPDATA%` не дал ничего. То есть
обязательная по CLAUDE.md проверка «push — не конец задачи, дождаться прогона
через `gh run watch --exit-status`» здесь невыполнима.

Обойти через веб тоже не вышло: репозитории приватные, страница Actions без
авторизации отдаёт 404. **Поэтому статус CI по трём коммитам этого отчёта
(`f1dd349`, `ce8e729`, `514904f`) не проверен** — их надо посмотреть глазами.
Изменения чисто документационные, но правило есть правило.

Формально это **не противоречит README** — он честно пишет, что ffmpeg и
playwright в `requirements.txt` не входят. Но если задача машины — готовить
сдачу, а не только считать классификацию, их надо ставить отдельно. Сами
артефакты в репозитории на месте: `census_reel.mp4` (14,2 МБ) и все 12 дорожек
озвучки `docs/report/video/narration/s01..s12.aac` лежат в гите, так что
пересборка нужна только при правках.

---

## 6. Риск: `requirements.txt` без пинов — сегодня повезло

Установка 02.08 разрешилась в:

```
numpy 2.5.1   scipy 1.18.0   trimesh 5.0.0
opencv-python-headless 5.0.0.93   pytest 9.1.1   ruff 0.16.1
```

Это **ровно те два релиза, которые по CLAUDE.md уже дважды роняли CI** — `ruff
0.16` (28.07) и `trimesh 5.0.0` (01.08). Сейчас оба прошли: набор правил ruff
теперь прибит в `pyproject.toml` (`select = ["E4","E7","E9","F"]`), а trimesh
5.0.0 конфликтовал с numpy 1.x, которого здесь нет — приехал numpy 2.5.1.

То есть защита сработала, но результат Python-развёртывания всё равно зависит от
даты установки: `requirements.txt` не пинует ничего. `docker/pip-constraints.txt`
эту дыру не закрывает — он намеренно описывает только образ (там `numpy==1.21.5`,
который на Windows и Python 3.14 просто не соберётся).

Это осознанное решение (комментарий в `pip-constraints.txt` объясняет, зачем
`requirements.txt` оставлен свободным), поэтому предлагаю не менять на ходу, а
просто знать: «у меня Python-часть зелёная» и «у тебя Python-часть зелёная» —
это не обязательно одинаковые наборы версий.

---

## 7. Что предлагаю делать

По убыванию важности:

1. **Решить вопрос с ОС или с машиной.** Развернуть здесь исполнительную часть
   нельзя. Варианты: другая машина с Windows 21H2+/11, либо нативная Ubuntu 22.04,
   либо доступ администратора + попытка легаси-Docker на Hyper-V (последнее —
   наименее надёжное, версия снята с поддержки).
2. **Git-блок закрыт целиком, делать ничего не нужно:** `user.name`/`user.email`
   заданы, ремоут `true` настроен, учётка GCM сохраняется, истории сверены и
   совпадают. Единственное необязательное улучшение — перенести identity в
   `--global` (глобального `~/.gitconfig` на машине нет вовсе), иначе в
   следующем checkout `git commit` снова упадёт на `Author identity unknown`.
3. **Дописать в README минимальную версию Windows** — сэкономит следующему
   человеку тот же день.
4. **Добавить `C:\Program Files\Git\cmd` в PATH**, чтобы PowerShell-команды из
   README работали.
5. **Поставить `gh`** — без него не выполняется обязательная по CLAUDE.md
   проверка CI после push, а веб-путь закрыт приватностью репозиториев.
   Заодно поправить в CLAUDE.md путь `%LOCALAPPDATA%\Programs\gh`: здесь по
   нему ничего нет.
6. Если машина нужна для сдачи — поставить ffmpeg и playwright+chromium.

## 8. Границы этого отчёта

- Проверено **только** то, что перечислено в таблице п. 1, живым запуском.
- ROS 2 / Gazebo / colcon / сквозной прогон — **не проверялись ни разу**, вывод
  об их состоянии из этого отчёта делать нельзя.
- Утверждения про минимальные версии Docker Desktop и WSL2 — это требования
  вендоров, а не результат эксперимента на этой машине: поставить и убедиться
  без прав администратора я не мог.
- Изменения в репозитории — только этот отчёт и настройка ремоута `true`.
  Артефакты сборки (`.venv/`, `sim/models/items/`) в `.gitignore`, в историю не
  попали.
- **Статус CI не проверен** (п. 5): `gh` не установлен, веб-страница Actions
  приватна. Три коммита отчёта уехали в оба репозитория непроверенными.
- Часть выводов этого отчёта менялась по ходу проверки. В частности, сначала
  было записано, что учётка GCM не сохраняется — это оказалось неверно
  (фильтр по выводу `cmdkey` дал ложный отрицательный результат, сырой вывод
  показал постоянную запись). Итоговые формулировки в п. 4.1 и 4.2 подтверждены
  прямым прогоном команд.

---

## 9. Журнал повторных проверок деплоя

Проверки идут циклом раз в 20 минут: `git pull true main` → прогон гейтов →
обновление этого файла → push в оба репозитория. Цикл остановится, когда деплой
пройдёт; тогда здесь появится запись об успехе.

Контрольная команда — `bash scripts/check_clean_deploy.sh` (полный деплой:
чистый checkout HEAD → сборка запиненного образа → headless e2e). Она же
считается критерием успеха.

| # | Время | Коммит после pull | Что изменилось | check_clean_deploy.sh | Итог |
|---|---|---|---|---|---|
| 1 | 02.08, старт | `04f1302` | pull: `Already up to date`, правок не приходило | `ABORT: docker is required` (exit 1) | **не пройден** |
| 2 | 02.08, +неск. мин | `3103eb1` | приехал `29697b1`: README, GIT.md, decisions.md, текст abort'а | `ABORT` (exit 1), docker отсутствует | **не пройден** |
| 3 | 02.08, +20 мин | `dadb2b3` | приехал `dadb2b3`: режим `--python-only`, 2 теста на формулировки | полный — exit 1; `--python-only` — **PASS, exit 0** | **частично, потолок машины** |
| 4 | 02.08, +40 мин | `20b1e1e` | приехал `20b1e1e`: только docs (decisions.md, handover-manual-tasks.md), кода не трогал | полный — exit 1; `--python-only` — **PASS, exit 0** | **частично, без изменений** |
| 5 | 02.08, +40 мин | `ed9c6cf` | приехал `fb494c8`: бюджет E-stop-смоука 180 → 300 с | `--python-only` — **FAIL, exit 1**, 1 упавший тест | **регрессия поймана** |
| 5а | 02.08, сразу за ней | `c2b965e` | приехал `b8d89b4`: бюджет закреплён инвариантом вместо числа | полный — exit 1; `--python-only` — **PASS, exit 0** | **частично, восстановлено** |
| 6 | 02.08, +60 мин | `4e9c4aa` | приехал `4e9c4aa`: встречный прогон ПОЛНОГО гейта у автора на `a1717e1` | здесь `--python-only` — **PASS, exit 0**; полный — **PASS у автора** | **УСПЕХ, цикл закрыт** |

Состояние окружения на итерации 1 (перепроверяется каждый раз, вдруг доставят
инструменты): `docker` — нет, `wsl` — нет, `gh` — нет, `ffmpeg` — нет,
каталога `C:\Program Files\Docker` нет, build ОС 17763.

### Итерация 2 — разбор присланного исправления `29697b1`

Правки приняты и подтверждены прогоном. Что сделано и что это меняет:

- **README получил границу версии Windows** — ровно то, чего не хватало (п. 3).
  Следующий человек не потратит день на выяснение, что машина не подходит.
  Туда же добавлен абзац про ffmpeg/playwright/chromium/gh (п. 5).
- **`check_clean_deploy.sh` теперь объясняет выход**, а не просто обрывается.
  Проверено — новый текст печатается:

```
ABORT: docker is required for the clean-deploy gate.
  Это шлюз развёртывания: он собирает образ из чистого checkout.
  Без docker его пройти нельзя — заглушки тут не будет.
  Windows: нужна 10 версии 21H2 (build 19044)+ или 11.
  На LTSC 2019 (build 17763) Docker Desktop не ставится в принципе,
  и WSL2 тоже недоступен — см. docs/deploy-report-2026-08-02.md.
  Python-часть при этом работает: pytest -q, ruff check .
```

- **`GIT.md` получил раздел про первый запуск на новой машине** — три git-дефекта
  из п. 4 теперь описаны там, где их будут искать.

**Но деплой от этого не проходит, и коммит этого не обещал** — автор прямо
пишет: «Он не приближает проверку исполнительной части». Изменение улучшает
диагностику отказа, а не снимает его. Гейт по-прежнему `exit 1`, потому что
docker на этой ОС не появится.

Python-гейты на смердженном коммите `3103eb1` зелёные: `ruff check .` чист,
`pytest -q` → **674 passed, 20 skipped** за 109 с. Расхождение с числами автора
(687 passed / 7 skipped) кажущееся: всего тестов одинаково, **694**, просто
здесь 13 ROS-зависимых уходят в skip, а в его окружении выполняются. Это ещё
одна иллюстрация того, что исполнительная часть тут не покрыта.

**Замечание по «Мешку», снятое автором, принято.** Отчёт цитировал
`Мешок K=0.800 -> D` как вывод эталонного анализатора — он совпадает с тем, что
напечатано в README, и расхождением с продом не является: там вмешивается
политика мягкого кома. Правок не требуется, вопрос закрыт.

### Итерация 3 — `--python-only`, и почему это правильный ответ

Приехал `dadb2b3`. Он не пытается обойти блокер и не подсовывает заглушку —
он отделяет проверяемое от непроверяемого. Это ровно то, чего не хватало:
раньше гейт умирал на первой строке и не говорил **ничего** о том,
разворачивается ли репозиторий; теперь половина, которая здесь работает,
проверяется из чистого checkout, а не из чьего-то рабочего дерева.

**Полный гейт — `exit 1`**, и его abort теперь называет выход:

```
ABORT: docker is required for the FULL clean-deploy gate.
  ...
  ЧТО МОЖНО СДЕЛАТЬ ЗДЕСЬ И СЕЙЧАС:
    bash scripts/check_clean_deploy.sh --python-only
```

**`--python-only` — `exit 0`, прошёл целиком.** Что он реально сделал:
`git archive HEAD` во временный каталог, свой venv, установка по
`requirements.txt`, `ruff check .`, `pytest -q`, `build_item_models.py`,
`measure_validation.py` (recall 8/8, category 8/8, contract 8/8, reject 4/4),
`measure_private_shapes.py` (**20/20**, включая три yaw-ориентации). Финальное
сообщение честно перечисляет непокрытое:

```
PASS (--python-only): чистый checkout развернулся, линт, тесты и
измерительные гейты зелёные.
НЕ ПОКРЫТО и об этом нельзя делать выводов: ROS 2, Gazebo, colcon,
сквозной прогон камера -> классификатор -> дивёртер.
```

Отдельно отмечу решение закрепить эти формулировки тестом: если из финального
сообщения пропадёт любое из слов «ROS 2 / Gazebo / colcon / сквозной прогон»,
тест упадёт. Зелёная половина действительно легко читается как зелёное целое, и
защита от такого прочтения на уровне теста — правильный ход.

**Что это меняет по существу.** Деплой на этой машине достиг потолка: всё
проверяемое проверено и зелёное, непроверяемое явно названо. Полным деплой
здесь не станет — не из-за кода, а из-за редакции Windows.

**Мелочь, замеченная в этом прогоне.** В выводе `measure_validation.py` через
Git Bash тире и прочерки таблицы печатаются как `?` — рассинхрон кодовой
страницы консоли Windows и UTF-8 вывода скрипта. На результат не влияет
(тот же скрипт из PowerShell печатает корректно), в гейтах не участвует, но
если организаторы будут смотреть логи с Windows — они увидят то же самое.

### Итерация 4 — обе стороны сошлись на одной формулировке

Приехал `20b1e1e`, кода не трогает: обновлены `docs/decisions.md` и
`docs/handover-manual-tasks.md`. Автор зафиксировал у себя тот же статус, к
которому пришёл этот отчёт, и прямым текстом отказался записать в задачи «деплой
на второй машине проверен» — назвав это подменой половины целым.

Это важно зафиксировать здесь, потому что снимает главный риск такой проверки:
**зелёный `--python-only` не превращается в «решение развёрнуто».** Обе стороны
теперь пишут одно и то же — классификация подтверждена независимо, исполнительная
часть подтверждена только машиной автора и CI.

Прогон на новом HEAD ничего не изменил, как и ожидалось: полный гейт — `exit 1`,
`--python-only` — `exit 0`. Окружение перепроверено, всё по-прежнему
отсутствует: `docker`, `wsl`, `gh`, `ffmpeg`; build ОС 17763.

**Состояние цикла.** Со стороны репозитория закрыто всё, что закрывалось. Дальше
итерации будут повторять один и тот же результат, пока не изменится **машина**.
Полным деплой станет при одном из трёх условий:

1. другая машина — Windows 10 21H2 (build 19044)+ или Windows 11, где ставится
   Docker Desktop и доступен WSL2;
2. нативная Ubuntu 22.04 — тогда работает и `provision_wsl.sh`, и полный гейт;
3. права администратора здесь — но это лишь частично: WSL2 на LTSC 2019
   недоступен и с админом, остаётся только легаси-Docker на Hyper-V, снятый с
   поддержки.

До выполнения одного из них статус этого отчёта не изменится.

### Итерация 5 — цикл поймал настоящую регрессию

Первая итерация, где проверка сработала не как формальность.

Приехал `fb494c8`: бюджет E-stop-смоука в `scripts/ci_e2e.sh` поднят со 180 до
300 с (разбор красного прогона 30730053772 с `exit 124`). Правка по существу
верная — E-stop-смоук поднимает тот же многоголовый мир, что и шаг скелета, и
гоняет поверх него поток, а бюджета имел меньше, чем более простой шаг.

**Но гейт на этой машине сразу упал:**

```
assert "timeout 180 bash scripts/smoke_estop_stream.sh" in e2e
E  assert 'timeout 180 ...' in '... timeout 300 bash scripts/smoke_estop_stream.sh'
FAILED tests/test_ci_fixture.py::test_ci_e2e_runs_occupied_estop_with_text_only_fixtures
1 failed, 675 passed, 20 skipped
```

`tests/test_ci_fixture.py` проверял бюджет **литеральным числом**, поэтому
изменение капа сломало тест, к самому капу отношения не имеющий.

Автор поймал это сам и выложил `b8d89b4` следом, до того как я успел сообщить.
Починка правильная по сути, а не по симптому: литерал заменён инвариантом —
бюджет E-stop-смоука должен быть **не меньше**, чем у более простого шага
скелета, независимо от конкретных секунд. После неё `--python-only` снова
`exit 0`.

**Что это доказывает про сам цикл.** До этой итерации проверки повторяли один и
тот же результат и выглядели бесполезными. Здесь стало видно, зачем они: между
`fb494c8` и `b8d89b4` в `main` существовало окно с красными тестами, и оно было
поймано независимой машиной из чистого checkout. Это ровно тот сценарий, от
которого страхует правило «push — не конец задачи».

**Что это НЕ меняет.** Полный гейт по-прежнему `exit 1`. Обе правки этой
итерации касаются CI и ROS-контура, то есть той половины, которую здесь
подтвердить нечем: сам `ci_e2e.sh` тут не запускается. Проверено ровно одно —
что его текст согласован с тестами.


---

## 10. Встречная проверка со стороны автора: полный гейт на текущем HEAD

Отчёт выше показывает половину, которую та машина проверить не может. Эта
половина проверена здесь — но у неё своя граница, и её важно назвать сразу.

**Прогон:** `bash scripts/check_clean_deploy.sh` (ПОЛНЫЙ, не `--python-only`),
коммит `a1717e1`, WSL Ubuntu-22.04, docker 29.6.1.

```
box_300x200x200 -> B: PASS (cycle 29.5s from launch)
  stages: bringup 18.8s, feed 2.5s, transit+verdict 8.2s
  legacy origin-scored verdict: PASS (body-scored: PASS)
[perception] item 1: 301x200x198 mm K=0.5148 heads=2
[classifier] item 1: B (k=0.545355, conf=0.94, n=13)
blade C after recovery gate: 0.000 rad
PASS: occupied E-stop froze the cell; feedback gate accepted parked C/D; fresh B crossed straight
PASS: clean checkout built the pinned image and the headless e2e smoke is green
```

**Что это доказывает.** Из чистого checkout (`git archive HEAD`) собирается
пиннутый образ и внутри него проходит сквозной headless-прогон камера →
классификатор → дивёртер, включая occupied-E-stop с гардом восстановления. То
есть половина, недоступная на LTSC-машине, на текущем коде работает — и работает
именно из checkout, а не из чьего-то рабочего дерева.

Попутно подтверждён поднятый бюджет E-stop-смоука (180 → 300 с, коммит
`fb494c8`): здесь bringup занял **18.8 с** против 30 с в том же шаге на раннере
CI, а красный прогон `30730053772` упирался в кап на секунду позже гарда
восстановления. Разброс в полтора раза по одному и тому же шагу — это и есть
загрузка машины, из-за которой кап должен иметь запас.

**Чего это НЕ доказывает, и это главное.** Прогон сделан на машине автора.
Он **не закрывает** требование «вторая физическая среда»: та же связка WSL,
тот же docker, те же слои. Независимое подтверждение с чужой машины есть только
для Python-половины (п. 1). Полный гейт на втором железе по-прежнему открыт, и
в отчёте это остаётся так до реального прогона на машине с Docker.

---

## 11. Итог: деплой успешно завершён

Цикл проверок закрыт на итерации 6. Ниже — что именно считается доказанным и
чем это доказано, без округления в лучшую сторону.

### Деплой работоспособен

**Обе половины решения развёрнуты из чистого checkout и прошли свои гейты.**

- **Восприятие и классификация — подтверждено на этой машине**, Windows 10 LTSC
  2019, Python 3.14.6, numpy 2.5.1. `check_clean_deploy.sh --python-only` →
  `exit 0`: `git archive HEAD` во временный каталог, свой venv, установка по
  `requirements.txt`, `ruff` чист, `pytest` зелёный, генерация 11 SDF-моделей,
  `measure_validation` 8/8 по всем четырём метрикам, `measure_private_shapes`
  20/20.
- **Полный контур — подтверждён автором** на `a1717e1` (п. 10): пиннутый образ
  собран из чистого checkout, внутри него прошёл headless-прогон камера →
  классификатор → дивёртер с occupied-E-stop и гардом восстановления.
  `PASS: clean checkout built the pinned image and the headless e2e smoke is green`.

Обе проверки идут через `git archive HEAD` — то есть подтверждают содержимое
репозитория, а не чьё-то рабочее дерево. **Решение разворачивается.**

Сильнее всего здесь стоит независимое подтверждение классификации: вывод
`analyze_models.py` на эталонном наборе организаторов совпал с README
**построчно**, включая пограничные случаи (`Мешок K=0.800 → D`, `Ручка 9 мм → C`,
`Цилиндр K=0.749 → B`, `Пуфик 489 мм → C`). Это получено на чужой машине, с
другой ОС, другим Python и другим мажором numpy — то есть числа воспроизводимы,
а не привязаны к окружению автора.

### Что осталось открытым

Одно, и это **не отказ деплоя, а незакрытая человеко-задача**:

> Полный контур ни разу не прогонялся на **втором физическом железе**. У автора
> это своя связка WSL + docker; здесь он неисполним по ограничению редакции
> Windows. Независимое подтверждение с чужой машины есть только у Python-половины.

Формулировать это как «деплой проверен на двух машинах» нельзя — половина
проверена на одной. В отчёте и в списке ручных задач это разделено явно.

### Что было починено по ходу проверки

Проверка не свелась к «запустил и работает» — она дала пять конкретных правок:

1. **README получил нижнюю границу версии Windows** — её не было вовсе, из-за
   чего машина выглядела подходящей, а не тянула два пути из трёх.
2. **`check_clean_deploy.sh` получил режим `--python-only`** — раньше гейт
   умирал на docker-пробе и не давал **никакого** сигнала о том,
   разворачивается ли репозиторий.
3. **Abort полного гейта теперь называет выход**, а не только отсутствующий
   бинарь: какая версия Windows нужна и что можно сделать здесь и сейчас.
4. **`GIT.md` получил раздел про первый запуск на новой машине** — три git-дефекта
   (виснущий credential helper, незаданная identity, отсутствие ремоута `true`)
   описаны там, где их будут искать.
5. **Пойман красный интервал в `main`** (итерация 5): `tests/test_ci_fixture.py`
   сверял бюджет CI литеральным числом и упал от правки этого числа. Починено
   инвариантом.

### Состояние машины на момент закрытия

Git настроен полностью: identity задана, ремоут `true` с двумя push-URL, учётка
GCM сохраняется, истории обоих репозиториев совпадают. Не установлено и для
работы не требуется: `docker`, `wsl`, `gh`, `ffmpeg`, playwright — их отсутствие
ограничивает только полный гейт, сборку видео и проверку CI из консоли.
