# План: доказать работоспособность на реальной линии

> Рабочий план сессии от **25.07.2026**, построен по промпту
> `docs/prompts/session-cameras-2vs3.md`. Стратегический корень — `PLAN.md`,
> карта режимов отказа — `docs/report/path_to_line.md`, рамка целевой среды —
> `CLAUDE.md`, раздел «Целевая среда — реальная линия, а не симулятор».
>
> Статус: **утверждён владельцем, реализация не начата.**

## Контекст

Владелец сменил критерий успеха. Зелёная метрика в Gazebo (163/165 по пяти seed)
доказывает отсутствие ошибок в логике при идеальных входных данных — и больше
ничего. Целью становится **каждый непокрытый режим отказа реальной линии,
закрытый либо измерением, либо реализованным механизмом с честно названной
границей** (`docs/report/path_to_line.md` — 16 строк, из них 4 не покрыты вовсе,
1 покрыт частично). Ограничение по объёму работ снято.

Метод, окупившийся трижды (miscal-мир нашёл дефект `SIDE_BELT_MARGIN_M`,
dropout-зонд нашёл уверенную ложь 114–265 мм, noisy-мир нашёл 231 фантомное
тело): **инъецировать следствие явления → прогнать через продовый код → назвать
рамку числа.**

### Решения владельца, принятые на входе

1. **Н1 вливается в `main` только при чистой ре-валидации** (перепись 33 бит-в-бит
   + мульти-seed ≥ 163/165). Дедлайн решения о мердже — 31.07.
2. **Флаг «не уверен» — только диагностический.** `controller_node.py` и правила
   классификации не трогаем; исполнительная часть (30 баллов) не рискует.
3. **Очередь Gazebo сначала отдаётся дешёвым живым прогонам Н6.**

### Конфликт календаря — назван, не разрешён мной

`PLAN.md:107` объявляет фиче-фриз после 28.07 и день упаковки 30.07. Дедлайн
мерджа Н1 31.07 этому противоречит. Планирую гейт мерджа на **30.07, 20:00** —
так у упаковки и репетиции защиты остаётся стабильное дерево, а 31.07 работает
как резерв, а не как рабочий день. Если владелец предпочитает 31.07 — сдвигается
только этот гейт, остальное без изменений.

---

## Порядок работ: зависимость, а не ценность

Промпт упорядочил Н1–Н7 по ценности. По коду порядок другой, и он важнее:

**Боковой головке нужен фон для сегментации, которого нет** (`src/multiview.py:11-14`).
Замена фона — плоскость ленты, подогнанная по кадру. Ровно тот же примитив нужен
Н3 (пустая лента обязана лежать в плоскости) и Н5 (калибровка как процедура).
Значит `src/belt_plane.py` — общий фундамент трёх направлений, а не деталь Н1.

Аналогично, канал, по которому запасной путь Н1 обязан себя объявить
(«это измерение с бока, не сверху»), — тот же контракт, что расширяет Н2.
Контракт `ItemMeasurement.msg` залочен тестом `tests/test_msg_contracts.py:25-35`
на точное равенство множества полей: менять его надо один раз, до Н1, а не дважды.

```
Д (Н7, баги)  ──▶  Ф (belt_plane + контракт)  ──┬──▶  Н1  ──▶  развилка 2 vs 3
   offline, 30 мин       offline, ~день          ├──▶  Н3
                                                 └──▶  Н5
Н4 (вибрация) и Н6 (живые прогоны) — независимы, идут по очереди Gazebo
```

---

## Д. Два бага инструментов (первое действие, offline, ~30 мин)

Оба подтверждены дословным чтением. Багфикс начинается с падающего теста.

**Д-1 — `scripts/dump_item_frame.sh:27`.** `OUT=${2:-/tmp/frames_$SLUG}` затирает
унаследованную переменную окружения → рецепт из докстринга
`scripts/probe_noise_heads.py:33-34` молча пишет в `/tmp`.
Фикс: `OUT=${2:-${OUT:-/tmp/frames_$SLUG}}`.
Grep выполнен: **все реальные вызовы передают аргумент 2** (`scripts/diagnose_side_clouds.sh:36`,
`runs/g_dump_3cam.sh:28`, `docs/report/validation.md:125,128`), `set -u` в этом
скрипте не взведён, ни один тест не закрепляет текущее поведение. Правка —
строгое расширение, никто на клоббер не полагается.
Тест: статическая проверка строки в `tests/test_dump_item_frame.py`.
Побочно: заголовок `runs/g_dump_3cam.sh:9-12` описывает баг как шов — обновить.

**Д-2 — `scripts/probe_sensor_noise.py:69`.** `_DIR_SUFFIX_RE = re.compile(r"(_node|_dyn|_oi\d+)$")`
не знает суффикс `_3cam` → `probe_noise_heads.py:166` падает
`ValueError: cannot map dump dir 'helmet_3cam'` ровно на трёх дампах
(`bag_3cam`, `helmet_3cam`, `pen_3cam`), которые **единственные** содержат
`depth_side_*` и ради которых зонд написан.
Фикс: `r"(_node|_dyn|_oi\d+|_\d+cam)$"`. Ни один слаг каталога не кончается на
`_<цифры>cam` (`box_300x200x200` безопасен), `mystery_3cam` продолжает падать —
`test_an_unknown_dump_dir_is_loud_instead_of_scoring_against_the_wrong_truth`
не ослабляется.
Тест: расширить параметризацию `tests/test_probe_sensor_noise.py:81-87` случаями
`helmet_3cam`, `bag_2cam`, `bag_oi1_3cam`.
В том же коммите: докстринг `probe_sensor_noise.py:24-27` утверждает «no `depth_side_*`
files exist» — это стало фактически неверным, исправить.
Побочно становятся избыточными `probe_depth_dropout._RIG_SUFFIX_RE`/`slug_of_dump`
(`:109-117`) и symlink-обходка в `runs/g_probe_noise.sh:18-22` — **не удалять в
этом коммите**, их тесты зелёные, а чистка мимо зоны запроса.

**Верификация Д:** `pytest -q tests/test_probe_sensor_noise.py tests/test_dump_item_frame.py`,
`ruff check .`, и фактический прогон зонда (Gazebo не нужен):
```
wsl -d ozon -- bash -lc "cd /mnt/d/vano/ozon && python3 scripts/probe_noise_heads.py runs/frames/helmet_3cam --trials 3 --seed 7"
```

---

## Ф. Фундамент: плоскость ленты и контракт неуверенности

Ветка `feat/line-readiness`. Обе части offline, без Gazebo.

### Ф-1. `src/belt_plane.py` — новый модуль

Один модуль, читаемый за проход (правило «плоско в своём коде»). Ориентировочный
контракт:

```python
@dataclass
class PlaneFit:
    normal: tuple      # unit normal, world frame
    offset_m: float    # signed distance from origin along normal
    inlier_frac: float # доля точек в допуске — качество фита
    rms_m: float       # невязка по инлаерам

def fit_belt_plane(pts_m, expected_z_m=BELT_TOP_Z_M, tol_m=..., rng=None) -> PlaneFit | None
def residual_to_plane_m(pts_m, fit) -> np.ndarray
def height_above_plane_m(pts_m, fit) -> np.ndarray
```

**Жёсткое ограничение:** штатный путь верхней головки НЕ меняется. `_item_mask`
(`src/perception.py:167`) продолжает работать против константы `BELT_DEPTH_M` —
на ней снята перепись 163/165, и трогать её значит обнулить эту перепись.
`belt_plane` — новый потребитель, а не замена.

**Метод выбран числом: не RANSAC, а обрезанный аналитический фит.** Проектный
прогон по дампам `_3cam` дал основания (рамка чисел — ниже):
- плоскость не неизвестна. RANSAC решает «найти доминирующую плоскость среди
  неизвестно чего»; у нас она известна из чертежа с точностью бюджета калибровки,
  и захват ±20 мм вокруг ожидаемой уже выделяет ленту;
- стоимость: детерминированный SVD по прореженным точкам — **0,55 мс**;
  RANSAC на 50 гипотезах — ×50 к этому, на пути ноды в 15 Гц;
- воспроизводимость: правило проекта — вся случайность только через явный seed.
  Аналитика не добавляет seed в продовый путь вовсе;
- **но одного SVD мало, и это главный результат:** на эмуляции бюджета
  ±2 мм / ±0,2° наивный SVD по загрязнённой полосе дал `rms = 5,5 мм` и сделал
  **хуже** (97 протечек ленты против 41). Обрезка по MAD с переfit'ом обязательна,
  а не опциональна;
- **fail closed:** мало инлаеров или `rms` выше порога → вернуть НОМИНАЛЬНУЮ
  плоскость с `fitted=False`, никогда не отдавать сомнительный фит молча.

Решение записать в `docs/decisions.md` строкой.

Тесты: плоскость, снятая с дампа, лежит на `BELT_TOP_Z_M` в пределах допуска;
внесённый наклон 0,2° детектируется невязкой; кадр без ленты возвращает `None`,
а не выдумывает плоскость.

### Ф-2. Контракт неуверенности (Н2)

Расширить `Measurement` (`src/perception.py:149-156`) и
`ros_msgs/msg/ItemMeasurement.msg` полями качества. Все сигналы **уже считаются
и выбрасываются** — это плюмбинг, а не новая математика:

| Сигнал | Где уже есть | Сегодня |
|---|---|---|
| доля пикселей без возврата внутри маски | `depth_m[mask] <= 0` под рукой в `measure_item` | не считается |
| число тел в кадре (фрагментация) | длина списка `measure_items` | только в зонде |
| сколько головок реально дало вклад | `n_side`, `perception_node.py:151` | только в info-лог |
| stale-кадр отброшен | `_side_clouds:107-111` | только `warn` |
| слияние деградировало до верха | три ранних возврата `fuse_dims_mm` (`:118,:121,:130`) | неразличимы |

Пороги калибруются по уже снятым числам зонда 25.07:
- потеря детекции — абсолютный пол `_MIN_ITEM_PX = 24 px` связной компоненты,
  а не доля маски;
- рассеянное выпадение: Ручка (маска 176 px) теряется с 50 %, крупные тела
  (3,7k–13k px) — с 80 %;
- связное пятно: крупные тела не теряются **никогда**, вместо этого врут на
  114–265 мм — это и есть тихая ложь, ради которой флаг ставится;
- рассеянное 70 % даёт 9–40 «тел» на месте одного товара — детектор фрагментации.

`out.confidence = 1.0` (`perception_node.py:161`) перестаёт быть заглушкой.
`controller_node.py` не трогаем (решение владельца) — флаг диагностический,
и в отчёте это сказано прямо.

**Уточнение: новое поле, скорее всего, не нужно.** Проверено по коду — `confidence`
уже есть в контракте, жёстко равно `1.0` (`perception_node.py:161`) и **никем не
читается**: `classifier_node.py:38` берёт `est.confidence` от `ItemAggregator`,
а не `msg.confidence`; в `controller_node.py` слова `confidence` нет вовсе.
Переиспользование этого поля даёт нулевую правку `.msg`, нулевую пересборку
`ros_msgs` в WSL, нулевую правку `tests/test_msg_contracts.py` и
`check_submission.py`, а `tests/test_perception_node.py:69`
(`assert m.confidence == 1.0`) продолжает проходить, потому что штатный путь
по-прежнему пишет 1.0. Семантика поля («measurement confidence [0..1]») ровно та,
что нужна. **Рекомендую этот вариант**; новое поле заводить, только если владелец
хочет, чтобы флаг был отделён от «уверенности» по смыслу.

Обновить в любом случае: потребители `Measurement` в зондах и
`save_items_overlay`/`_dump_frame`.

**Чем доказать (объявлено ДО прогона):** расширить
`scripts/probe_depth_dropout.py` сравнением «с механизмом / без механизма» на тех
же seed. Критерий: **доля тихой лжи** (уверенное измерение вне допуска) падает,
**доля честного «не уверен»** растёт, **полная потеря товара не растёт**.

---

## Н1. Независимая детекция на боковой головке

Самая большая работа. Ветка `feat/side-detect`, отдельная от Ф.

**Измеренная проблема:** среди 68 розыгрышей с потерянной верхней детекцией
боковые головки спасли **0** — по построению. `_side_clouds`
(`src/perception_node.py:96-115`) принимает `measurement` аргументом: нет верхней
детекции → нет `position_m`/`dims_mm` → боковое облако физически не существует.
Публикация одна и внутри обработчика верхнего кадра (`:163`). Пока это так,
ответ на «а если верхняя ослепла от блика» — «ячейка встала», сколько бы камер
ни висело.

**Что строить:** второй режим сегментации, работающий против фитнутой плоскости
ленты (Ф-1) вместо скалярного порога `BELT_DEPTH_M`, включающийся **только** при
отсутствии верхней детекции.

**Шов найден и он узкий.** В `on_depth` при пустом `measurements` (`:139`) список
`item_ids` пуст, цикл `:146` не исполняется, публикации нет. Запасной путь
встраивается **между `:139` и `:140`**: если `measurements` пуст — попытаться
найти товар боковой головкой и подставить результат в тот же список. Тогда
`tracker.update` (`:140`), цикл, публикация (`:163`) и лог остаются ровно одни и
на месте, а «одна публикация в ячейке» сохраняется структурно, а не соглашением.

### Что закрыто проектным прогоном по дампам

> **Рамка этих чисел.** Сняты проектным проходом 25.07 на кадре 000 дампов
> `bag/helmet/pen_3cam`, обе боковые головки, продовыми функциями
> `src.multiview`/`src.perception`. Скрипта, воспроизводящего их одной командой,
> **пока нет** — значит по правилу воспроизводимости (Карпаты №5) они являются
> проектным основанием, но ещё **не являются числами проекта**. Первый коммит
> ветки обязан оформить их в `scripts/probe_side_fallback.py` и строку
> `docs/experiments.md`, иначе цитировать их в отчёте нельзя.

- **Сегментировать растром головки, а не в мировых координатах.** Купол шлема в
  мировых XY распадается на **два** кластера (13 142 + 18 941 точек: ближний и
  дальний ободы, между ними невидимая полость) → два фантомных item_id. В растре
  — **одна** связная компонента на всех трёх дампах, ноль ложных. Плюс стоимость:
  мировой путь требует бэкпроекции всего кадра (**31,6 мс**), растровый —
  **5–8,5 мс** на маску+label с бэкпроекцией только пикселей компоненты.
- **Примитив — призма товара, а не плоскость.** Формулировка «глубина меньше
  глубины ленты минус запас» у горизонта вырождается: луч идёт параллельно
  плоскости и 8 мм высоты превращаются в метры глубины. Проверено: эта
  формулировка нашла 0–166 px вместо правильных 113–32 095. Призма
  (полоса ленты × [запас, потолок]) особенности не имеет.
- **Порог 8 мм переносится без переопределения.** Лента с бока реконструируется
  на `+0,67 мм` (p99 `+1,25 мм`), а интервал [2, 8) мм содержит 0–43 точки из
  ~200 000: между лентой и товаром **буквально пустой зазор**. `_MIN_ITEM_PX = 24`
  тоже переносится — пустая лента даёт 0 px, а Ручка (самый тонкий товар, ради
  которого боковые головки и существуют) даёт 113/164 px.
- **Отбраковка по границе обязательна, и это самый вероятный убийца затеи.**
  Верхняя головка намеренно отвергает товары, касающиеся границы кадра
  (`perception.py:348` — товар въезжает/выезжает и даёт мусорные габариты).
  В живом прогоне это, вероятно, **доминирующая** причина отсутствия верхней
  детекции — гораздо более частая, чем дропаут глубины. Без такой же отбраковки
  fallback начнёт «спасать» ровно те частичные виды, которые верх выбросил
  осознанно, и публиковать мусор.
- **Бюджет реального времени не растёт по построению.** Fallback срабатывает
  ровно на тех кадрах, которые сегодня **самые дешёвые**: пустая лента —
  **1,28 мс** против 17–201 мс на кадре с товаром. Собственная стоимость
  fallback — 10–17 мс без body-OBB.
- **`_body_obb_dims_mm` на fallback-пути выключить.** Две независимые причины:
  он даёт основной вклад в стоимость (13,6–72,1 мс против 10–17 мс без него),
  и `tests/test_multiview.py:250` уже доказал, что на скользящем боковом облаке
  гейт `BODY_OBB_MIN_RELIEF` **всегда открыт** — этому боксу нельзя верить.
- **Фича за env-флагом, выключенным по умолчанию.** Тогда `main` может нести код
  с доказуемо пустым дифом поведения, а вопрос мерджа сводится к смене одного
  значения по умолчанию. Ре-валидация при этом гоняется **с флагом ON**, иначе
  она ничего не доказывает про мердж.

### Измеренный риск, который надо признать до реализации

**Ручка переворачивает вердикт C→B.** Истина 148×13×9 → **C** (минимальный
габарит 9 < 10). Бок-only даёт 138×15×11 → минимум 11 → **B**. При этом bag
(199×181×176 против истины 202×176×170) и helmet (356×299×276 против 352×298×282)
проходят допуск организаторов. Смягчение честное, но неполное: сегодня, когда
верх теряет Ручку, не публикуется ничего, и матрица это уже фиксировала как
«no measurement → default B» — то есть fallback **не хуже статус-кво на этой
ячейке, но и не лучше, а неверный ответ выглядит уверенным.** Это самый сильный
аргумент за диагностический флаг и за критерий «ни одна корректная сегодня
ячейка не стала некорректной».

### Ключевые проектные развилки

- **K при отсутствии верхнего вида — публиковать ровно `0.0`.** С бока K брать
  нельзя (боковая головка видит торцевой круг лежащего тела и отправит каждое
  лежащее тело в D). Из всех возможных значений `0.0` — единственное, которым
  fallback **не может создать вердикт D, для которого у него нет доказательств**:
  K входит в классификацию единственным способом, `k > ROUND_K_THRESHOLD` строго.
  `1.0` или `0.8` — изобретение доказательства, `NaN` — отравление медианы.
  **Страховка уже существует и её надо назвать в отчёте:** `ItemAggregator` берёт
  **медиану** K по кадрам одного item_id, поэтому товар с десятью верхними
  кадрами и одним fallback-кадром не сдвинется вовсе; K уедет к нулю только у
  товара, которого верхняя головка почти не видела — а для такого у нас
  действительно нет секционных данных. Медианная агрегация делает `k = 0.0`
  безопасным по построению и стоит ноль.
  (Мой первоначальный вариант «брать K из истории товара» отменяется как
  избыточный: агрегатор уже делает ровно это, и делать то же второй раз внутри
  перцепции — лишняя механика.);
- **item_id** — `ItemTracker.update` кормится списком позиций одного кадра;
  боковая детекция входит в тот же список (см. шов выше), а не открывает второй
  трекер;
- **пороги** — калибруются **только** на живых дампах `runs/frames/{bag,helmet,pen}_3cam`
  (они содержат `depth_side_neg_y_*.png`/`depth_side_pos_y_*.png`). Переиспользовать
  верхние нельзя: `MASK_MARGIN_M = 5 мм` при переносе на бок уже стоил переписи
  (весь комментарий `SIDE_BELT_MARGIN_M`), `_MIN_ITEM_PX = 24` выведен из 2,72 мм/px
  на 1,5 м у верхней головки, `BODY_OBB_MIN_RELIEF = 0.5` на скользящем облаке
  **доказанно всегда открыт** (0,86 против 0,0, `tests/test_multiview.py:250`).

### Условия победы — объявляются ДО реализации

1. **Спасение.** Сегодня `scripts/probe_depth_dropout.py:314-316` печатает ноль
   **как утверждение о контракте**, а не как измерение: «lost draws … side-head
   rescues among them: 0», и следующей строкой объясняет почему (кроп привязан к
   измерению, которого в потерянном розыгрыше не существует). Условие победы —
   **эта строка становится измеряемой и ненулевой**: боковая головка даёт
   измерение И маршрутизация верна. Порог, уточнённый после проектного прогона:
   fallback возвращает измерение на **≥ 60 %** потерянных розыгрышей, и из них
   **≥ 70 %** внутри допуска организаторов (5 мм на сторону ИЛИ 10 % объёма).
   База сегодня — **0 %**, и это не оценка, а литерал в выводе зонда.
   Ниже порога — механизм существует, но линии не помогает, и это записывается
   как есть.
2. **Бит-в-бит на штатных кадрах.** Перепись 33/33 не сдвигается ни на ячейку,
   и измерения совпадают дословно по строкам лога, а не «в пределах допуска».
3. **Цикл ячейки** — уточняю формулировку промпта, потому что «X %» здесь плохо
   определён: запасной путь работает только на кадрах, где верхняя детекция
   отсутствует, то есть где основной конвейер сделал меньше работы.
   - **худший кадр (p100) не растёт вообще — 0 %**, и это структурная гарантия,
     подкреплённая измерением: fallback срабатывает только там, где `measure_items`
     вернул пусто, а это 1,28 мс против 17–201 мс на кадре с товаром;
   - собственная стоимость запасного пути — **≤ 25 мс/кадр** (измерено 10–17 мс
     без body-OBB, который на этом пути выключен);
   - число опубликованных измерений на товар в потоковом прогоне не меняется —
     то есть кадры не роняются.
   Критерий сформулирован на **худшем** кадре, а не на среднем: бюджет уже пробит
   одной камерой (73,1 мс против периода 66,7 мс), а среднее набивают кадры,
   которых запасной путь не касается, — «+10 % к среднему» здесь бессмысленно.

### Ранний сигнал «взлетит / не взлетит» — до Gazebo

Первый рабочий шаг ветки — офлайн-харнесс по образцу
`scripts/probe_depth_dropout.py` (он уже выбивает возврат глубины у верхней
головки и гоняет **продовый** код). Прогон на `_3cam`-дампах отвечает на вопрос
«находит ли боковая головка товар вообще» за минуты, а не за 5 часов симулятора.
Если не находит — ветка закрывается до того, как заняла очередь.

**Честная граница этого гейта.** Проверено: дампы существуют и полны — по 3 кадра
× 3 головки в `runs/frames/{bag,helmet,pen}_3cam`. Но это **3 товара из 11 и по
одной позе** — база, достаточная чтобы отсеять «не взлетит», и недостаточная
чтобы калибровать пороги. Если гейт пройден, потребуется дозаливка дампов
(`SIDE=1 BRIDGE_CONFIG=sim/bridge_3cam.yaml` через `runs/g_dump_3cam.sh`, ~4 мин
на товар) — и это первая статья расхода очереди Gazebo после Н6. Планировать её
на 27.07, а не обнаружить 29-го.

### Дисциплина

Правка перцепции обнуляет перепись. Прецедент, почему это не формальность:
`cv/section-k-geometric` дала 39/48 против 32/48 на офлайн-рендере с «нулём
регрессий», а перепись в Gazebo показала **30/33 против 33/33**.
Полная ре-валидация: перепись 33 ячейки (~35 мин) и мульти-seed 165 ячеек (~5 ч).

---

## Н3. Рантайм-самодиагностика: пустая лента как эталон (режим #8)

Дёшево после Ф-1. Между товарами камера видит пустую ленту, и та обязана лежать
в плоскости. Невязка к фитнутой плоскости + доля пикселей без возврата на пустой
ленте — два непрерывных бесплатных индикатора. Уход плоскости = дрейф экстринсиков
или грязь на оптике.

**Условие победы, объявленное заранее:** индикатор срабатывает **раньше**, чем
ломается маршрутизация. Проверяется на мирах с внесённой деградацией
(`make_miscal_world.py`, `make_noisy_world.py`) — то есть на уже существующих
инструментах.

Известная ловушка, которую индикатор обязан ловить: кадр `bag_oi1_dyn` — это
фактически пустая лента; при σ ≤ 0,2 % он честно не даёт детекции, при 0,5 %
даёт **уверенное измерение фантома**. Это самый ценный случай флага и
одновременно самый трудный: фантом по всем внутренним признакам «уверенный»
(маска большая, дырок нет, границы не касается). Ловит его именно контроль
плоскостности фона, а не контроль дырок — поэтому Н3 не факультативен к Н2.

---

## Н4. Вибрация и дрожание ленты (режим #9)

Новый генератор `scripts/make_vibrated_world.py` по готовому образцу двух
существующих. Шаблон копируется буквально (оба — regex по тексту SDF, не парсер):
1. `X_present(sdf_text)` — что мир реально содержит;
2. узкая нежадная регулярка, привязанная к имени элемента;
3. счётчик замен сверяется с перечислением → `raise` вместо неатрибутируемого мира;
4. форматирование float в 9 значащих цифр (иначе вносимая ошибка не переживает
   собственный файл);
5. явный печатный отчёт, что изменено.

Замерить: при какой амплитуде ломается сегментация и при какой — маршрутизация.
Расчётно эффект складывается с рассинхроном (#3) и шумом по нормали (#4) —
**проверить, складывается ли фактически**, а не заявить.

---

## Н5. Калибровка как эксплуатационная процедура (режим #16)

После Ф-1. Продукту нужна процедура, а не константа: мишень, отчёт о невязке,
**отказ работать вне бюджета**. `SIDE_BELT_MARGIN_M = 8 мм` сегодня закладывает
запас вместо того, чтобы снимать зависимость.

**Оговорка, уже стоившая времени и повторяемая здесь намеренно:** RANSAC **не
является** «решением Ручки». Ручка проходит через кроп
(`tests/test_multiview.py:121-125`), её высота берётся максимумом по облаку, и
отсечка снизу её не двигает. Не переоткрывать.

---

## Н6. Достроить частично закрытые режимы — первыми в очередь Gazebo

Решение владельца: дешёвые живые прогоны идут раньше долгой ре-валидации Н1.
Каждый — одна-две ячейки, десятки минут, и каждый переводит строку таблицы
режимов отказа из «спроектировано» в «реализовано».

- **#1 потеря головки в смене.** Механизм под тестами
  (`test_a_lost_head_degrades_to_the_top_measurement_instead_of_crashing`), но
  живого прогона с отключением головки в середине потока не делалось. Отдельно:
  `cameras.md` §8 называет пробел — проверки «боковой кадр без верхнего не
  публикует ничего» в `tests/test_perception_node.py` нет. Добавить тест в том же
  коммите.
- **#2 дрейф экстринсиков.** Проверен **один угол** — тангаж. Расширить
  `make_miscal_world.py` рысканием и креном (сегодня в `:70-72` правится только
  `pitch`), прогнать по ячейке на каждый угол.
- **#15 чужая серверная среда.** `check_clean_deploy.sh` не гонялся на чужой
  машине ни разу. Это одновременно режим отказа и пункт сдачи; Gazebo не
  конкурирует — работа человеческая.

---

## Развилка «две головки или три» — после Н1

Владелец решил: на линию закладываем несколько камер, по симуляторной метрике
это не пересматривается. Открыт вопрос, **во что вкладывать инженерное время.**

**Главный тезис, который меняет постановку:** весь вред третьей головки идёт из
одного места — `fuse_dims_mm` (`src/multiview.py:141`) берёт покомпонентный
максимум, поэтому шумовой выброс ЛЮБОЙ головки может только раздувать габарит.
Габарит есть экстремальная статистика, а максимум из N розыгрышей растёт с N.
Отсюда числа «за две»: Ручка 43,6 → 78,0 → **182,9** мм, Мешок 33,6 → 36,6 →
**105,1** мм при σ = 0,5 % дальности.

Три из четырёх ограничений стойки живут **в слиянии, а не в числе камер**.
Решать развилку на старом слиянии — значит измерять дефект слияния.

**Замер, способный перевернуть вывод — структурное слияние × три головки.**
Не мерилось ни в одной ветке. Соседняя `feat/two-cameras` дала структурной схемой
(след сверху + высота из бока) 30/33 против 29/33 у монотонной; отдельно три
головки дают 22/23 против 19/23 у двух. Перемножение никто не проверял, а
структурная схема **не берёт максимум** — то есть именно она снимает механизм
вреда третьей головки.

Хорошая новость: **ничего изобретать не нужно, оба куска уже написаны.**
- Офлайн-стенд: `scripts/probe_camera_count.py` содержит правила
  `structural-gated` и `union-prod` (`:192`) и четыре конфигурации головок
  (`:61-66`) — замер «2 vs 3 на новом слиянии» стартует без Gazebo.
- Сам структурный оценщик лежит в `true/feat/two-cameras`, коммит **75b229c**
  («структурный оценщик — допуск 27→30, роль боковой камеры = высота»):
  +237 строк в `src/perception.py` и `tests/test_perception_fusion.py` (201 строка).
  Это **порт, а не cherry-pick** — на той ветке слияние жило в `src/perception.py`,
  у нас оно вынесено в `src/multiview.py`.

Два других неизмеренных:
- **асимметрия двухголовой стойки** — довод произносится с 21.07 и ни разу не
  проверен. `dump_item_frame.sh` уже принимает `SPAWN_Y` (`:34`), то есть дамп
  товара со смещением к дальнему краю стоит одной ячейки Gazebo;
- **механическое центрирование потока** как более дешёвая замена третьей камере —
  отраслевая практика, в проекте не рассматривалась вообще. Расчётная записка +
  геометрия, кода не требует.

---

## Очередь Gazebo и календарь

Симулятор держит **один** прогон. Машина может уснуть посреди длинного прогона —
25.07 так погиб замер на 12 ячеек.

| День | Offline (параллельно) | Очередь Gazebo |
|---|---|---|
| 26.07 | Д (два бага), Ф-1 `belt_plane`, Ф-2 контракт | Н6-#1 отключение головки; Н6-#2 рыскание |
| 27.07 | Н1 офлайн-харнесс на `_3cam` → **гейт «взлетит/нет»** | Н6-#2 крен; **дозаливка `_3cam`-дампов** (если гейт пройден) |
| 28.07 | Н1 реализация; Н3 индикатор | Н4 генератор + свип амплитуд; асимметрия (`SPAWN_Y`) |
| 29.07 | Н2 зонд «с механизмом / без»; Н5 процедура | **Н1: перепись 33** (~35 мин) → мульти-seed 165 (~5 ч) |
| 30.07 | развилка 2 vs 3 офлайн на `probe_camera_count` | добивка мульти-seed |
| **30.07 20:00** | — | **гейт мерджа Н1** (см. конфликт календаря выше) |
| 31.07–01.08 | отчёт, `path_to_line.md` переписан по факту | резерв |

Параллельно и за ресурсы не конкурируют: монтаж ролика по
`docs/report/presentation_outline.md`, заливка в облако и ссылка в `README.md:53`
(**подставляет владелец**, не мы), `check_clean_deploy.sh` на чужой машине,
вливание ветки и тег `v1.0-submission`, репетиция защиты.

---

## Верификация

**На каждый коммит:** `ruff check .` и `pytest -q` зелёные. Базовая линия на
хосте — 587 passed / 36 skipped / **7 failed**, все 7 — отсутствие `cv2` в
локальном `.venv`; в WSL `ozon` сюита зелёная целиком. Рост числа падений сверх
этих 7 — стоп-сигнал.

**Прогоны в WSL — только файлом-драйвером** `wsl -d ozon -- bash runs/<драйвер>.sh`.
Однострочники с `$var` и `$(...)` ломаются: внешний Git Bash раскрывает их раньше
WSL. Образцы: `runs/census_2cam_sweep.sh`, `runs/g_*.sh`. `LOGDIR`/`NODE_LOG`
внутри `runs/`, никогда `/tmp` (WSL стирает `/tmp` при выходе последнего процесса).

**Ловушка `BRIDGE_CONFIG`:** `launch/skeleton.launch.py` ждёт имя **без** префикса
(`bridge_3cam.yaml`), `scripts/dump_item_frame.sh:58` — **с** префиксом
(`sim/bridge_3cam.yaml`). Перепутаешь — прогон молча уйдёт в однокамерный мир.
Дефолтный мир всех прогонов — `sim/worlds/cell_diverter.sdf`, **одна** головка.

**Сквозной прогон** перед любой вехой. «Должно работать» не существует.

---

## Дисциплина и чего НЕ делать

- **Условия победы объявляются ДО прогона** — так сделано 25.07, и поэтому
  решение читается как инженерное, а не как подгонка.
- **Каждое число — со своей рамкой:** что доказывает и что НЕ доказывает. Числа
  из разных рамок не складывать (урок 92 %).
- **Отрицательный результат — полноценный результат.**
- Каждый значимый прогон — строка в `docs/experiments.md`, каждое решение — строка
  в `docs/decisions.md`, **сразу**.
- Новый модуль или функция с логикой → pytest-тест в том же коммите.

Не делать:
- **не править ядро вне отдельной ветки и без полной ре-валидации;**
- **не гонять мульти-seed трёх головок ради маршрутизации** — доказано
  конструктивно (`cameras.md` §8), что превысить 163/165 они не могут; если
  гонишь, то ради ДОПУСКА измерения, и это объявляется отдельной целью;
- **не менять метрику `probe_occlusion_heads.py`** в одиночку — соглашение держит
  сопоставимость с `census_tolerance.py`;
- **не трогать `scripts/explain_section_k.py` и `tests/test_explain_section_k.py`** —
  незакоммиченная работа другого участника в том же дереве;
- **не подставлять ссылку на видео в `README.md:53`** — `check_submission.py`
  обязан выдавать ровно одно замечание; позеленевший чекер означает вписанную фикцию;
- **не тюнить пороги CV на синтетике** — только на живых кадрах Gazebo;
- коммитить **только свои файлы** точечным `git add <путь>`, никогда `-A`, `.`
  или `commit -a`; `git push` настроен на оба ремоута сразу.
