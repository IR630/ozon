# Монтажный сценарий видеодемонстрации (задача H2)

> ## СТАТУС 31.07: сценарий описывает ролик, который НЕ отгружается
>
> Видеодемонстрацией сдачи работает `docs/report/video/census_reel.mp4` (6:03, все
> 33 ячейки переписи, счётчик до 33/33) — он лежит в репозитории. Разбор решения
> перенесён в питч, поэтому повествовательный ролик с карточками поверх клипов не
> отгружается, а `demo_reel.mp4` в репозитории отсутствует. Драматургия ниже
> сохранена как рабочий материал: она объясняет, зачем нужен каждый сегмент, и
> пригодится, если повествовательный ролик решат вернуть. Как артефакт сдачи он не
> заявляется.

> **Статус 30.07: ролик ПЕРЕСОБРАН на отгружаемой ДВУХкамерной стойке** —
> `docs/report/video/demo_reel.mp4`, 5:44, 1280×720, без звука; пересобирается
> командой `python scripts/build_demo_reel.py`.
> Текст закадра под этот файл, с тайм-кодами по факту сборки, —
> `video_narration.md`. Этот документ остаётся драматургией: зачем каждый сегмент
> и что он доказывает. Что произносить — в `video_narration.md`.
>
> Ролик — **обязательная видеодемонстрация** (`task.md`); отдельно `task.md`
> ограничивает **защиту** семью минутами. Это разные вещи: 5:44 выбрано так, чтобы
> ролик годился и как видеодемонстрация, и как тело защиты с запасом на вопросы.
> Числа сверены с пакетом **30.07** (`README.md`/`final_report.md`/`calc_vs_sim.md`)
> и совпадают с декой; не менять их при озвучке.
> Слайдовая раскадровка живой защиты — `presentation_outline.md` (тот же нарратив).

## Клип-инвентарь
| Клип | Где | Содержание |
|---|---|---|
| `hero_stream_mixed_cd.mp4` | **в репо** | money shot: 5/5 непрерывной сортировки (bottle→D, plate→D, box→C, pouf→C, pen→C). **ПЕРЕСНЯТ 30.07 на отгружаемой ДВУХкамерной стойке** (38.0 с): `world=cell_diverter_2cam.sdf`, **69 строк `heads=2` в логе и ни одной `heads=1`**, `rc=0`, все пять товаров PASS (лог `runs/stream_20260730_053900_seed0`). Прежняя версия 19.07 снималась на одной верхней головке и защищалась доводом «механизм от числа головок не зависит»; довод верен, но теперь не нужен — клип показывает ту стойку, которая отгружается. Одноголовый оригинал сохранён рядом как `*.1cam-19jul.bak.mp4` |
| `rig2_bottle_D.mp4`, `rig2_box_300x200x200_B.mp4`, `rig2_box_400x400x300_C.mp4`, `rig2_pen_C.mp4` | **в репо** | по клипу на каждый исход правила. Все четыре ПЕРЕСНЯТЫ 30.07 на отгружаемой ДВУХкамерной стойке, у каждого подтверждено логом `heads=2` и `world=cell_diverter_2cam.sdf`, вердикты PASS. Обрезаны до 14 с окном вокруг замеренного пика активности кадра; постер выбран той же метрикой, поэтому лежит внутри клипа. Оговорка, посчитанная а не предположенная: у `box_400` один кадр из восьми прошёл с `heads=1` — первый, до прихода бокового кадра (штатная деградация к верхней головке). **Подпись Ручки исправлена по существу:** прежняя обещала «9 мм: габарит бьёт форму», но 9-мм тело на общем плане участка 6×10 м не видно — и не было видно во вчерашнем «крупном плане» тоже. Толщину доказывает depth-кадр с наложенными габаритами, подпись теперь говорит это прямо. Партия `rig3_*` от 29.07 снята с трёхкамерной стойки, которая больше не отгружается, и удалена с диска, чтобы в облако не уехали две противоречащие партии |
| `hero_diverter_cylinder_B.mp4` | локально + облако | Цилиндр проходит НЕтронутым в B (доказательство K-правила). **ПЕРЕСНЯТ 30.07 на двухкамерной стойке** (25.2 с), `rc=0`, лог `runs/clips2cam_proof/cylinder_B.nodes.log`: **11 кадров `heads=2`** и один первый `heads=1` — штатная деградация к верхней головке до прихода бокового кадра |
| `hero_diverter_box400_C.mp4` | локально + облако | полный цикл механизма, взят в ролик. **ПЕРЕСНЯТ 30.07 на двухкамерной стойке** (25.7 с), `rc=0`, лог `runs/clips2cam_proof/box400_C.nodes.log`: **7 кадров `heads=2`** + один первый `heads=1`, как выше |
| `hero_diverter_bottle_D.mp4`, `hero_diverter_box300_B.mp4` | локально + облако | одиночные маршруты D/B, **одна головка, 19.07 — в ролик НЕ входят**, держатся как резерв. Если пойдут в облако, конфигурацию подписывать явно |
| `skeleton_bottle_D.mp4` | локально + облако | ранний скелет, в ролик не вошёл |
| **Нужно собрать (не клипы, а screen-capture/графика):** overlay depth-кадра с маской/ID (`day9_overlay_ids_state.png` как кадр), таблица «доказано числом» (`one_pager.md`), тайминг-график, E-stop-момент | — | статичные вставки |

## Тайминг

Замысел (этот документ): интро 0:20 · задача+контур 0:40 · классификация+погранцы
1:00 · **ДЕМО money shot 1:30** · Цилиндр→B 0:35 · исполнительная часть+бережность
1:00 · метрики+УГТ 1:05 · ограничения+развитие 0:30 · аутро 0:20.

**Фактическая сборка — 5:44**, разбивка печатается командой
`python scripts/build_demo_reel.py --plan` и является источником истины для
тайм-кодов. Главное отличие от замысла: раздел «правило по клипу на каждый исход»
(4 клипа, 1:18) вырос за счёт сокращения разговорных заставок — жюри проверяет
обработку ТРЁХ категорий, и показать это клипами дешевле, чем рассказать.

## Посегментно

### 1 · 0:00–0:20 · Интро
- **Экран:** титул (трек 3, команда) → короткий проброс money-shot (3–4 с без звука).
- **Закадр:** «Участок предсортировки: камера над лентой видит товар, классифицирует
  и физически раскладывает по трём зонам. Весь контур замкнут в симуляции и
  воспроизводится одной командой.»

### 2 · 0:20–1:00 · Задача и контур
- **Экран:** схема участка A→B/C/D + блок-схема `perception→classifier→controller→шибер`
  (из `architecture.md`).
- **Титр:** `B — в сортировщик · C — негабарит · D — круг в сечении (доупаковка)`.
- **Закадр:** «Ошибка сортировки — это затор и ручной труд. Наш контур — единый ПАК,
  а не два компонента: результат классификации напрямую становится управляющим
  сигналом механизма.»

### 3 · 1:00–2:00 · Классификация и пограничные случаи
- **Экран:** depth-кадр с наложенной маской, id и подписью `dims / K` (overlay); рядом
  блок правил.
- **Титр:** `габариты 10³…450×320×320 мм · круг в сечении K=r_впис/R_опис > 0.8 ·
  приоритет габаритов над формой`.
- **Закадр:** «Классифицируем по геометрии depth, не по цвету и не по названию.
  Пограничные случаи — доказательство глубины: Цилиндр K≈0.75 — не круг, это ловушка;
  Шлем K=0.78 — впритык под порогом; Ручка 9 мм и Пуфик 489 мм — габарит важнее формы.»

### 4 · 2:00–3:30 · ДЕМО (money shot) ★ ядро
- **Экран:** `hero_stream_mixed_cd.mp4` целиком — 5 товаров подряд в C/D на финальном
  диверторе. Оверлеем по ходу: маршрут каждого (bottle→D, plate→D, box→C, pouf→C, pen→C).
- **Закадр (поверх, синхронно):** «Камера увидела → классификатор набрал уверенность
  по кадрам → контроллер по упреждению сформировал стенку лопасти ДО прихода товара →
  лента свела товар со скоростью ленты на склиз → зона. И механизм вернулся.»

### 5 · 3:30–4:05 · Цилиндр → B (K-правило вживую)
- **Экран:** `hero_diverter_cylinder_B.mp4` — Цилиндр проходит нетронутым мимо лопастей в B.
- **Титр:** `K≈0.75 < 0.8 → B: имя круглое, сечение — нет`.
- **Закадр:** «Вот почему классификация по сечению, а не по названию: у Цилиндра
  четыре продольных стяжки делают сечение некруглым — он идёт в основной сортировщик.»

### 6 · 4:05–5:05 · Исполнительная часть и бережность
- **Экран:** крупный план дивертора (замедление на формировании стенки и возврате);
  вставка `skeleton_helmet_B.mp4` или `hero_diverter_box400_C.mp4`; момент E-stop.
- **Титр:** `шибер, не толкатель · ≥1.3× (точечно 3.0×) мягче по пиковому ускорению ·
  полный цикл + возврат · E-stop/зоны`.
- **Закадр:** «Механизм выбран числом бережности: товар идёт со скоростью ленты, без
  удара выброса. Полный цикл — сигнал, стенка, склиз, зона, возврат. Безопасность:
  аварийная остановка замораживает лопасть на месте, зоны разделены.»

### 7 · 5:05–6:10 · Подтверждение числом (метрики + УГТ)
- **Экран:** статичная таблица «доказано числом» (`one_pager.md`) + тайминг-график.
- **Титр/закадр:** «Классификация — census **33/33** на актуальном ядре, multi-seed
  **163/165** по пяти seed, оба промаха — товары у порога K, ошибок исполнения ноль.
  Устойчивая производительность отгружаемой двухкамерной стойки — **9 шт/мин**
  (одноголовая 9–10 на той же матрице; 18 — короткий пик, не выдаём за sustained). Синхронизация камера→решение — медиана
  **0.042 с**. Пять величин исполнительного контура сведены с расчётом от первых
  принципов — это обоснование Исполнения Уровня 4.»

### 8 · 6:10–6:40 · Честные ограничения и развитие
- **Экран:** короткий список на заставке.
- **Закадр:** «Честно: производительность — sustained, не пик; массы и трение —
  инженерные допущения; плотный поток одной зоны деградирует; физический прототип —
  осознанный NO-GO, симуляция по условию достаточна. Развитие — DOE физики и вторая
  проекция камеры с измеренной ценой.»

### 9 · 6:40–7:00 · Аутро
- **Экран:** терминал с одной командой запуска + `33/33`.
- **Закадр:** «Каждая цифра воспроизводится одной командой и seed. Спасибо.»

## Титры/оверлеи — тексты (копировать дословно)
- `B — в сортировщик` · `C — негабарит` · `D — круг в сечении, доупаковка`
- `K = r_впис / R_опис > 0.8` · `габариты 10³ … 450×320×320 мм`
- `Цилиндр K≈0.75 → B` · `Шлем K=0.78 → B` · `Ручка 9 мм → C` · `Пуфик 489 мм → C`
- `census 33/33 · execution 0` · `9 шт/мин на отгружаемой стойке` ·
  `камера→решение 0.042 с` · `Исполнение УГТ 4 (расчёт↔симуляция)`

## Готовые вставки (собраны) и что доснять
**Готово** в `docs/report/video/inserts/` (1920×1080, SVG-источник + PNG, числа сверены):
- `metrics_card.*` — карточка «Доказано числом» (сегмент 7).
- `sync_timeline.*` — «упреждение работает с запасом» (сегмент 4).
- Оверлей depth-кадра (сегмент 3) — существующий `docs/report/img/day9_overlay_ids_state.png`.

Перерендер после правки SVG — `python scripts/render_inserts.py`. Прежняя
инструкция звала `rsvg-convert`, которого нет ни в одной среде проекта, и ровно
поэтому PNG карточки метрик пережил снятую заявку «10 шт/мин».

**Досъёмка не требуется:** все клипы, на которые ссылается сценарий, лежат
локально в `docs/report/video/`. Если понадобится другой ракурс — поза крупного
плана записана в `sim/models/spectator/model.sdf`, а `record_stream_video.sh`
принимает `SPECTATOR_POSE`.

## Какой ракурс и какая стойка в каком клипе

Спросят на защите — ответ должен быть готов, а не выведен на месте:

| Сегмент ролика | Клип | Стойка |
|---|---|---|
| 01:22 сквозной поток | `hero_stream_mixed_cd` | **2 головки, 69 строк `heads=2`, ни одной `heads=1`** (30.07) |
| 02:07–02:55 галерея исходов | `rig2_*` (4 клипа) | **2 головки, `heads=2` в логе каждого** (30.07) |
| 03:14 Цилиндр → B | `hero_diverter_cylinder_B` | **2 головки, 11 кадров `heads=2`** + первый `heads=1` (30.07) |
| 03:55 полный цикл механизма | `hero_diverter_box400_C` | **2 головки, 7 кадров `heads=2`** + первый `heads=1` (30.07) |

**Ответ теперь короткий: весь ролик снят на отгружаемой двухкамерной стойке.**
До 30.07 три клипа из четырёх сегментов были одноголовыми (19.07), и их
приходилось защищать доводом «механизм и маршрутизация от числа головок не
зависят». Довод верен сам по себе, но он требовал от жюри поверить в различие
между тем, что заявлено, и тем, что показано, — поэтому клипы пересняты, и
довод больше не нужен.

Единственная оговорка, посчитанная а не предположенная: у одиночных клипов
ПЕРВЫЙ кадр проходит с `heads=1` — боковой кадр ещё не пришёл, и слияние
штатно деградирует к верхней головке (`src/multiview.py`). Это то же поведение,
что описано у `rig2_box_400`, и оно является свойством контура, а не дефектом
съёмки.

Все ЧИСЛА заявки (33/33 чисто, 32/33 под шумом, 9 шт/мин, 0.042 с) по-прежнему
сняты отдельными прогонами, а не извлечены из видео. `hero_diverter_bottle_D` и
`hero_diverter_box300_B` остались одноголовыми (19.07), но в ролик не входят.
