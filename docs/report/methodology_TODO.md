# Дозаполнить в methodology_and_limitations.md

> Пункты, где на момент написания раздела не хватило данных в
> `docs/decisions.md` / `docs/experiments.md`. Закрывать по мере появления цифр.

1. ✅ **УГТ, матрица 4×4, закрыто 13.07; исполнение 3→4 обосновано 17.07.**
   В п. 1.7 самооценка **CV 4 × исполнение 3 = 15/20** с обоснованным переходом
   к **исполнению 4 (20/20)**: недостающее сведение расчёт↔симуляция выполнено в
   `docs/report/calc_vs_sim.md` (5 величин из первых принципов ↔ симуляция,
   совпадение). Финальный балл — заявка жюри, подтверждается командой на сверке.

2. ✅ **Раздел 5.1 (cycle time и производительность) закрыт 14.07; sustained
   перемерен 17.07.** Заявляемое — **sustained медиана 10 товаров/мин** на полной
   матрице всех 11 моделей (`run_stream_suite.sh 0 0 2`, 6 эпизодов, 33/33), p95-
   медленный такт 4, поэпизодно 4–15. Прежние 18 — пиковый короткий 3-товарный
   поток (не sustained). Startup исключён по построению, расчётный пол сведён с
   симуляцией (`calc_vs_sim.md`).

7. ✅ **Раздел 5.2 (камера→решение) закрыт 15.07 общим timestamp'ом.** Плечо
   больше не «ниже шумового пола»: стамп кадра камеры едет в хедере сообщений,
   контроллер держит оба конца на одном sim-clock и логирует `(cam->decision Xs)`.
   Измерено **медиана 0.094 с, p95 0.186 с** (n=12, seeds 0/2/3/4, routing 12/12).
   Прежняя косвенная оценка ~0.05 с была шумом двух cross-node разностей —
   заменена прямым числом (раздел 2.7).

3. ✅ **Раздел 4.4 (безопасность эксплуатации: E-stop, зоны) закрыт 12.07.**
   Реализация, runtime-smoke, разметка и честная граница программного прототипа
   зафиксированы в `docs/report/safety.md`.

4. ✅ **Финальная routing-матрица 11×3 закрыта 14.07.** В п. 1.7 опубликованы
   три body-scored census финального механизма: **33/33 ×3**, классификация без
   ошибок. Прежний отказ Helmet oi0 закрыт как дефект старого порядка стенда:
   лента запускалась под уже лежащим товаром; физичная подача идёт на ходу.

5. ✅ **Solidity для Bag закрыт отрицательным результатом.** Реальные кадры дали
   `solidity = 1.0` и для Bag, и для Plate, поэтому признак не различает классы и
   намеренно не подключён. Bag исправлен отдельным elongation-гейтом на реальном
   кадре; подбирать неиспользуемый порог solidity больше не требуется.

6. ✅ **Пограничные CV-срезы закрыты и расширены 15.07.** В `validation.md` и
   `scripts/measure_validation.py` зафиксированы восемь реальных видимых срезов
   (включая наклонный Helmet, лежащую Bottle и диагональную Pen) и четыре
   ожидаемых reject (частичные товары + документированный standing-Pen limit):
   visible recall/category/размерный contract **8/8**, expected reject **4/4**,
   max `|Δdim|=22.1 мм`. Старые кадры без RGB остаются ограничением визуального
   аудита, а не входа текущего baseline.
