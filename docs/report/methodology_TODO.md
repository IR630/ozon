# Дозаполнить в methodology_and_limitations.md

> Пункты, где на момент написания раздела не хватило данных в
> `docs/decisions.md` / `docs/experiments.md`. Закрывать по мере появления цифр.

1. **Раздел 2 (УГТ, матрица 4×4).** В отчёте не покрыт: нет самооценки, где
   решение садится на сетке 4×4 (глубина классификации × глубина исполнительной
   части). Нужно позиционирование по обеим осям.

2. **Раздел 5.1 (cycle time и производительность).** Есть только поэпизодные цифры
   (~24–28 с от старта launch, включая ~10 с подъёма узлов). Нет агрегированной
   пропускной способности (товаров/мин) и «чистого» такта без launch-оверхеда.
   Как ограничение производительности в отчёте пока не заявлено — решить, нужно ли.

3. ✅ **Раздел 4.4 (безопасность эксплуатации: E-stop, зоны) закрыт 12.07.**
   Реализация, runtime-smoke, разметка и честная граница программного прототипа
   зафиксированы в `docs/report/safety.md`.

4. **Финальная routing-матрица 11×3.** Намеренно опущена (прогон идёт). По
   завершении — вставить сводную таблицу точности по причинам (классификация /
   подача / механизм / вердикт зоны) и снять оговорку в начале Части 2.

5. **Конкретный порог solidity для Bag.** Ещё не снят с реального кадра (в коде
   `_silhouette_solidity` — только каркас, не подключён). В отчёте честно указано
   «порог по реальному кадру», числа по факту нет.
