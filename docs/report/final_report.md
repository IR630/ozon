# Итоговый отчёт — роботизированная пресортировка товаров

> Хакатон Ozon, трек 3. Команда «ИИ в массы»:  
> Иван Пушкин · Иван Резников · Максим Седов · Михаил Анцев · Владимир Зайцев.  
> Принятая программная точка: **v0.5 + Stage 26 + depth-scale fix**.  
> Подробные доказательства и команды не дублируются здесь: они связаны ссылками
> с соответствующими разделами отчёта и журналом экспериментов.

## 1. Результат в одном абзаце

Разработана виртуальная ячейка, которая на непрерывно движущейся со скоростью
1 м/с ленте измеряет товар depth-стойкой из двух головок (верхняя плюс боковой
борт; габариты и K берутся с верхней, боковая добавляет только скрытую высоту —
и только когда прибавка выше её собственного разрешения),
определяет его категорию по
габаритам и признаку круга в сечении, сохраняет `item_id` и заранее выставляет
один из двух поворотных шиберов. Товар B проходит прямо, C и D физически
перенаправляются по разным склизам. Полный контур камера → решение → воздействие
замкнут в ROS 2 Humble и Gazebo Fortress и запускается воспроизводимо из
запиненного Docker-образа.

## 2. Задача и маршруты A/B/C/D

| Точка | Назначение | Действие ячейки |
|---|---|---|
| **A** | Подача товара на участок ПАК | Камера наблюдает товар до исполнительной зоны |
| **B** | Основной сортировщик | Товар в допустимых габаритах без круга в сечении проходит прямо |
| **C** | Негабаритные товары | Шибер C выводит товар, нарушающий окно 10×10×10…450×320×320 мм |
| **D** | Доупаковка | Шибер D выводит товар с `K = r_впис / R_опис > 0.8` |

Габаритное правило имеет приоритет над формой: круглая Ручка толщиной 9 мм и
Пуфик размером 489 мм относятся к C. Основной сортировщик после B, сама
доупаковка в D и дальнейшая обработка C находятся вне границ проекта.

Организаторы разрешили перехватывать товар до накопителя. Поэтому наша ячейка
не останавливает базовый поток и перенаправляет товар в движении; это осознанная
компоновка, а не отсутствие обязательного узла.

## 3. Архитектура единого контура

```text
товар A → RGBD/depth → perception → aggregation → classification B/C/D
         → ROS 2 message → controller + прогноз положения → шибер → B/C/D
```

1. `perception_node` выделяет все видимые товары, измеряет трёхмерные габариты,
   оценивает K и поддерживает устойчивый `item_id`.
2. `classifier_node` агрегирует несколько кадров медианой и применяет формальные
   правила задачи из одного модуля `src/classification.py`.
3. `controller_node` переносит положение товара к моменту встречи с механизмом,
   ставит таймер с упреждением и допускает не более одного физического действия
   на `item_id`.
4. Позиционные шиберы C/D направляют товар по склизу; B проходит без удара и
   лишнего воздействия.

Типизированная граница между частями — `ItemMeasurement` и
`ItemClassification`; единицы зафиксированы: размеры в миллиметрах, положение в
метрах, время в секундах. Детальная схема топиков, состояний и тайминга приведена
в [архитектуре](architecture.md).

## 4. Определение и классификация

Решение не распознаёт имена 11 известных STL и не требует обучающей разметки.
Цвет не используется: алгоритм работает по глубине, поэтому произвольная палитра
упаковки не меняет классификацию по построению.

- Габариты восстанавливаются из калиброванной depth-маски и приводятся к
  ориентированному 3D-параллелепипеду тела.
- Масштаб считается попиксельным обратным проецированием глубины; это исправило
  систематический недомер выпуклых тел и подняло офлайн-допуск с 8/11 до 11/11.
- K берётся как лучший подтверждённый признак верхней или торцевой проекции.
- Частично видимый товар не получает выдуманный размер: кадр отклоняется до
  классификации.
- Физически невозможные размеры и `K` вне `[0, 1]` вызывают явную ошибку.
- У габаритных границ применяется консервативный слой с отводом безопасной
  B-категории в C; симметричной полосы около `K=0.8` нет, чтобы не испортить
  эталонный Шлем с `K=0.78`.

Полное описание признаков, правил и пограничных случаев — в
[разделе классификации](classification.md); проверка на сохранённых реальных
Gazebo-кадрах — в [валидации](validation.md).

## 5. Исполнительная часть

Из нескольких вариантов выбран поворотный шибер, формирующий направляющую стенку,
а не быстрый поперечный толкатель. Лента продолжает двигать товар вдоль стенки,
поэтому боковой импульс меньше. На тяжёлом коробе шибер измеренно мягче толкателя
не менее чем в 1.3 раза, в точечном сравнении — примерно в 3 раза.

Шиберы C и D разнесены вдоль X на 0.5 м, имеют отдельные склизы и накопительные
клетки. Перед сменой зоны планировщик требует 3.1 с: 2.5 с удержания и 0.6 с
возврата. Опасный график подачи отклоняется до запуска Gazebo. Расчётные тайминги,
транзит и предел темпа сопоставлены с наблюдениями симуляции в документе
[«Расчёт ↔ симуляция»](calc_vs_sim.md); геометрия и доступ для обслуживания — в
[компоновке](layout.md), воспроизводимый STEP/STL — в `docs/defense/cad/`.

## 6. Безопасность и нештатные ситуации

E-stop останавливает ленту, отменяет будущие действия и удерживает занятый
позиционный шибер по свежей обратной связи, чтобы не выдернуть опору из-под
товара. После ручной очистки reset ждёт подтверждённой парковки обоих шиберов и
только затем выполняет soft-start. Физический occupied-smoke с двумя коробами и
занятым C-шибером входит в обязательный e2e-gate.

Отдельно контролируются устаревшие измерения, повторная команда на один ID,
небезопасный интервал подачи, jam/feed-watchdog и потеря обратной связи. SDF
подтверждает цифровую логику и геометрию, но не заменяет аппаратный safety PLC,
межблокированное ограждение, LOTO и оценку рисков реального оборудования.
Подробности — [safety.md](safety.md) и [failure_scenarios.md](failure_scenarios.md).

## 7. Подтверждённые результаты

| Проверка | Результат | Свидетельство |
|---|---:|---|
| Непрерывная перепись актуального ядра | **33/33**, 0 timeout/execution errors | `runs/census_depthfix_c2aa3dd`, [experiments.md](../experiments.md) |
| Два принятых потоковых suite v0.5 | **33/33 + 33/33** | [soak.md](soak.md) |
| Multi-seed **отгружаемой ДВУХкамерной** стойки (seeds 0–4) | **163/165**; два K-промаха Мешка, execution 0 | `feat/three-cameras@d45f42b`, `runs/census2cam_seed0..4`, [experiments.md](../experiments.md) |
| Тот же multi-seed одноголовым fallback-путём на depth-scale базе | **163/165** — ячейка в ячейку; два K-промаха, execution 0 | `feat/three-cameras@4581d8d`, [experiments.md](../experiments.md) |
| Пять seed до depth-scale fix | **162–165/165** run-to-run; промахи только у K≈0.8, execution 0 | [soak.md](soak.md) |
| Офлайн-точность габаритов после depth-scale fix | **11/11** (чистые офлайн-рендеры, не Gazebo-кадры) | [experiments.md](../experiments.md), [decisions.md](../decisions.md) |
| Устойчивая производительность полной матрицы | отгружаемая ДВУХкамерная стойка **9 шт/мин** (30.07, RTF 0.44, all-pass 5/6, terminal FAIL 0); одноголовая 9 (28.07) и **10** (17.07), диапазон 4–15. **Чистый перезамер 30.07 на незагруженной машине: 11 шт/мин, all-pass 5/6, routed 29/33, камера→решение 0.040 с** — заявка 9 подтверждена сверху и оставлена консервативной | [mechanism.md](mechanism.md), `runs/throughput_prod2cam_clean` |
| Единственный воспроизводимый отказ потока | `oi1_pass_b`: **2/6**, повторён дважды; Мешок/Моющее/Шлем сходят с ленты влево (`y ≈ −1.3 м`), классификация верна во всех шести. Зазор 1.0→1.8 м поднимает до 3/6 — такт часть причины, не вся; полная причина НЕ найдена | `runs/diag_gap18_oi1` |
| Камера → решение | медиана **0.042 с** в общем sim-clock на отгружаемой двухкамерной стойке (30.07, n=33, p95 0.088; 0.030 с — та же метрика на одноголовой 17.07) | [soak.md](soak.md) |
| Физический occupied E-stop | PASS в pinned ROS/Gazebo CI | [experiments.md](../experiments.md) |

Числа разных ревизий не складываются в один знаменатель. 163/165 снят дважды —
на отгружаемой двухкамерной стойке (24.07) и одноголовым fallback-путём той же
камерной ветки на depth-scale базе, — и совпадение до ячейки само является
свидетельством: вердикт держится на верхней головке, добавление боковой его не
двигает. Для точного последующего `main` остаётся отдельный полный seed 33/33.
Исторический
диапазон 162–165/165 сохраняется как оценка run-to-run разброса. Короткий пик
18 шт/мин не выдаётся за sustained-производительность.

## 8. Воспроизводимость и проверка

Рекомендуемый путь — Docker Compose из корня репозитория:

```bash
docker compose -f docker/docker-compose.yml build
docker compose -f docker/docker-compose.yml run dev
```

Основные проверки:

```bash
ruff check .
pytest -q
python scripts/check_submission.py
bash scripts/run_skeleton.sh box_300x200x200 B
bash scripts/run_stream_suite.sh 0 0 2
bash scripts/check_clean_deploy.sh
```

Вся случайность задаётся явным seed; конкретная ячейка определяется тройкой
`(seed, item_index, orient_index)`. `check_clean_deploy.sh` создаёт чистый
checkout `HEAD`, собирает запиненный образ и выполняет CPU/headless e2e тем же
путём, что CI. Полные параметры и ожидаемый вывод каждой команды приведены в
корневом [README](../../README.md).

## 9. Ограничения и направления развития

- Модель использует rigid-body, заданные массы и ODE-трение; реальные ткань,
  упаковка, ресурс привода и контактные свойства не измерены.
- Реальный аппаратный стенд не изготовлен. Перед изготовлением нужны расчёт
  прочности лопасти и шарнира, подбор двигателя/редуктора, допуски, крепёж и
  аппаратная оценка безопасности.
- **Отгружается ДВУХкамерная стойка** (верх + боковой борт, решение 30.07);
  одноголовый путь остаётся переключением одной переменной окружения и деградирует
  бит-в-бит, если боковая молчит. Решение прошло через две ревизии, и обе стоит
  назвать, потому что каждая была отменена ИЗМЕРЕНИЕМ, а не мнением. Довод «доп.
  голова вредит» (шумовой gate 25.07, 31/33 против 18/33) снят 28.07: это были
  таймауты по стоимости сегментации, и после её починки стойки вышли на паритет —
  чисто 33/33 у всех, под шумом 32/33 / 31/33 / 32/33 (28.07). Двухкамерная строка
  этого ряда с тех пор устарела: та стойка несла боковую `rgbd 640×480@15`, а
  отгружаемая идёт на `depth 320×240@10` и перемерена 30.07 на перегенерированном
  шумном мире — **32/33**, то есть паритет со всеми. Обратное решение «берём три
  головки» снято 30.07: третья не купила маршрутизацию ни в одном замере, а
  мульти-seed 29.07 дал у неё 159/165 против 164/165 у одной головки — обе стойки
  в этом сравнении шли одним заходом на одних и тех же пяти seed'ах, поэтому здесь
  стоит 164/165, а не 163/165 из таблицы выше (то другой прогон, 24.07 на
  depth-scale базе; разбор — в [camera_tradeoff.md](camera_tradeoff.md)). Механизм
  разрыва найден и он структурный — слияние брало высоту как максимум по головкам,
  максимум по шумной оценке смещён вверх по построению, и 9 мм Ручки поднимались
  до 11 мм, выводя товар из-под габаритного правила. **Дефект закрыт расчётом**
  (`SIDE_HEIGHT_MIN_GAIN_MM`: боковая головка переписывает высоту только если
  прибавка больше её вертикального разрешения — 3.4 мм на пиксель по геометрии,
  4.2 мм по дампу реального кадра), после чего третья головка осталась платой
  без выигрыша: 1.9× к циклу, третий несинхронный поток, третья калибровка.
  **Чего это не даёт:** превышения по точности нет и оно недостижимо без смены
  контракта слияния — K и след принадлежат верхней головке, боковым доступна
  только высота. Многокамерность обоснована как архитектура ДОСТУПНОСТИ для
  реальной линии (тёмная, бликующая, прозрачная упаковка), и там реальные
  depth-dropout, рассинхрон и калибровочный дрейф **не измерены**: офлайн-выбор
  3A дал 92 % в допуске при типичной калибровке и 6.2 мм ошибки Мешка при потере
  верхней головы, но structural fusion, timing и калибровочный gate в `main` не
  входят. Мульти-seed 163/165 снят на одной головке; для двух подтверждён
  отдельно, для трёх **не мерян**. Числа 1/2/3 головок и критерий пересмотра
  решения сведены в [разделе о выборе камер](camera_tradeoff.md).
- Мульти-seed показал недетерминизм около `K=0.8` у Шлема/Мешка. Это честно
  сохранённый known gap, а не скрытая «идеальная точность».
- Ресурсный/latency-gate двух feedback-топиков Stage 26 должен быть повторён на
  точном окружении организаторов.

Развёрнутые допущения и отрицательные результаты приведены в
[методологии и ограничениях](methodology_and_limitations.md),
[физических допущениях](physics_assumptions.md) и
[пути на реальную линию](path_to_line.md), а также в
[журнале решений](../decisions.md).

## 10. Комплект сдачи

- исходный код и инструкции — корневой [README](../../README.md);
- карта критериев → артефакт — [criteria_coverage.md](criteria_coverage.md);
- короткое резюме — [one_pager.md](one_pager.md);
- презентация — `docs/report/slides/deck-c-ozon.html`;
- видеодемонстрация — ссылка в README после загрузки в облако организаторов;
- журналы доказательств — [experiments.md](../experiments.md) и
  [decisions.md](../decisions.md).

На 21.07 кодовая и документная часть комплекта собрана; открытые человеко-задачи
остаются явными: облачная видеоссылка, прогон чистого deploy на чужой машине,
пробная загрузка и репетиция защиты до 7 минут.
