# Ночная сессия 27→28.07: гейт мерджа стойки в `main`

Работаешь один, человека рядом нет: вопросов не задавать, развилки решать по
правилам ниже, спорное — записывать и идти дальше. Ветка `feat/line-readiness`.

**Цель ночи одна: дать владельцу утром решение «мерджим / не мерджим» с числами.**
Владелец уже склонился к мерджу; ночь должна его подтвердить или опровергнуть.
Мердж **не выполнять** — только подготовить.

**Почему это срочно:** в `main` физически нет `final_report.md`, `path_to_line.md`,
`rig_decision.md`, `cameras.md`. `README.md` ссылается на несуществующие файлы,
`check_submission.py` на `main` обязан падать — сдавать `main` в текущем виде
нельзя. Ре-валидация есть единственное, что стоит между веткой и сдаваемым деревом.

**Контекст до первой команды:** `rig_decision.md` §6–7 (открытые границы),
`docs/plan-line-readiness.md` разделы «Верификация» и «Чего НЕ делать».

## Жёсткие запреты

- **Не мерджить и не пушить в `main`.** Готовишь решение, не принимаешь его.
- Не трогать `src/classification.py`, `src/controller_node.py`, правила
  классификации, пороги CV.
- **Не трогать** `scripts/explain_section_k.py` и `tests/test_explain_section_k.py`
  — незакоммиченная работа другого участника в том же дереве.
- `git add` только точечный по путям; никогда `-A`, `.`, `commit -a`.
  Законченный шаг — коммит и `git push` (настроен на оба ремоута).

## Порядок работ

Симулятор держит **один** прогон. Шаги 1–2 — это вся ночная очередь; всё
остальное офлайн и параллельно. Шаг упал — записать и идти дальше.

**0. Проверка исходной точки.** `git fetch --all`, убедиться, что ветка синхронна
с обоими ремоутами (`origin` и `true`), `pytest -q` зелёный. Расхождение —
разобраться до запуска симулятора, а не после пяти часов прогона.

**1. [Gazebo, ~35 мин] Перепись 33 ячеек на ветке.** Цель — B0 = 33/33.

> **Ловушка, из-за которой наивное сравнение соврёт.** Формат лога на ветке
> изменился: `classifier_node.py` печатает `k=%.6f` вместо `%.3f`,
> `perception_node.py` добавил `heads=N`. Сравнение логов **построчно
> невозможно по построению** — тексты отличаются всегда. Сравнивать **разобранные
> значения**: категория по ячейкам и габариты, а не строки. Если сравнивалка
> этого не умеет — починить её, а не ослабить критерий.

**2. [Gazebo, ~5 ч] Мульти-seed, 165 ячеек.** Порог — **≥ 163/165** (база `main`).
Это главный расход ночи. Прогресс писать на диск после каждого seed: машина
может уснуть посреди прогона (так погиб замер 25.07). Упало на середине —
досчитать сколько успел и честно назвать неполноту, а не выдавать частичное
число за полное.

**3. [офлайн, параллельно с 1–2] Н5 — процедура калибровки на смену.**
Раздел Н5 в `docs/plan-line-readiness.md`. Поверх `src/belt_plane.py`: мишень,
отчёт о невязке, **отказ работать вне бюджета**. Без неё третья головка не
окупается (`rig_decision.md` §5.1): её преимущество целиком калибровочное, и при
посредственной калибровке две головки бьют три. Новый модуль → pytest-тест
в том же коммите.

**4. [Gazebo, только если ночь осталась] Дозаливка `_3cam`-дампов** через
`runs/g_dump_3cam.sh`, ~4 мин на товар: все живые числа стойки стоят на 3 товарах
из 11 в одной позе. Ловушка `BRIDGE_CONFIG` — `launch/skeleton.launch.py` ждёт имя
**без** префикса, `dump_item_frame.sh` — **с**; перепутаешь, и прогон молча уйдёт
в однокамерный мир.

## Чего ночью НЕ делать

Доводку независимой детекции с 47 % до 70 % (2–3 дня, в календарь не влезает),
переход на 8 Гц (ещё одна полная ре-валидация), Н4-вибрацию (очередь дороже
отдать гейту мерджа).

## Верификация и утренний отчёт

- Перед каждым коммитом: `ruff check .` и `pytest -q` зелёные. База в WSL `ozon`
  — **676 passed, 7 skipped**; рост падений сверх этого — стоп-сигнал.
- Прогоны только файлом-драйвером: `wsl -d ozon -- bash runs/<драйвер>.sh`.
  Однострочники с `$var` ломает внешний Git Bash. `LOGDIR` внутри `runs/`,
  никогда `/tmp` — WSL стирает его при выходе последнего процесса.
- Каждый прогон — строка в `docs/experiments.md`, каждое решение — строка в
  `docs/decisions.md`, **сразу**.

**Утреннее сообщение начинается одной строкой: `МЕРДЖ: ДА` или `МЕРДЖ: НЕТ`,**
дальше числа переписи и мульти-seed, что упало, и что осталось непроверенным.
Отрицательный результат — полноценный результат; «должно работать» не существует.
