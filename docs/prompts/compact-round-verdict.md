# Промпт следующей сессии: закрыть вопрос «компактное круглое тело»

Скопировать всё ниже в новую сессию Claude Code (рабочий каталог `D:\vano\ozon`).

---

Продолжаем работу по хакатону Ozon (трек 3). Прочитай `PLAN.md` (карту документов),
`docs/probe-models.md` и утверждённый план
`D:\vscode_data\.claude\plans\encapsulated-wiggling-pearl.md` — мы на его Шаге 2→6.

## Состояние на конец прошлой сессии (19.07)

- Ветка **`cv/section-k-geometric`**, чистая, запушена в оба ремоута.
  `src/` **побайтово идентичен main** — переписка K на геометрическое сечение
  отменена коммитом `1d3461e` (разбор: `docs/probe-models.md`, раздел
  «Отменённая попытка»). Diff ветки против main — только документы и зондовый
  гейт. Мульти-seed 165/165 коллеги остаётся валидной: ядро не менялось.
- По утверждённому плану проведена кампания измерений на **реальных кадрах
  Gazebo** (152 кадра: мешок 122 в 11 позах, мяч 17, банка стоя 11). Кадры в
  `runs/kband/*/depth_*.png` (WSL, runs/ в gitignore). Анализатор — 80-строчный
  скрипт (5 сырых признаков на кадр: k_sil, flat, k_sec, elong, rms/R оболочки);
  лежал в скретчпаде прошлой сессии
  `C:\Users\admin\AppData\Local\Temp\claude\D--vano-ozon\6e0c5e2c-b8f4-4331-b629-50e26bd5a2f2\scratchpad\kband.py`
  — если файл уже вычищен, пересоздать по описанию в плане (Шаг 1.3).

## Результат кампании: NO-GO по обоим вариантам (числа пулом)

| Критерий (зафиксирован до данных) | Измерено | Вердикт |
|---|---|---|
| Зазор K силуэта `D_min − B_max ≥ 0,10` | **−0,078** (B_max=0,878; D_min=0,799) | NO-GO |
| `D_min ≥ 0,93` | 0,799 | NO-GO |
| Запасной A: `min(rms мешка) ≥ 2× max(rms мяча+банки)` | **0,49×** (0,0365 против 0,0752) | NO-GO |
| Выборка ≥ 8 поз / ≥ 60 кадров | 11 поз / 122 кадра | GO |

Перекрытие массовое: 91 кадр мешка из 122 выше D_min. На малой выборке оба
признака выглядели проходными (зазор 0,053 при одной позе мешка; rms 3,45×) и
деградировали с ростом n — ровно тот эффект, от которого страховал критерий 2.

## Задачи, по порядку

1. **Развязать последний аналитический хвост — банку.** `D_min = 0,799` даёт
   банка, не мяч (мяч 0,933); разброс банки 0,799…0,976, а рендер давал 0,987.
   Посмотреть **глазами** `runs/kband/can_upright/overlay_*.png` (принцип
   Карпаты №1): низкие K — это краевые кадры при въезде товара в кадр, падение
   банки набок или честные полные виды? Если минимум по полным кадрам ≥ 0,93 —
   пересчитать критерии с фильтром краевых кадров **для всех наборов
   одинаково** (не только для банки!) и переоценить вердикт. Если NO-GO
   устоит — п. 2.
2. **Исполнить вариант C плана (исход по умолчанию):** ядро не трогать,
   `KNOWN_GAPS` оставить как есть; в `docs/probe-models.md` добавить раздел с
   двумя распределениями (K и rms/R, мешок против мяча+банки) и их перекрытием;
   строки в `docs/decisions.md` и `docs/experiments.md`; коммит в ветку, пуш в
   оба ремоута. Формула защиты: «мы измерили оба признака на 150+ реальных
   кадрах, обнаружили перекрытие классов и отказались ставить порог внутрь
   перекрытия».
3. **Решить судьбу ветки с пользователем:** diff против main — только документы
   и зондовый гейт, ядро идентично. Предложить влить как docs-only PR.
4. **Мяч, производительность (решение пользователя «сначала измерить как
   следует»):** один прогон дал 281 с против 16 с у короба; поштучный замер
   скорости продвижения качения не сделан. Попутная находка прошлой сессии:
   мяч, положенный с зазором 5 мм (SPAWN_Z=0.405), **стоит на месте**;
   сброшенный с 0.5 — едет. Проверить и записать.
5. **Напомнить пользователю** про вопрос жюри о трактовке «K по любой
   плоскости» (первый шаг вердикта совета, `docs/defense/council_cameras.md`) —
   отправить может только команда; к вопросу приложить контрпример с кубом
   (сечение ⊥ диагонали — правильный шестиугольник, K=0,866).

## Ловушки (проверено на своей шкуре в прошлой сессии)

- **Пороги и вердикты — только на реальных кадрах Gazebo** (`CLAUDE.md`;
  `scripts/render_depth.py`, HONESTY LIMIT). Вывод по синтетике уже один раз
  стоил отката ядра (`1d3461e`).
- Gazebo — через `wsl -d ozon -- bash -lc '...'`; многострочные скрипты и циклы
  внутри `-lc` ломаются (CRLF) — однострочники или файл через `sed -i 's/\r$//'`.
- Кадры узла (`PERCEPTION_DUMP_DIR`) дописываются с задержкой — не проверять
  каталог сразу после завершения прогона.
- Узел публикует **пост-гейтовое** K — сырые признаки только пересчётом PNG.
- Коллега (Alice) работает параллельно в этом же дереве: коммитить только свои
  файлы точечным `git add`, никогда `-A`; её незакоммиченное не трогать;
  Gazebo-прогоны разводить по времени (скрипты делают `pkill`).
- Пуш всегда в оба ремоута (`git push` настроен на оба).
