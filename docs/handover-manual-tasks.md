# Что должен сделать человек руками

Всё, что можно было автоматизировать, уже в репозитории. Ниже — только то, что
принципиально требует человека: доступы, публикация во внешние сервисы, решения
и приёмка глазами. Список пересобран **30.07** и отсортирован по срочности.

**Что изменилось 30.07, до чтения списка:** отгружаемая стойка переведена на ДВЕ
головки. Причина не вкусовая: слияние головок поднимало 9 мм Ручки до 11 мм и
выводило товар из-под габаритного правила, а вердикт начинала решать форма. Дефект
закрыт расчётом (порог прибавки = вертикальное разрешение боковой головки), прод
переключён, клипы галереи пересняты на двухкамерной стойке с `heads=2` в логе.
Разбор — `docs/decisions.md` и `docs/report/camera_tradeoff.md` за 30.07.

**Что изменилось позже в тот же день (вторая волна):**

- **Ролик пересобран целиком на двухкамерной стойке.** Три клипа оставались
  одноголовыми (19.07); пересняты, `heads=2` подтверждён логом каждого
  (money shot — 69 строк, ни одной `heads=1`). Одноголовые оригиналы лежат в
  `runs/clip_backups_1cam/` и **намеренно вынесены из `docs/report/video/`**,
  чтобы в облако не уехали две противоречащие версии одного клипа.
- **Закрыт блокер: дека не работала из чистого клона.** `.gitignore` re-include'ил
  `rig3_*` (партия трёхкамерной стойки), а дека играет `rig2_*` — четыре из пяти
  клипов галереи в репозиторий не попадали. Префлайт теперь ловит это
  (`UNTRACKED_MEDIA`, git как источник истины), а не только наличие на диске.
- **Клипы перекодированы в H.264:** `save_video.py` пишет mpeg4, который браузеры
  не играют. Если будешь переснимать что-то сам — перекодируй, префлайт проверит
  (`UNPLAYABLE_MEDIA: ... not H.264`). Готовая команда, ffmpeg берётся из
  `static_ffmpeg`, который уже стоит в окружении проекта:

  ```bash
  FF=$(python -c "import static_ffmpeg.run as r; print(r.get_or_fetch_platform_executables_else_raise()[0])")
  "$FF" -y -i clip.mp4 -c:v libx264 -pix_fmt yuv420p -crf 21 -an clip.h264.mp4
  mv clip.h264.mp4 clip.mp4
  ```
- **Числа в кадре исправлены:** карточка «Доказано числом» показывала
  `8 шт/мин · 3 головки` и `0.030 с` — заявку трёхкамерной стойки и синхронизацию
  одноголовой. Стало `9 шт/мин · 2 головки`, `0.042 с`; PNG перерендерен.
  Закадр говорил «восемь товаров» и «тридцать миллисекунд» — исправлено.
- **Длительность ролика — 5:44** (в документах жили три версии: 5:39, 5:44, 6:06).
  *Снято 31.07: этот ролик не отгружается, демонстрация — `census_reel.mp4`, 6:03.*
- **Бюджеты харнесса привязаны к отгружаемой стойке:** `SOFT_START_TRIES` 60→240,
  `CELL_TIMEOUT` 180→400. При старых дефолтах bringup двух головок (28.7 с) имел
  зазор 1.3 с, и загруженная машина жюри дала бы «belt never reached full speed».
- **Известный изъян, названный во всех документах:** эпизод `oi1_pass_b` даёт
  **2/6** и воспроизведён дважды — три товара сходят с ленты в плотном same-zone
  потоке, классификация при этом верна. Причина не найдена; за найденную не
  выдаётся. Ответ для защиты готов в `qa_prep.md`.

**Что изменилось в ночь на 31.07 (аудит перед сдачей):**

- **CI снова зелёный.** Он был красным непрерывно с 28.07, и ни одна из трёх
  причин не была дефектом решения: (а) `requirements.txt` просит голый `ruff`, CI
  ставил свежий `0.16`, у которого шире набор правил по умолчанию — 370 findings;
  набор теперь назван явно в `pyproject.toml`; (б) в colcon-контейнере
  `git ls-files` падает с кодом 128 (dubious ownership), а `tracked_files` звал его
  с `check=True` — три теста падали на каждом пуше; (в) тест свежести вставок
  сравнивал `mtime` PNG и SVG, а git его не хранит — на чистом клоне порядок
  произволен, разница была 0,2 мс.
- **Тем же фиксом закрыта дыра сдачи:** префлайт падал трейсбеком у любого, кто
  распакует архив без `.git`. Проверено `git archive | tar -x` — теперь печатает
  нормальный отчёт.
- **Префлайт научился проверять ролик.** Он держал список медиа руками и знал
  только про `hero_stream_mixed_cd.mp4`, поэтому расхождение с планом ролика было
  невидимо. Гард читает план из самого `build_demo_reel.py`. После решения сдавать
  `census_reel.mp4` он выведен из блокирующих проверок — повествовательный ролик не
  артефакт сдачи, и его недостающие исходники не должны её блокировать; вместо
  этого префлайт требует наличия самого отгружаемого ролика в репозитории.
- **Ролик переписи `census_reel.mp4` принят в комплект** — отсмотрен покадрово,
  внесён в README и в состав пакета, разрешён поимённо в `ALLOWED_LARGE_FILES`
  (13,6 МБ против порога 5). Добавлен тест, гоняющий размерный гейт против
  настоящего `git ls-files`: старый гонялся с пустым инвентарём и такого файла не
  замечал.
- **Числа приведены к отгружаемой стойке:** `31/33` → `32/33` в четырёх местах,
  расхождение `163/165` против `164/165` объяснено текстом, а не сглажено. Из
  обоснования второй головки убраны union-числа (`4.58 / 16.68 мм`) — они сняты на
  правиле слияния, которого в проде нет; вместо них измерение на продовом правиле
  (`14/33 → 30/33`).
- **Отказ `2/6` вынесен на слайд ограничений** во всех четырёх деках вместе с
  `29/33`; утверждение о причине («насыщается ход ленты») заменено на «причину не
  нашли». В отчёт добавлена ведущая гипотеза с пометкой, что она не проверена.
- **Протокол GPU-прогона 29.07 перенесён в журналы `main`** из ветки
  `5h-session-claude` — `159/165` больше не число без происхождения.

Проверить состояние комплекта в любой момент:

```bash
python scripts/check_submission.py       # должно остаться РОВНО одно замечание — п.1
python scripts/make_nextcloud_bundle.py  # собрать пакет для облака
bash scripts/check_clean_deploy.sh       # чистый checkout -> образ -> headless e2e
```

Состояние на 01.08: `ruff check .` чист, **669 passed / 7 skipped**, CI на `main`
зелёный по всем трём работам. Живые прогоны отгружаемой стойки 30.07: перепись
**33/33** (`rc=0` во всех 33 ячейках, `heads=2`), потоковая suite **5/6 all-pass,
29/33**, throughput **11 шт/мин** при заявке 9 (заявку оставили консервативной),
камера→решение **0.040 с** при заявке 0.042.

## 0. ГДЕ СОБИРАТЬ ПАКЕТ — читать до п.1

**Не на любой машине.** Видеодемонстрация теперь в репозитории целиком
(`census_reel.mp4`), поэтому раздел `01-video-demo` соберётся где угодно. А вот
папки `runs/` с логами прогонов в git нет принципиально, и на машине без неё
раздел `04-run-artifacts` — «логи, из которых пересчитывается каждая цифра» —
соберётся ПУСТЫМ. Раньше сборщик молчал об этом и отдавал код 0; теперь кричит
`GAP: ... раздел собрался ПУСТЫМ` и возвращает 1. Собрать полный пакет по-прежнему
можно только там, где логи лежат.

**Развилка по двум клипам ЗАКРЫТА 01.08 — решать больше нечего.**
`hero_diverter_cylinder_B.mp4` и `hero_diverter_box400_C.mp4` внесены в
репозиторий (927 КБ на оба). Выбор был не между «воспроизводимость ролика» и
«лишний вес»: `presentation_outline.md` держит первый из них как **клип 2
презентации**, то есть файл нужен защите, которая отгружается, а не только
повествовательному ролику, который не отгружается. Провенанс проверен — оба
сняты 30.07 в одной партии с `hero_stream_mixed_cd.mp4`, `heads=2` в логах
`runs/clips2cam_proof/`, кодек h264. Теперь и ролик (`--plan` даёт 5:44), и
презентация собираются из чистого клона.

**Пакет больше не зависит от того, что лежит у тебя на диске (01.08).** Маска
`docs/report/video/*.mp4` забирала `demo_reel.mp4`, которого нет в git: состав
пакета различался по машинам, а сама копия собрана 30.07 — ДО правки чисел
31.07, и несла снятое аудитом число. Файл исключён поимённо (`NOT_BUNDLED`),
причина печатается в MANIFEST. Если решишь вернуть повествовательный ролик в
отгрузку — пересобери его командой `python scripts/build_demo_reel.py` и убери
из `NOT_BUNDLED`; отгружать копию с диска нельзя, она устаревшая.

---

## 1. Загрузить пакет в Nextcloud и вписать ссылку — БЛОКИРУЮЩЕЕ

`README.md` содержит плейсхолдер `<ссылка на облако организаторов>`, и
`check_submission.py` намеренно возвращает `BLOCKED`, пока он там.

**Почему не сделано за тебя:** нужна публикация во внешний сервис под вашей
учёткой. Вписать туда правдоподобную строку — значит сделать чекер зелёным при
отсутствующем видео, поэтому замечание должно оставаться до реальной заливки.

**Что сделать:**

```bash
python scripts/make_nextcloud_bundle.py   # -> build/nextcloud/
```

Загрузить папку `build/nextcloud/` целиком, взять публичную ссылку, подставить её
вместо плейсхолдера в README, прогнать `python scripts/check_submission.py` —
должно стать `PASS`.

Что внутри пакета (раскладка ровно по категориям письма организаторов):

| папка | категория | объём |
|---|---|---|
| `01-video-demo` | видеодемонстрация решения | ролик переписи `census_reel.mp4` (6:03, 13,6 МБ) + клипы галереи, постеры, сценарий и текст закадра — 22 файла, 18,1 МБ |
| `02-cad-and-models` | CAD-файлы и 3D-модели в исходных форматах | 48,9 МБ (STEP/STL ячейки + 11 моделей организаторов) |
| `03-simulation` | файлы симуляций | 11,3 МБ (миры Gazebo, конфиги моста, SDF товаров) |
| `04-run-artifacts` | объёмные бинарные материалы | 0,7 МБ логов, из которых пересчитывается каждая цифра |

`MANIFEST.md` внутри пакета перечисляет всё поимённо и отдельно называет то, что
в пакет **не** попало и почему.

**Решение, которое надо принять:** в корне лежит `ozon_stream.mp4` — **397 МБ,
80 минут** записи рабочей сессии. В пакет он НЕ включён: это не артефакт сдачи.
Если это нужный материал — добавить в загрузку осознанно.

## 2. CI — ЗАКРЫТО 31.07, осталось только взглянуть перед сдачей

28.07 CI возвращён в автоматический режим (`push` в `main` + `pull_request`,
ручной запуск сохранён), и с того же дня он был **красным непрерывно, 8 прогонов
подряд**. Проверено и починено в ночь на 31.07 — три причины, ни одна не дефект
решения, разбор в шапке этого файла и в `docs/decisions.md` за 31.07. Последние
прогоны `main` зелёные по всем трём работам: `lint-and-test`, `colcon-build`,
`e2e-smoke`.

**Что осталось сделать:** перед самой сдачей ещё раз глянуть Actions — после
твоих коммитов прогон запустится заново. `gh run list --limit 3` показывает то же
из терминала.

Локальные аналоги зелёные: `ruff check .` чист, сюита **669 passed / 7 skipped**,
`bash scripts/check_clean_deploy.sh` — PASS дважды, в том числе без кэша слоёв.

## 3. Прогон `check_clean_deploy.sh` на ЧУЖОЙ машине

Локально PASS есть, но он проверяет мою машину. Вторая физическая среда — то,
что реально ловит «у нас не собирается».

**Что сделать:** склонировать репозиторий на другую машину с Docker и выполнить
`bash scripts/check_clean_deploy.sh`.

## 4. Перечитать цену решения по конфигурации прода

**Прод — ДВЕ головки** (30.07), гейт деплоя зелёный (`heads=2` в e2e-логе, PASS,
цикл 29.2 с). Числа отгружаемой стойки сняты 30.07 на текущем дереве
(`runs/chain2_20260730_024359/throughput`):

| стойка | чисто | под шумом | шт/мин, настенные | RTF | шт/мин в sim-времени | all-pass | terminal FAIL |
|---|---|---|---|---|---|---|---|
| 1 камера (28.07) | 33/33 | 32/33 | 9 | 0.47 | 19 | 5/6 | 1 (Шлем) |
| **2 камеры (прод, 30.07)** | **33/33** | **32/33** | **9** | 0.44 | 19 | 5/6 | **0** |
| 3 камеры (28.07, до фикса) | 33/33 | 32/33 | 8 | 0.43 | 19 | 5/6 | 0 |

**Главное, что надо понять про эту таблицу:** прежняя строка двух камер была
5 шт/мин при all-pass 1/6 — и она описывала стойку с ДОРОГОЙ боковой головкой
640×480 при 15 Гц, которой в дереве больше нет. То есть худшая строка таблицы
измеряла файл, а не решение. Теперь она перемерена на текущем мире.

Что важно знать помимо этого:

- **третья головка снята не по цене, а по вреду:** её слияние поднимало 9 мм Ручки
  до 11 мм и выводило товар из-под габаритного правила. Дефект закрыт расчётом
  (`SIDE_HEIGHT_MIN_GAIN_MM` = вертикальное разрешение боковой головки), и после
  фикса третья головка осталась платой без выигрыша;
- строка трёх головок снята ДО этого фикса и после него не перемерялась —
  трёхкамерная стойка не отгружается, и тратить на неё стенд смысла нет;
- по точности измерения стойки **неразличимы** (и переписью, и офлайн-зондом на
  одинаковых кадрах) — 28.07 это подтверждено поячеечным разбором;
- **защита Ручки не абсолютна, и это надо знать перед защитой:** при 11 мм и низком
  K вердикт спасает консервативная политика у размерной границы, но при 11 мм и
  K = 1.0 (товар торцом) она не помогает — она сторожит размеры, а не K. Проверено
  исполнением, записано в `docs/experiments.md` 30.07;
- откат на одну головку или на три — одна переменная окружения
  (`WORLD` + `BRIDGE_CONFIG`), повторного прогона не требует.

## 5. Посмотреть переснятые клипы галереи — приёмка глазами

**Все четыре клипа пересняты 30.07 на ДВУХкамерной стойке**, и конфигурация каждого
подтверждена логом, а не названием файла: `heads=2` и
`world=cell_diverter_2cam.sdf` в `skeleton.log`/`plan.log` каждого прогона
(`runs/stream_20260730_023*`). Вердикты верные: Бутылка→D, Короб 300→B,
Короб 400→C, Ручка→C, все PASS.

Съёмка на двух головках идёт заметно легче: 76 с на клип против многих минут
29.07, потому что камера-зритель теперь ТРЕТИЙ рендерящий сенсор, а не четвёртый.
Прежняя жалоба на голодание потока кадров в основном снята.

Клипы обрезаны до 14 с окном вокруг замеренного пика активности кадра, постер
выбран той же метрикой (поэтому он гарантированно внутри клипа). Партия `rig3_*`
удалена: она снята на стойке, которая больше не отгружается.

**Что изменено в подписи, и это стоит знать перед приёмкой.** Прежняя подпись
Ручки обещала «9 мм: габарит бьёт форму». Проверка показала, что 9-мм тело на
общем плане участка 6×10 м не видно — и не было видно во вчерашнем «крупном
плане» тоже: его постер оказался тем же кадром, что новый. Подпись теперь говорит
прямо, что клип показывает МАРШРУТ и механизм, а сами 9 мм доказывает depth-кадр
с наложенными габаритами. Если такой ответ не устраивает — нужен не другой ракурс
общего плана, а отдельная вставка с depth-кадром крупно.

**Что сделать:** посмотреть четыре клипа и решить, устраивает ли кадр и подпись.

## 6. Озвучка — ОТПАЛА 31.07, читать только если возвращаете повествовательный ролик

Видеодемонстрацией сдачи работает `census_reel.mp4` (6:03, все 33 ячейки, подписи
выжжены в кадр) — он самодостаточен и немой по замыслу: разбор решения идёт в
питче. Повествовательный `demo_reel.mp4`, под который написан закадр, **не
отгружается**, поэтому накладывать голос не на что и не нужно. Ниже — прежний
текст пункта; он остаётся рабочей инструкцией на случай, если решите вернуть
повествовательный ролик.

**Ролик смонтирован:** `docs/report/video/demo_reel.mp4` — 5:44, 1280×720, немой.
В нём титры и подписи выжжены в кадр, поэтому он читается и без звука: задача,
участок, правило, depth-кадр, контур, сквозной поток 5/5, по клипу на каждый
исход правила, ловушка Цилиндра, механизм с полным циклом, синхронизация,
метрики, нештатные ситуации, честные границы, воспроизводимость.

**Текст закадра написан под его тайм-коды** — `docs/report/video_narration.md`:
посегментно, с бюджетом секунд и пометками, где оставлена тишина под кадр.
Около 740 слов, ~5,5 минуты в спокойном темпе.

**Что сделать:** записать голос и свести:

```bash
ffmpeg -i docs/report/video/demo_reel.mp4 -i voice.wav \
       -c:v copy -c:a aac -shortest docs/report/video/demo_reel_voiced.mp4
```

Если голос не уложится в 5:44 — не ускоряйте видео: увеличьте длительность
карточек в `SEGMENTS` (`scripts/build_demo_reel.py`) и пересоберите одной
командой, клипы при этом не трогаются.

Про регламент, чтобы не путать две вещи: **видеодемонстрация** — обязательный
элемент сдачи, её полная версия уходит в облако; **семь минут** — это лимит
ЗАЩИТЫ, где формат на выбор (живая демонстрация, видео или их сочетание).
5:44 выбрано так, чтобы ролик годился и туда, и туда.

## 7. Репетиция защиты и финальный просмотр деки

Дека — `docs/report/slides/deck-c-ozon.html` (открыть в браузере; стрелки —
слайды, пробел — пуск клипа). В ней **пять играющих видео**, числа приведены к
отгружаемой ДВУХкамерной стойке и все девять слайдов проверены рендером на
переполнение стенда 1920×1080 (не на глаз — скриншотом каждого слайда).

**Что сделать:** прогнать доклад по таймеру на 7 минут, проверить, что каждое
число вы готовы защитить вопросом «откуда оно». Заготовки — `docs/report/qa_prep.md`,
обязательный вопрос теперь звучит как «18, 10, 8 или 9?» (ответ: 9 — на той стойке,
которая отгружается; цифра менялась трижды, потому что менялась КОНФИГУРАЦИЯ, а не
метрика, и в заготовке это проговорено).

Отдельно: в `docs/report/slides/` лежат ещё три варианта деки (a/b/c) в других
стилях. **Они не синхронизированы по содержанию** и несут по одному видео —
презентовать нужно `deck-c-ozon.html`, префлайт проверяет именно её.

## 8. Пробная сдача пакета организаторам

Репетиция механики сдачи: форматы, лимиты загрузки, поля формы. Нужен доступ
к платформе — сделать до финального дедлайна, а не в день сдачи.

## 9. Прочитать три сведённых вручную документа

При слиянии в `main` три файла сводились авторски: `PLAN-WEEK5.md`, `README.md`,
`docs/report/path_to_line.md`. В последнем выброшены 62 строки версии `main`
в пользу более полной версии ветки. Они идут жюри — прочитать глазами.

## 10. Судьба ветки `feat/line-readiness`

У ветки осталось 51 своих коммитов (`cameras.md`, `rig_decision.md`,
`calibration.py`, `belt_plane.py`, зонды выпадения глубины, гейт-отчёт), и она
на 44+ коммита позади `main`. Владельцу предстоит новое сведение прозы —
предупредить его заранее.

---

## Чего в этом списке НЕТ, потому что сделано

- клипы деки перекодированы в H.264 и играют из чистого клона; префлайт теперь
  проверяет сам кодек и требует не меньше трёх играющих клипов;
- постеры галереи пересобраны — четыре разных кадра вместо одного общего;
- обрезаны простои: Ручка 191 → 52 с, Короб 300 — 14,6 с пустой ленты в начале;
- заявка производительности пересчитана и разведена по конфигурациям (8 / 9 / 18)
  во всех документах, включая деку и отчёт;
- отчёт догнал прод: `camera_tradeoff.md` больше не утверждает «одна камера»;
- красная сюита починена: двухголовый мир отстал от трёхголового на перф-правке;
- логи трёх потоковых прогонов перенесены в репозиторий — документы больше не
  ссылаются на путь, существующий только на одной машине;
- пакет для облака собирается одной командой и описан манифестом;
- журналы `docs/decisions.md`, `docs/experiments.md`, выжимка по камерам.
