# Что должен сделать человек руками

Всё, что можно было автоматизировать, уже в репозитории. Ниже — только то, что
принципиально требует человека: доступы, публикация во внешние сервисы, решения
и приёмка глазами. Список составлен 28.07 и отсортирован по срочности.

Проверить состояние комплекта в любой момент:

```bash
python scripts/check_submission.py     # должно остаться РОВНО одно замечание — п.1
bash scripts/check_clean_deploy.sh     # чистый checkout -> образ -> headless e2e
```

---

## 1. Ссылка на видео в облаке организаторов — БЛОКИРУЮЩЕЕ

`README.md:53` содержит плейсхолдер `<ссылка на облако организаторов>`, и
`check_submission.py` намеренно возвращает `BLOCKED`, пока он там.

**Почему не сделано за тебя:** нужно залить крупные mp4 в облако организаторов
под своей учёткой и получить ссылку. Ни доступа, ни права публиковать от вашего
имени у меня нет, а вписать туда правдоподобную строку — значит сделать чекер
зелёным при отсутствующем видео. Это единственное замечание префлайта, и оно
должно оставаться до момента реальной заливки.

**Что сделать:** залить ролики из `docs/report/video/` (и новые, если запишу),
подставить ссылку вместо плейсхолдера, прогнать `python scripts/check_submission.py`
— должно стать `OK`.

## 2. Посмотреть, что CI зелёный — БЛОКИРУЮЩЕЕ до сдачи

28.07 CI возвращён в автоматический режим (`push` в `main` + `pull_request`,
ручной запуск сохранён). На машине, где я работаю, **нет `gh` и нет доступа к
GitHub Actions**, поэтому подтвердить зелёный прогон я не могу — только
локальные аналоги трёх работ CI (они зелёные, см. ниже).

**Что сделать:** открыть Actions в обоих репозиториях и убедиться, что последний
прогон `main` зелёный по всем трём работам: `lint-and-test`, `colcon-build`,
`e2e-smoke`. Если красный — прислать лог, чиню.

Локально проверено: `ruff check .` чист, полная сюита **656 passed, 3 skipped**
(в WSL с ROS), `bash scripts/check_clean_deploy.sh` — PASS дважды, в том числе с
нуля без кэша слоёв.

## 3. Решение по конфигурации прода — ТВОЁ, не моё

Числа на 28.07, все на одном стенде и одном ядре:

| стойка | маршрутизация, чисто | маршрутизация, шум | пропускная способность |
|---|---|---|---|
| 1 камера | 33/33 | 32/33 | _(замер идёт)_ |
| 2 камеры | 33/33 | 31/33 | 5 товаров/мин (по 3 прогонам) |
| 3 камеры | 33/33 | 32/33 | 6 товаров/мин (по 6 прогонам) |

Что важно знать перед решением:

- по маршрутизации стойки **равны**, разница в одну ячейку и та инфраструктурная;
- по точности измерения стойки **неразличимы** (проверено и переписью, и
  офлайн-зондом на идентичных кадрах);
- **превышение недостижимо без смены контракта слияния**: K и след принадлежат
  верхней головке, боковым доступна только высота, а вердикты каталога от высоты
  не зависят;
- мульти-seed 163/165 снят на ОДНОЙ головке; для двух он подтверждён отдельно,
  для трёх **не мерян**;
- три головки не укладываются в такт камеры (72 мс против 66.7) — предел железа.

## 4. Прочитать три сведённых вручную документа

При слиянии в `main` три файла пришлось сводить авторски, а не механически:
`PLAN-WEEK5.md`, `README.md`, `docs/report/path_to_line.md`. В последнем я
**выбросил 62 строки** версии `main` в пользу более полной версии ветки.

**Что сделать:** прочитать эти три файла глазами — они идут жюри.

## 5. Судьба ветки `feat/line-readiness`

Её гейт на 30.07 задумывался, чтобы принести камерный механизм в `main` — он уже
там. Но у ветки осталось **51 коммит** своего: `cameras.md`, `rig_decision.md`,
`calibration.py` (Н5), `belt_plane.py`, зонды выпадения глубины, гейт-отчёт. И
она теперь **на 44 коммита позади** `main`, то есть её владельцу предстоит новое
сведение прозы.

**Что сделать:** предупредить владельца ветки до вечера 30.07.

## 6. Репетиция защиты и финальный просмотр деки

Дека и её содержание — в `docs/report/slides/`. Автоматически я могу привести
числа и вставить видео, но не могу проверить, как это звучит вслух за 7 минут и
попадает ли в регламент.

**Что сделать:** прогнать доклад по таймеру, проверить, что каждое число на
слайде вы готовы защитить вопросом «откуда оно».

## 7. Мелочь: минуты Actions уже потрачены

28.07 я дважды запушил рабочую ветку, у которой в `ci.yml` ещё стояли триггеры
`push`/`pull_request` — это запустило CI и израсходовало минуты зеркала. Правило
проверять `ci.yml` ветки перед пушем приехало в `CLAUDE.md` позже, чем ветка
отпочковалась, но потратил их я. Сейчас и `main`, и ветка в согласованном режиме.

---

## Чего в этом списке НЕТ, потому что сделано

- прогон и сведение переписей 1/2/3 камер, чисто и под шумом;
- две оптимизации перцепции (бит-в-бит) и фикс холостой работы боковых головок;
- изоляция DDS-домена тестов — «флейки» контроллера были чужим симулятором;
- бюджеты харнесса для многоголовых стоек, закреплённые тестами;
- возврат CI в автоматический режим;
- журналы `docs/decisions.md`, `docs/experiments.md`, выжимка по камерам.
