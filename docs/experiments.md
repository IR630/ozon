# Журнал экспериментов

> Одна строка на значимый прогон: дата, коммит, параметры, метрика (CLAUDE.md, принцип 7).
>
> **Правило чтения записей 13.07:** результаты до `688346f`, использующие
> legacy origin-scored вердикт, сохраняются как история экспериментов, но не как
> текущая routing-метрика. В частности, `5abe909` и оба прототипа склиза негодны
> для физического вывода о Pouf: последующая body-scored проверка показала, что
> baseline-товар уже лежал в зоне. Актуальная принятая точка —
> `v0.5-stable-stream`: два полных stream-suite `33/33 + 33/33`; финальный
> census-проход был прерван и покрывает 33 уникальные ячейки как prefix `32/32`
> + возобновлённый Helmet-tail `3/3`. Старые красные серии остаются историей и
> не смешиваются с новым знаменателем.

| Дата | Коммит | Что запускали | Параметры | Результат |
|---|---|---|---|---|
| 2026-07-26 | `main@b079b0d` | **Финальный локальный deploy + повтор resource/latency-профиля Stage 26** | (1) `check_clean_deploy.sh`: чистый `git archive HEAD`, pinned Docker digest, CPU headless; (2) тихий WSL/Fortress из ASCII-worktree, seed 0 / orient 1, тот же худший mixed-план `bottle:D:0 plate:D:1.0 box_400x400x300:C:3.1 pouf:C:1.5 pen:C:5.0`; логи `runs/resource_stage26_current_b079b0d` | Clean deploy **PASS**: box300→B и occupied E-stop с feedback-gated парковкой и свежим B. Mixed **5/5 PASS**, roster 5/5/5, FAIL/JAM 0; camera→decision median **0.056 с**, p95 **0.080 с**; peak RSS **960.5 MiB**, CPU 3.896 avg / 6.449 peak cores, 11 процессов за 26.1 с. Против профиля до Stage 26 RSS 944→960.5 MiB (+1.7%); CPU 2.8→3.896, но один более короткий эпизод не отделяет код от нагрузки/RTF. `check_submission.py` блокирует только ожидаемая ссылка на облачное видео. Gate на точном окружении организаторов и чужой машине остаётся открытым |
| 2026-07-26 | `feat/line-readiness@24a5878` | **Офлайн-развилка продуктовой стойки 2 / 3A / 3B продовым классификатором** | 4 конфигурации × 4 бюджета калибровки × 5 правил fusion × 165 сохранённых поз; Gazebo-контур не запускался; отдельно fallback Мешка при потере верхней головы | Для будущей линии выбрана **3A: верх + два встречных бока**: при типичной калибровке union-prod 92 % в допуске против 82 % у двух голов; 3B не выиграла ни одну клетку; fallback Мешка 48.6→6.2 мм. Ограничение решающее: в худшем столбце две головы со structural fusion 86 % лучше 3A 82–83 %; structural fusion не в production, H1 timing 40–75 мс при gate ≤25 мс, live/noise regression не выполнен |
| 2026-07-26 | `work/a2-heads-noise@d03851f` против `4187aa2` | **Live single-item A/B новых однокамерных фильтров на трёх позах Бутылки** | Один WSL/Fortress-стенд, `cell_diverter_noisy.sdf`, σ=3 мм, seed 0, oi=0..2; перед прогоном 599/6 pytest, ruff, colcon и SDF зелёные | Кандидат улучшил корректные CV-решения **0/3 → 2/3** (`K`: 0.769→1.000 и 0.434→0.884), но oi2 остался D→B (`K=0.251`), поэтому single-item gate НЕ закрыт и полный census остановлен. Routing не сравнивается: контроллер на этой машине отвергал первые измерения обоих вариантов как старые (`stamp age` baseline 1.63–1.65 с, кандидат 1.73–1.74 с); у кандидата это дало два `no_fire`. Код в `main` не переносится |
| 2026-07-26 | `work/a2-heads-noise@d03851f` | **Точечные кандидаты робастности однокамерного оценщика после шумового gate** | Офлайн-корпус 33 сохранённых кадров, σ=3 мм, 8 draws/ячейку; 95-й перцентиль гребня K + median 3×3 только для статистики высоты | Под шумом **32.4/33**, чисто 33/33; общий denoise, дававший 33/33, отвергнут из-за регрессии чистой точности Ручки/Пуфика. Это НЕ live-census: перенос в `main` заблокирован до полного regression-gate |
| 2026-07-25 | `work/a2-heads-noise@4187aa2` | **Live-сравнение 1/2/3 камер под одинаковым модельным шумом** | Fortress+Humble, σ=3 мм, seed 0, 11×3 ячейки; двух- и трёхкамерные чистые контроли | **1 камера 31/33; 2 камеры 18/33; 3 камеры 18/33.** Чистые 2/3 камеры 33/33. Текущий fusion бокового облака под шумом схлопывает C/D в B; один seed и упрощённая модель шума не доказывают реальную камеру |
| 2026-07-24 | `feat/three-cameras@d45f42b` | **Multi-seed двухкамерного полного контура** | `cell_diverter_2cam.sdf`, 11 товаров × 3 ориентации × seeds 0–4, логи `runs/census2cam_seed0..4` | **163/165 — ровно как одна камера**, два K-промаха Мешка, execution 0. Цена цикла 1.48× остаётся без прироста routing; P1 закрыт отрицательным результатом |
| 2026-07-24 | `feat/two-cameras@75b229c`, `feat/three-cameras@d45f42b` | **Итоговый triage камерных веток против принятой базы** | Полный контур seed 0 для 1/2/3 головок; однокамерная multi-seed перепись 11×3×5; замеры цикла и точности габаритов из журналов веток | Маршрутизация seed 0 одинакова: 33/33 для 1/2/3; проверенная multi-seed база — 163/165 с одной головкой (перепись снята на `feat/three-cameras@4581d8d`, поячеечно 33/32/33/33/32, `runs/a2sweep_20260722_*_seed{0..4}`; детали — `soak.md`). Цикл: 1.0×/1.48×/1.9×. Дополнительные камеры улучшают допуск измерений, но не итоговую маршрутизацию: оба оставшихся промаха определяются K верхней головы. Gate для продвижения кандидата в `main` не закрыт; production остаётся однокамерным |
| 2026-07-24 | `fix/estop-hold-window@ca4b2da` → main | **Triage premise occupied E-stop smoke** | Три физических повтора ветки; два `ign topic -e` (до/после) и первая ROS-публикация E-stop перед самим stop | Production `HOLD_S=2.5 с` истекал примерно за секунду до stop: лопасть возвращалась штатно, и smoke мог проверять parked state вместо занятой стенки. Test-default повышен до 15.0 с только в `smoke_estop_stream.sh`; stop по-прежнему посылается сразу после подтверждения engage. Удалённый e2e нужен как подтверждение сценария |
| 2026-07-21 | `main@0e98ea5`, mirror CI #310 | **Повтор CI после фикса colcon-preflight; triage нового occupied-smoke FAIL** | GitHub Actions; pinned ROS/Gazebo image, полный контур + occupied E-stop | `lint-and-test` **PASS**, `colcon-build` **PASS** — предыдущая ошибка закрыта. `e2e-smoke` дошёл до post-reset физической проверки: до reset C `0.7103→0.7086` рад, controller gate разрешил рестарт, но прямой текстовый probe сообщил невозможные **4.829 рад**. Это RED парсера: protobuf-default zero не печатает `position`, а `grep -A6` уходил за границу `axis1`. Добавлен структурный brace-aware parser и регрессия «parked zero + чужое position 4.829»; удалённый физический PASS требует следующего CI |
| 2026-07-21 | `main@587e7e1`, mirror CI #268 | **Первый CI итогового отчёта: triage трёх независимых jobs** | GitHub Actions; `lint-and-test`, `colcon-build` в `ros:humble-ros-base`, физический `e2e-smoke` | `lint-and-test` **PASS** (45 с), `e2e-smoke` **PASS** (4 мин); `colcon-build` собрал ROS messages, затем дал **502 passed / 3 skipped / 1 failed**. Единственный FAIL — не код узлов и не геометрия: `test_current_package_has_only_the_known_human_placeholder` вызвал `git ls-files` в контейнере без гарантированного Git-индекса (exit 128). Исправление ограничено тестовой инъекцией tracked-инвентаря; production preflight остаётся строгим |
| 2026-07-21 | `main@44c3d76` | **Аудит preflight против обязательного состава сдачи (§4 постановки)** | Сопоставлены исходный код CV, исполнительная часть, ROS-связка, цифровая модель, отчёт, презентация, CAD и видео; добавлены негативные тесты ссылки | Обнаружен пробел: прежний gate мог пройти без ядра полного контура. Добавлены обязательные core-артефакты и проверка HTTPS в абзаце видео; текущий комплект по-прежнему BLOCKED ровно ожидаемым H6-плейсхолдером |
| 2026-07-21 | `main@3e84d60` | **Submission preflight red→green по контракту репозитория** (`scripts/check_submission.py`) | Windows 11, Python 3.13; tracked tree, обязательные отчёт/слайды/CAD/hero/deploy-файлы, placeholder и large-binary policy; целевые тесты + полный lint/pytest | Первый RED выявил два разрешённых крупных исходника организаторов в `docs/Step`/`docs/Stl`; политика уточнена явным исключением, а не повышением порога. Финал: preflight **BLOCKED ровно одним ожидаемым H6-плейсхолдером** ссылки на облако; обязательные артефакты на месте, неожиданных бинарников >5 MiB нет. Целевые 2/2, полный gate **465 passed / 6 skipped**, ruff чист |
| 2026-07-21 | `main@41dd5f9` | **Windows UTF-8 subprocess regression: фоновые ошибки чтения Bash-вывода превращены из предупреждений в проверяемый контракт** | Windows 11, Python 3.13, Git Bash; `pytest -W error::pytest.PytestUnhandledThreadExceptionWarning` на четырёх воспроизводящих тестах, затем полный `ruff check .` + pytest с тем же строгим warning-gate | **RED:** 4/4 падали `UnicodeDecodeError` в `_readerthread` (UTF-8 Bash читался CP1251). **GREEN:** целевые 4/4; полный gate **463 passed / 6 skipped**, ruff чист, предупреждений reader-thread нет. Производственный код и результаты камерных веток не затронуты |
| 2026-07-17 | `3201d38` | **Первый обязательный CI physical gate feedback-gated occupied E-stop:** два 3-кг C-короба, выдвинутый шибер, E-stop, settled freeze, ручная очистка-модель, парковка C/D по свежей обратной связи, новый B | GitHub Actions run `29591607813`; pinned Docker, ROS 2 Humble, Gazebo Fortress; текстовые fixture-модели без STL организаторов | **PASS, все три job зелёные.** Host `385 passed / 6 skipped`, ROS **421 passed / 3 skipped**. За двухсекундный settled-интервал оба товара сместились <0.00002 м; C-лопасть `0.710054→0.708590` рад и не ушла к park, `FIRED 2→2`. После reset измеренный C-угол `0.000` рад, свежий B достиг терминальной полосы без нового fire/re-latch. Это физический контракт цифровой fixture-модели с оболочкой 400×400×300 мм и массой 3 кг, не реального материала; resource/latency и полный merge-gate остаются открыты, тег `v0.5-stable-stream` не меняется |
| 2026-07-17 | `46a74d1 → ccdbccc` | **Удалённая приёмка ROS/CI safety-кандидата и устранение ошибок самого тестового стенда** | GitHub Actions, ROS 2 Humble; runs `29588432123`, `29589126737`, `29590418273`; локально `ruff check .`, `pytest -q` | Первый run: lint и обычный B `e2e-smoke` PASS, ROS **413 passed / 3 skipped / 6 failed** — пять positional fixtures не задавали штатный `retract_cmd=0.0`, а `node.timers` в Humble является generator. После фикса `3dbdd7e` второй run: lint/E2E PASS, ROS **418/3/1**; единственный тест проверял положительную команду до следующего ramp/DDS-такта. После ожидания со свежим C/D feedback коммит `ccdbccc` дал полностью зелёный run `29590418273`: lint/test PASS, `colcon-build` **419 passed / 3 skipped**, `e2e-smoke` PASS. Host gate **383 passed / 6 skipped**, ruff чист. Обычный B-smoke не является occupied C/D E-stop recovery и не закрывает resource/latency gate; кандидат остаётся в ветке, тег не меняется |
| 2026-07-17 | `int/estop-engaged-recovery@46a74d1` | **Test-first восстановление занятого позиционного шибера после E-stop:** C/D joint state, restart-interlock, feedback-gated reset, второй E-stop; затем adversarial-аудит невозможного, старого/будущего feedback, изменения угла под нагрузкой, немедленного повторного stop и 3D-freeze smoke | Windows host Python 3.13; `ruff check .`, `pytest -q`, `bash -n scripts/smoke_estop_stream.sh`; официальный source audit Fortress 6.18.0 / ros_gz Humble | Первый целевой тест был RED: отсутствовали границы допустимого feedback. После фикса значения вне `[-0.10; 1.30]` защёлкивают E-stop и не становятся actuator-target; 1.24 рад под измеренной нагрузкой остаётся допустимым. Acquisition stamp должен быть строго новее команды и не новее локального `/clock`, поэтому старый и future-relative пакеты не являются доказательством. Финальное ревью нашло второй RED-кейс: hold 0.72 → нагрузка 1.10 → reset → immediate stop возвращал старые 0.72. Теперь reset требует post-hold C/D feedback, атомарно обновляет pre-park snapshot, а startup требует два post-command sample за 0.2 с. Smoke сравнивает 3D-позу и запрещает уход лопасти к park относительно исходного угла; `FIRED` считается от подтверждённой строки E-stop. Host gate **383 passed / 6 skipped**, ruff, shell syntax и `git diff --check` чисты. ROS-node тесты пропущены локально без `rclpy`; Gazebo/Fortress физически не запускался. Source audit подтвердил sim-time stamp в 6.18.0 и его перенос bridge в ROS Header; `<update_rate>` появился только после релиза и в 6.18.0 не работает. На момент host-прогона оставались ROS CI, occupied-smoke и resource/latency gate; удалённый CI закрыт следующей записью, физический и ресурсный gate остаются открыты. `v0.5-stable-stream` не меняется |
| 2026-07-19 | (ветка cv/section-k-geometric) | **Поштучный замер транспорта мяча лентой: 2+2 прогона `run_skeleton.sh ball B` с трассой динамики** (`CAPTURE_DYNAMICS=1`, трассы `runs/ball_roll/trace_*.log`, x(t) через `capture_dynamics.parse_trace`) | WSL ozon, Fortress, `cell.sdf`; режим A — `SPAWN_Z=0.405` (положен с зазором 5 мм на идущую ленту), режим B — `SPAWN_Z=0.5` (сброс ≈95 мм); по 2 повтора на режим | **Транспорт сферы бимодален; движут её отскоки, а не трение — оба режима 2/2.** Положенный мяч неподвижен весь эпизод (x=−1,500000 все 54–56 с при ленте 1 м/с, |ω|=0 — непрерывный контакт не передаёт импульс). Сброшенный скачет вдоль ленты почти со скоростью ленты (до 0,97 м/с в фазе отскоков, подскоки до z=0,59, конец ленты x=4,84 за 12–15 с; peak_accel 45–75 м/с²). Исторический прогон bfb2765 (281 с, x=4,54) — промежуточный режим: гипотеза — отскоки затухли в середине ленты. Для отчёта: время транспорта круглого товара неповторяемо (12 с … 281 с … никогда) и зависит от фазы отскоков при подаче |
| 2026-07-19 | (ветка cv/section-k-geometric) | **Кампания «K-полоса»: сырые до-гейтовые признаки на реальных кадрах Gazebo** — дампы боевого узла через `PERCEPTION_DUMP_DIR` (`runs/kband/`), офлайн-пересчёт PNG функциями `perception.py` (узел публикует пост-гейтовое K) | WSL ozon, Fortress; мешок seeds 0–4 × orient 0–2 (11 устоявшихся поз, 122 кадра), мяч 17 кадров, банка стоя 11; на кадр 5 чисел: k_sil, dz/max(dx,dy), k_sec, elong, rms/R оболочки | **NO-GO по заранее зафиксированному критерию.** k_sil: мешок 0,700/0,821/0,878 (min/med/max), мяч 0,933/0,976/0,986, банка 0,799/0,886/0,976 — зазор D_min−B_max = −0,078 при требуемых +0,10; 91/122 кадров мешка выше D_min. Запасной rms/R: мешок min 0,0365 против max 0,0752 у зондов — 0,49× при требуемых 2×. Причина (по оверлеям): вне надира виден бок цилиндра, K банки гладко падает 0,976→0,799 к краям обзора — кадры честные, фильтровать нечего. Итог — вариант C: порог не вводим, `KNOWN_GAPS` без изменений (`docs/probe-models.md`, раздел «Кампания „K-полоса“»). Пересчёт тех же кадров после фикса оценщика `fa4d30e` (K из общего центра): B_max=0,873, D_min=0,782, зазор −0,091 — NO-GO подтверждён на обеих версиях ядра |
| 2026-07-19 | (ветка cv/section-k-geometric) | Прототип геометрического K против старой подгонки окружности, офлайн-рендер | 11 официальных × 3 позы + 7 зондов = 48 ячеек | **32/48 → 39/48 верных. Изменилось 7 ячеек, все 7 в сторону истины, 0 регрессий.** Починены официальные Бутылка oi0 (B→D), Тарелка oi1 (B→D), Шлем oi2 (C→B) и зонды ball, hex_bar ×2, squat_can on_side. Гейт зондов 9→13 pass, пробелов 6→2. Остались: squat_can upright (круг в горизонтальном сечении, гасит FLATNESS_MAX) и hex_bar yaw90 (K=0,796 против 0,8 — разрешение биннинга) |
| 2026-07-19 | (этот) | **Мульти-seed census: воспроизводимость routing 11×3 по seeds 0–4** — `run_matrix.sh` на 5 seed + агрегатор `census_seed_sweep.py` | WSL ozon, Fortress, `cell_diverter.sdf`, seeds 0/1/2/3/4, orient 0..2, `CELL_TIMEOUT=180`; каждый seed в свой `runs/sweep_seed{0..4}` | **165/165 routed, classification 0, execution 0, seed-чувствительности нет; Pouf 15/15** (в т.ч. исторически капризные oi0/oi1). Первые два фоновых запуска убиты средой (WSL-фон не живёт дольше ~30–55 мин), но per-cell `.status` durable — 2 полных seed’а уцелели, seeds 2–4 досняты отдельным запуском. Характеризует seed-ось (5 разных seed по одному прогону), НЕ фиксированный-seed replay: Stage-24 31/33 (Pouf) — отдельная ось недетерминизма одного seed, требует повтора seed ×N |
| 2026-07-19 | bfb2765 | `run_skeleton.sh ball B` в Gazebo против эталона `box_300x200x200` | `ITEM_MODEL_ROOT=sim/models/probe_items`, поза по умолчанию | **Такт: мяч 281,2 с против 16,4 с у короба (×17), FAIL по таймауту эпизода.** Финальная поза мяча — корректная зона B (`zone_verdict` → YES, |y|=0,14 при допуске 0,3): с ленты не укатился, лента его не тащит, а он катится. Классификация совпала с офлайн-рендером слово в слово (Gazebo `195x180x161 K=0.00 → B conf=0.84` против рендера `189x181x181 K=0.000 → B`) — рендер предсказывает живую ячейку |
| 2026-07-19 | (этот) | `scripts/measure_probe_shapes.py` — 7 процедурных зондов через боевой контур рендер→перцепция→классификация | 15 поз покоя, детерминированно (без seed: позы именованные, не случайные) | **9 PASS, 6 промахов (3 механизма), 0 необъяснённых.** Промахи: `squat_can` и `ball` → B вместо D (K=0 — обнулены оба маршрута), `hex_bar` → B вместо D (K формулы 0,866). Подтверждено верно: `u_bracket` не расщепляется (3/3), `slab_over_limit` держит порог 320 при запасе 12 мм (завышение измерения ~5 мм), `thin_sheet` 8 мм виден над маской 5 мм, `ring` с дыркой не ломает сегментацию |
| 2026-07-18 | рабочее дерево после `main@898e770` | **P1: видимая камера в кадре — полный комплект потоковых клипов** — visual-only проп `sim/models/camera_gantry` (портальная рама + корпус сенсора в реальной позе 1.5/0/1.9) спавнится только рекордерами, как спектатор; переснято 5 одиночных клипов + hero-поток | WSL ozon, llvmpipe, ракурс b3; `RUN_STREAM_POLL_ITERS=600`; production-мир не менялся (проп без collision, стойки y=±1.2 вне FOV depth-камеры, корпус за near-clip); логи `runs/record_p1_1807/*_gantry*.log` | Все клипы PASS: helmet→B 36.1 с, bottle→D 28.3 с, plate→D 41.6 с, pouf→C 32.0 с, pen→C 26.7 с, hero-поток 3/3 (box_400→C 27.0, pen→C 34.2, bottle→D 51.3) — `stream_*.mp4` в `docs/report/video/`. Перцепция фантомов от рамы не видит (roster чист во всех клипах). Один hero-дубль дал flake: bottle→D замерла на x=1.39 без E-stop/JAM (измерения шли, отсчёт до выстрела завис на ~1.05 с — позиция физически не продвигалась); немедленный повтор — 3/3. Единичный шум контактной физики съёмочного эпизода, в знаменатели suite не входит |
| 2026-07-18 | рабочее дерево после `main@f7c39f2` | **P1: пересъёмка helmet→B + недостающие D-клипы** — 3 дубля `record_skeleton_video.sh helmet B` (poll 300/600), затем переход на `record_stream_video.sh` для одиночных товаров: `helmet:B:0`, `bottle:D:0`, `plate:D:0` | WSL ozon, llvmpipe, ракурс b3; env-ручка `RUN_SKELETON_POLL_ITERS` починена (скрипт жёстко перезаписывал её на 300 — рекомендация плана «переснять с poll=150» физически не действовала); логи дублей — `runs/record_p1_1807/` | **Skeleton-рекордер для helmet→B непригоден, и это НЕ бюджет опроса:** дубли с poll=300 и poll=600 дали FAIL с побитово идентичной финальной позой (origin x=4.062290 y=-0.067978 z=0.532264; тело x=3.954 < 4.2) — товар детерминированно останавливается, вдвое больший бюджет ничего не меняет; вчерашний диагноз «класс наблюдение» отозван. Один дубль — повтор вчерашнего транзиента (ранний выход без вердикта; полный лог эпизода погиб вместе с /tmp при автоостановке WSL-дистрибутива — вот почему «лог раннера не сохранился» и 17.07). Точный механизм остановки не вскрывался (ядро заморожено); отличие записываемого эпизода — спавн модели спектатора в мир + старт ленты под лежащим товаром (известный дефект skeleton-стенда для округлых). **Потоковый рекордер (подача на уже идущую ленту, production-режим) — PASS с первого дубля во всех трёх клипах:** helmet→B 31.6 с, bottle→D 20.7 с, plate→D 34.6 с — `stream_{helmet_B,bottle_D,plate_D}.mp4` в `docs/report/video/` (не коммитятся, GIT.md). Дыра плана «bottle_D отсутствует на диске» закрыта тем же заходом |
| 2026-07-17 | `main@2624006` | **P1: сырой футаж пограничников + hero-поток** — `record_skeleton_video.sh` для helmet→B, pouf→C, pen→C и `record_stream_video.sh` (default 3-товарный поток) | WSL ozon, llvmpipe, ракурс b3; зомби-cleanup между дублями по решению 17.07; mp4 в `docs/report/video/` (не коммитятся, GIT.md) | **pouf→C PASS** (32.5 с), **pen→C PASS**, **hero-поток 3/3 PASS** (box_400→C 14.4 с, pen→C 17.2 с, bottle→D 24.7 с) — плюс ранее plate_D и bottle_D. **helmet→B — годного дубля НЕТ:** дубль 1 FAIL по бюджету опроса (x=4.06 из 4.2 при poll=60: спектатор-камера дополнительно роняет RTF; это класс «наблюдение», не физика — в suite без записи helmet B 6/6), дубль с `RUN_SKELETON_POLL_ITERS=150` показал товар, не сошедший со спавна (транзиент после длинного батча, лог раннера не сохранился), две повторные попытки остановлены извне. Текущий `skeleton_helmet_B.mp4` (17M) — брак, к пересъёмке при свободном стенде |
| 2026-07-17 | `main@9a6da9b` | **Stage 25: однокомандная проверка развёртывания на чистом checkout** — `scripts/check_clean_deploy.sh`: `git archive HEAD` → сборка запиненного образа (digest + pip-lock) → headless CPU `ci_e2e.sh` внутри | WSL `ozon`, docker-ce 29.6.1; база `osrf/ros:humble-desktop@sha256:3d87cf…`, lock `docker/pip-constraints.txt` | Первый прогон — честный RED: `.dockerignore` (`**` + `!requirements.txt`) не пропускал `docker/pip-constraints.txt` в контекст сборки; исправлено через красный тест на whitelist каждого COPY-источника. Повтор: образ собран с нуля по digest, colcon ros_msgs 9.1 с, эпизод box_300→B **PASS** (body и legacy вердикты), итог **PASS**. Пины сняты с фактического образа: numpy 1.21.5 / scipy 1.8.0 / pytest 6.2.5 — apt-сборки (bare-имена requirements удовлетворялись системными пакетами, pip их не трогал), opencv 4.11.0.86 / trimesh 4.12.2 / ruff 0.15.21 — pip. Ограничение: lock действует только на деплой-образ; dev-среды (WSL numpy 1.26.4, CI py3.11) сознательно не пинятся — расхождение версий сред записано, а не спрятано. |
| 2026-07-17 | рабочее дерево после `main@424a0a2` | **Достройка Stage 24: ресурсный профиль потока и физическое восстановление после E-stop** — первый боевой прогон нового watcher на худшем mixed oi1; новый `smoke_estop_recover.sh` ×2 | Тихий WSL/Fortress, seed=0/orient=1, спеки `bottle:D:0 plate:D:1.0 box_400x400x300:C:3.1 pouf:C:1.5 pen:C:5.0`; каталоги `runs/resource_mixed_oi1_424a0a2`, `runs/estop_recover_r{2,3}` | Поток: **5/5 PASS, roster 5/5/5**; ресурсы за 48 с: peak RSS 944 MiB, CPU в среднем 2.8 / пик 4.4 ядра, пик 12 процессов, RSS к концу — плато 853 MiB без роста; item_id растут монотонно до 5, фантомов нет. Recovery: **2/2 PASS** — E-stop замораживает товар (dx=0), сброс логирует повторный soft-start, товар доезжает через divert-секцию в B-хвост, повторной защёлки E-stop нет. Первые версии смока падали честно и были исправлены: E-stop ложился в загрузочную рампу (ломал счётчик soft-start), а порог x>3.0 маскировал перелёт зоны — в смоке нет вердикт-полла, останавливающего ленту, поэтому критерий ослаблен до «пересёк x=3.3», зона-вердикт остаётся за эпизодными раннерами. Host gate: **388 passed / 3 skipped**, ruff чист (флейка E-stop-теста на тихом стенде нет). |
| 2026-07-17 | рабочее дерево после `main@cafbbd4` (хвост census — после pull `main@2ff0132`) | **Закрытие красного Stage 24: диагностика и фикс Pouf→C в потоке** — минимальные пары Box400→Pouf, трение пола зон, парные/транспортные зазоры, финальный gate suite ×2 + census | Тихий WSL/Fortress, seed=0, orient 0..2; пол зон C/D `mu=0.05`; зазоры Box400→Pouf 1.5 м, Pouf→Pen 5.0 м; `RUN_STREAM_POLL_ITERS=200`; каталоги `runs/pouf_*_cafbbd4`, `runs/pouf_fix_final_*_cafbbd4`, `runs/b_oi1_poll200_cafbbd4` | Корень НЕ CV: на штатном трении Box400 останавливался у устья склиза C и Pouf ложился на него вне зоны. Пара на 1.0 м — 1/2 PASS (блок устья) и слияние roster в oi2; `mu=0.05` + 1.5 м — **2/2 во всех трёх ориентациях**. Pouf→Pen: фокусная пара 2/2 уже на 4.0 м, но в пятитоварном потоке oi1 roster 4/5 (Pouf дополнительно тормозится предшественником); на 5.0 м — **5/5 дважды**. B-хвост: Helmet oi1 ещё ехал на x=3.906 при 150 опросах, poll=200 — 6/6 (бюджет наблюдения, физика не менялась). **Финальный gate: два полных suite 33/33 + 33/33** (roster 5/5/5 mixed и 6/6/6 B во всех эпизодах, 0 FAIL/TIMEOUT/JAM), **census 32/32 + добитый после обрыва сессии helmet-хвост 3/3** (`runs/pouf_fix_final_census_seed0_helmet_tail`), triage classification=0/execution=0. Host gate: ruff чист, pytest 385: 384 passed + E-stop тест, флейкнувший под параллельным Gazebo и зелёный отдельно. **Gate GREEN.** |
| 2026-07-16 | рабочее дерево после `main@635ca0a` | **Процедурный private-set proxy по официальному FAQ:** новые аналитические B/C/D depth-формы без released STL/slug проходят production perception+classification | 640×480 calibrated depth; yaw 0/27/61; призма B, near-round ellipse K≈0.77, oversized prism C, flat disc D, 9 mm tag C, oversized round C; `measure_private_shapes.py`, целевые pytest, полный pytest/ruff | RED **11/12**: задуманный D имел честно измеренные 345×345 мм и правильно стал C из-за лимита 320 мм. После исправления только генератора на 292×292 — **12/12**; полный gate **359 passed / 6 skipped**, ruff чист. Это generalization-контракт правил, не статистика приватного распределения и не основание менять CV-пороги. |
| 2026-07-16 | рабочее дерево после `main@9ad91b0` | **Красно-зелёный контракт ресурсного профиля Stage 24:** обход нескольких `/proc`-деревьев, process name с пробелом, отсечение постороннего PID, peak RSS/processes и CPU cores; статический контракт жизненного цикла watcher в stream runner | Windows, Python 3.12; synthetic procfs в `tmp_path`; `pytest -q tests/test_sample_process_resources.py tests/test_run_stream.py`, `bash -n scripts/run_stream.sh`, полный `pytest -q`, `ruff check .` | Целевые **20/20**, shell syntax clean, полный gate **357 passed / 6 skipped**, ruff чист. `run_stream.sh` теперь сохраняет `resources.csv/json` и fail-closed при отсутствии summary. Реальный RSS/CPU Gazebo не заявлен: после перезагрузки WSL-дистрибутив отсутствует, Docker CLI не установлен; пункт Stage 24 остаётся открытым до Linux/Fortress-прогона. |
| 2026-07-16 | `main@f4d3540` | **Stage 24 stability:** точный повтор полного seed 0 suite orientations 0..2 и новый seed 1/orientation 0; свежий Gazebo на каждый эпизод | Baseline `SAME_ZONE_GAP_M=1.0`, `HOLD/LEAD=2.5/0.5`; `runs/week4_stability_seed0_repeat_f4d3540`, `runs/week4_stability_seed1_oi0_f4d3540` | Seed0 repeat: **4/6 all-pass, 31/33**; B 18/18, mixed oi2 5/5. Mixed oi0/oi1 потеряли только Pouf→C: полный roster, верная C и `FIRED`, но body-verdict FAIL в `(3.219,0.808,0.494)` и `(3.035,0.730,0.207)`. Seed1/oi0: **2/2, 11/11**, Pouf PASS. Итого новых прогонов **6/8 эпизодов, 42/44 товаров**, roster 44/44/44, TIMEOUT/JAM/E-stop 0; с прежним all-pass suite контекстный итог 75/77, но ревизии не склеиваются в «процент качества». Нефатально: один `wait_for: No record of process`, terminal ledger при этом полный. Gate RED, новый тег не ставится; нужна различающая физическая серия Pouf, а не CV-фикс. |
| 2026-07-16 | рабочее дерево после `main@0889cce` | **Timing A/B этапа 22:** A=`HOLD/LEAD 2.5/0.5`, B=`2.0/0.5`, C=`2.5/0.4`; по два Pouf→C с dynamics и по два перехода C→D→C→D→B для B/C; baseline-переход A — из предыдущей записи | Тихий WSL/Fortress, seed=0/orient=0; B transition gap 2.6 м, A/C 3.1 м; `runs/week4_hold_lead_ab`, `runs/week4_hold20_transition_gap26_{1,2}`, `runs/week4_lead04_transition_gap31_{1,2}` | Pouf routing A/B/C: **2/2, 2/2, 2/2**. Медиана cycle: **26.8/27.3/26.2 с**; peak impulse: **6.93/5.43/5.46 Н·с**. B и C: каждый **2/2 эпизода, 10/10 товаров**, roster 5/5/5, cross-fire 0, 7–8 шт/мин; baseline A: 10/10, 7 шт/мин. Peak accel не ранжируется: вращательная граница/приземление и два повтора дают перекрывающийся шум. Кандидаты не дали выигрыша cycle/throughput и не прошли полный gate, поэтому production-дефолты не меняются. Подтверждённая заявка ограничена 18 шт/мин в коротком штатном 3-товарном потоке; all-model suite — 3–12 шт/мин. |
| 2026-07-16 | рабочее дерево после `main@67bf9bb` | **Gap A/B и переходы этапа 22:** suite orientation 0 с same-zone 0.8; повтор красного mixed; C→D→C→D→B ×2 | Тихий WSL/Fortress, seed=0/orient=0; `SAME_ZONE_GAP_M=0.8`; transition gap 3.1; каталоги `runs/week4_gap08_*`, `runs/week4_transition_cdcdb_seed0_oi0*` | Gap 0.8: B **6/6, 15 шт/мин**, mixed **4/5 затем 5/5**; красный Pouf — execution (`x=2.97,y=0.78,z=0.54`), roster 5/5/5. Универсальный 0.8 отвергнут как нестабильный. Переходы C→D→C→D→B: **2/2 эпизода, 10/10 PASS**, roster 5/5/5, темп 7 шт/мин, cross-fire 0. Cross-zone 3.1 подтверждён; `HOLD_S/FIRE_LEAD_S` не менялись. |
| 2026-07-16 | `main@c91c541` | **Физическая приёмка Helmet ID fix и полного gate 4:** Pouf→Helmet ×2, occupied E-stop, отдельный census 11×3; stream-suite/SDF/host gate из соседней записи | Тихий WSL `ozon`, Fortress 6.18, seed=0/orient=0; handoff `pouf:C:0 helmet:B:3.1`; каталоги `runs/week4_pouf_helmet_idfix_replay{1,2}_c91c541`, `runs/week4_idfix_estop_c91c541`, `runs/week4_idfix_census_c91c541` | **GO.** Два handoff: 4/4 физических PASS, оба roster 2/2/2, IDs `[1,2]`; fragmentation 2→3 не повторилась. E-stop: оба `dx=0`, C-blade `0.7092→0.7086` рад, `FIRED 2→2`. Census: **33/33**, 33 status `rc=0`, triage classification=0/execution=0. Gap 2.5 м отдельно дал ожидаемый planner ABORT до Gazebo (`C→B needs 3.1 m`) и не включён в физический знаменатель. Полный gate дополняют stream-suite 33/33, SDF 13/13, frozen 8/8+4/4, pytest 352/6 и ruff. |
| 2026-07-16 | `main@3f3a7ed` | **Полная потоковая матрица этапа 21:** два смешанных эпизода на каждую orientation 0..2, все 11 моделей в каждой | Тихий WSL `ozon`, ROS 2 Humble, Fortress 6.18, seed=0; `run_stream_suite.sh 0 N N`; каталоги `runs/week4_suite_gate_3f3a7ed_seed0_oi{0,1,2}` | **6/6 эпизодов, 33/33 terminal PASS.** Все шесть `.status rc=0`; roster в mixed 5/5/5 и B 6/6/6 для каждой ориентации; 0 FAIL/TIMEOUT/JAM, phantom ID нет. Наблюдаемый темп эпизодов 3–12 шт/мин не трактуется как потолок без A/B этапа 22. Нефатальные сигналы среды: SciPy предупреждает о NumPy 1.26 против системного диапазона `<1.25`; в двух mixed эпизодах shell сообщил `wait_for: No record of process`, но feeder выдал точные sim-ticks, terminal ledger полный и rc=0. |
| 2026-07-16 | рабочее дерево после `main@32073e6` | **Frozen hard-scene suite через production baseline:** synthetic угловое касание, полная грань, частичное перекрытие, вход/выход; реальные Gazebo Helmet/Plate/Pen и partial Box/Helmet/Pen | Windows, Python 3.12; `python scripts/measure_hard_scenes.py`, целевые pytest, полный pytest/ruff | `split=0, merge=1, phantom_id=0, category=9/9`; целевые **2/2**, полный gate **352 passed / 6 skipped**, ruff чист. Единственный merge — намеренно сохранённый контакт полной гранью без top-view признака разделения; новый CV-порог не вводился. |
| 2026-07-16 | рабочее дерево после `main@8939ce6` | **Красно-зелёный контракт метрик сложных сцен:** чистая последовательность и различающая последовательность со split, merge, фрагментацией ID, detection без truth и сменой категории | Windows, Python 3.12 из `requirements.txt`; `pytest -q tests/test_hard_scene_metrics.py`, полный `pytest -q`, `ruff check .` | Целевые **2/2**, полный gate **350 passed / 6 skipped**, ruff чист. Метрики дают split=1, merge=1, phantom=2, category=2/3 на различающей фикстуре и нули + 2/2 на чистой. Это проверка evaluator, не качество perception: synthetic/real кадры ещё не подключены. |
| 2026-07-16 | рабочее дерево после `main@fd63246` | **Host replay смены ID у кувыркающегося Helmet:** test-first последовательность двух видимых позиций, 3 пустых кадров и возврат; отдельный Pouf+Helmet guard против кражи соседнего ID | Windows, чистое временное Python 3.12-окружение из `requirements.txt`; камера контура 15 Гц; `pytest -q tests/test_item_tracking.py`, полный `pytest -q`, `ruff check .`, `measure_validation.py` | **RED → GREEN на точном seam:** до изменения оба теста дали новые ID (`[2]` и `[1,3]`), после минимальной калибровки tracker — **6/6**. Полный host gate **348 passed / 6 skipped**, ruff чист; frozen validation visible contract **8/8**, expected reject **4/4**. Физический вывод НЕ заявляется: на хосте нет WSL/Docker/`ign`, поэтому Pouf→Helmet, SDF, census, stream-suite и occupied E-stop не повторены; кандидат остаётся вне закрытого gate до тихого Gazebo-стенда. |
| 2026-07-15 | рабочее дерево после `main@a07282d` | **Аудит честности и производительности stream harness после первой suite:** одинаковый mixed D/D/C/C/C поток на wall-clock и Gazebo `/clock`; затем batch dynamic-pose и минимальные изоляции Box→Pouf, Pouf, Pouf→Helmet | WSL/Fortress, seed=0/orient=0; feed-план `0/1/4.1/5.1/7.6`; логи `week4_mixed_oi0_{repro,simclock,simclock_batchpose,handofffix}_20260715`, `week4_{box_pouf_gap31,pouf_hold40,pouf_hold25_control,pouf_helmet_*}_20260715` | **Инструментальный фикс подтверждён, физический gate честно остаётся красным.** Wall `sleep` под нагрузкой дал только roster 4/5: обещанные метры сжимались при RTF<1. Новый subscriber `/clock` подал ровно `sim_t=0.0/1.0/4.1/5.1/7.6` и восстановил roster **5/5**. Один JSON dynamic-pose на lap заменил до пяти `ign model`: тот же 200-lap отказ завершился за **167 с вместо внешнего timeout 304 с**. Fortress иногда печатает два JSON при `-n1`; parser теперь берёт последний валидный. Точный график открыл настоящую физику: Box и Pouf по отдельности PASS, но второй крупный C-товар может остаться на занятой кромке (`x≈2.8,y≈0.66,z≈0.4`); увеличение HOLD 2.5→4.0 не принято — одиночный Pouф PASS при обоих. Pouf→Helmet: один повтор JAM/E-stop, второй физически 2/2, но оба дали Helmet ID fragmentation (roster 2→3). Runner теперь завершает latched E-stop сразу с JAM-причиной вместо тысяч frozen-кадров. 36 целевых host + 36 WSL-тестов зелёные; открытый риск перенесён в этап 23, не выдан за PASS |
| 2026-07-15 | рабочее дерево после `main@84f9e88` | **Дифференциальный разбор phantom ID длинного Cylinder в первой потоковой матрице:** одиночный Cylinder; минимальный Detergent→Cylinder→Helmet; точный шеститоварный B-эпизод до/после aspect-gate | Тихий WSL/Fortress-стенд, seed=0/orient=0, B-gap 1.0 м; depth/overlay до фикса `runs/week4_dch_frames_gap1_20260715`, контроль `runs/week4_dch_aspectfix_20260715`, полный повтор `runs/week4_oi0_pass_b_aspectfix_20260715` | **Корень и фикс подтверждены A/B.** Одиночный Cylinder был 1 товар/1 ID, но в плотном потоке Detergent→Cylinder→Helmet физически 3/3 при roster **5 ID**: Cylinder временно делился на половины 213–234×48–57 мм, затем сливался обратно в 432–437×48–56 мм. Реальная ложная маска имела aspect **9.06**, обычный Cylinder 8.58; поддерживаемые touching-box пары 1.30–2.01. Консервативный gate `aspect≤4` сохраняет длинное тело одним объектом: тот же минимальный эпизод **3/3 и roster 3/3/3**, точный B-повтор **6/6 и roster 6/6/6** вместо прежних 8 ID. Новый synthetic guard до фикса даёт 2 куска, после 1; host **332 passed / 6 skipped**, WSL/ROS **355 / 3**, ruff чист. Ограничение: две длинные вещи в end-to-end касании намеренно останутся одной маской — безопаснее недоделить неподтверждённое касание, чем послать две команды на один товар |
| 2026-07-15 | `main@6618ded` | **Day 20 freeze-регрессия:** свежий полный census текущего общего main, triage и два физических safety-сценария после чистого Day 19 build/e2e | Тихий WSL/Fortress-стенд; `LOGDIR=runs/week3_freeze_main_20260715 run_matrix.sh 0 3`; `triage_matrix.py`; затем строго последовательно `smoke_multi_item.sh` и `LOGDIR=runs/week3_freeze_estop_20260715 smoke_estop_stream.sh` | **GO:** census **33/33**, каждая из 11 моделей 3/3, classification=0, execution=0, TIMEOUT=0. Multi-item: simultaneous `ids=[1,2]`, оба в B (`x=6.154/6.072`). Occupied E-stop: оба `dx=0.000`, engaged C-blade `0.710→0.709` рад, `FIRED` 2→2. Вместе с чистым checkout (`347 passed / 3 skipped`, SDF 13/13, e2e PASS) закрывает freeze-набор; логи разных ревизий не складываются в один статистический знаменатель |
| 2026-07-15 | `main@abc8be8` | **Day 19: инженерный аудит и повтор README на чистом checkout после всех изменений недели 3** | Временный detached worktree `C:\tmp\ozon-clean-day19`, без `install/` и `sim/models/items`; WSL `ozon`; с нуля `colcon build --packages-select ros_msgs` → `build_item_models.py` → `check_sdf.sh` → полный lint/pytest → `run_skeleton box_300x200x200 B`; затем временный каталог удалён | **GO:** ROS-пакет собран, 11 моделей сгенерированы, оба мира + 11 моделей SDF OK, ruff чист, **347 passed / 3 skipped**; сквозной Box300→B **PASS**, cycle 18.0 с. До этого полный Windows-прогон вскрыл Unicode-дефект самого теста (`cv2.imread` не читал фикстуры из пути `C:\Users\Максим`): тест переведён на production `load_depth_png`, после фикса хост **324 passed / 6 skipped**. SVG-компоновка теперь математически связана с асимметричными collision-only бортами SDF; README документирует входы раннеров; architecture явно отделяет sim от production STEP/safety |
| 2026-07-15 | `main@66f17e4` | **Независимая приёмка 25 командных коммитов + safety после синхронизации:** полный lint/ROS pytest/SDF/real-frame validation; затем multi-item и occupied E-stop строго последовательно | WSL `ozon`, тихий Fortress-стенд; `ruff check .`, `pytest -q`, `check_sdf.sh`, `measure_validation.py`, `smoke_multi_item.sh`, `LOGDIR=runs/week3_post_sync_estop_20260715 smoke_estop_stream.sh` | **GO:** ruff чист; **344 passed / 3 skipped**; оба мира + 11 моделей SDF OK; visible contract **8/8**, expected reject **4/4**. Multi-item: simultaneous `ids=[1,2]`, оба товара в B (`x=5.557/5.495`). E-stop: оба `dx=0.000`, engaged C-blade `0.710→0.709` рад (не парковка), `FIRED 2→2`. Новые метрики/оптимизация perception не дали safety-регрессии. Soak-когорты и dirty-stand checklist сведены в `docs/report/soak.md`; два уже снятых post-change census 33/33 + 33/33 не дублировались третьим без причины. |
| 2026-07-15 | `main@f394bfc` | **Пограничная матрица после принятия pose-ветки:** Pouf, Pen, Plate, Cylinder, Helmet × 3 seeded-ориентации через полный контур и телесный verdict | WSL `ozon`, тихий стенд, `run_matrix.sh 0 3 6 10`, финальный `cell_diverter.sdf`; 15 свежих Gazebo-эпизодов; логи `runs/week3_boundary_main_20260715`; `triage_matrix.py` | **routing 15/15; каждый товар 3/3; classification=0, execution=0, TIMEOUT=0.** Pen стабильно C при 9–10 мм; Cylinder B (`K=0.12`); Plate D (`K=0.98`); Helmet B, включая oi1 со средней осью 313 мм и oi2 после 3D-фикса 340×285×280; Pouf 3/3 C по телу. Краткая matrix-строка не нашла measurement у трёх Pouf и Plate oi1 из-за порядка flush при остановке launch, но полные логи содержат решения classifier, `FIRED` и физический body-scored PASS. У Pouf oi0 legacy-origin всё ещё дал бы ложный FAIL (`origin z=0.473`, lowest body z=0.008), что независимо подтверждает правильность текущей линейки. |
| 2026-07-15 | main после isolated review `sim/verdict-scores-the-body` | **Приёмка pose-sweep/ограждений/3D body-OBB без слияния старой ветки целиком:** одинаковые frozen frames, отдельный worktree, стоимость кадра, исходная наклонная поза Шлема и multi-item smoke | WSL `ozon`, main и `C:\\tmp\\ozon-pose-review`; Bag/Helmet benchmark ×40; проблемный Helmet с фиксированным quaternion ×2 в worktree + ×1 после переноса; `smoke_multi_item.sh`; полный lint/pytest/SDF | **GO.** Frozen категории сохранены; медиана кадра выросла примерно на 9% (Bag 5.75→6.25 мс, Helmet 8.31→9.07 мс). Исходный Helmet, ранее падавший у x≈2.1, теперь **3/3 B PASS** (2/2 изолированно, 1/1 на main; финальный main-кадр 350×298×267, K=0.73); multi-item сохранил `ids=[1,2]`, оба товара в B. Расширенная real-frame validation: **visible recall 7/7, category 7/7, partial reject 1/1**. Итоговый main: ruff чист, **294 passed / 3 skipped**, оба мира и 11 моделей SDF OK. Приняты только семь тематических коммитов; stale merge и отменённый docs-вывод не переносились. |
| 2026-07-15 | `main@d65b0ad` | **Повтор физических нештатных сценариев:** два близких товара в одном кадре + E-stop занятой ячейки с уже выдвинутым позиционным шибером | WSL `ozon`, свободный Fortress-стенд; `bash scripts/smoke_multi_item.sh`; `LOGDIR=runs/week3_estop_stream_20260715 bash scripts/smoke_estop_stream.sh` | **Оба PASS.** Multi-item: simultaneous `ids=[1,2]`, оба box_300 физически в B (`x=5.269/5.193`). Occupied E-stop: оба товара после осадки неподвижны (`dx=0.000 м`), C-лопасть не паркуется (`0.913→0.900 рад`, parked=0), поздних срабатываний нет (`FIRED 2→2`). Это физические доказательства для крайних концов сценарной матрицы; feed/jam/late-decision внутри контроллера закрываются детерминированными ROS node-тестами, а не выдаются за случайно вызванный Gazebo-затор |
| 2026-07-15 | `main@12f141e` | **Чистая серия плотного пяти-товарного потока** для воспроизведения Lunchbox B→D без изменений физики/CV | WSL `ozon`, Humble + Fortress, отдельный свежий Gazebo на эпизод, seed=0/orient=0; box_400→C @0 с, pen→C @1 с, bottle→D @4.5 с, lunchbox→B @8 с, detergent→B @9 с; 5 повторов; `runs/week3_dense_{baseline,repeat2..5}_20260715_seed0` | **5/5 all-pass эпизодов, 25/25 товаров:** каждый маршрут 5/5, включая Lunchbox→B; perception видел ровно id 1–5, controller принял все решения, в логах нет `QhullError`, `FEED JAM`, `E-STOP` или смерти ноды. Старые три Lunchbox B→D FAIL не удаляются и не объявляются исправленными: текущая чистая серия их **не воспроизвела**, поэтому менять механику без различающего сигнала нельзя. Серия измеряет надёжность конкретного плотного плана, но не заменяет headline throughput штатного 3-товарного потока |
| 2026-07-15 | `main@12f141e` + рабочее дерево | **Изоляция D→B на расчётном пределе возврата шибера + аудит calc↔sim сводки:** Bottle→D, затем Lunchbox→B ровно через `HOLD 2.5 + RETRACT 0.6 = 3.1 с`; отдельный dry-run 3.0 с | WSL `ozon`, seed=0/orient=0; 3 исходных повтора + 1 контроль после сохранения `plan.log`; `runs/week3_db_floor{1,2,3}_20260715`, `week3_db_planlog_validate_20260715` | **4/4 эпизода, 8/8 товаров; Bottle→D 4/4, Lunchbox→B 4/4.** План 3.0 с отвергнут ДО Gazebo: D-лопасть ещё удерживается; 3.1 с принят с margin `+0.00 с`. Выходные arrival-разницы при этом 0.8–2.0 с, потому что B и D имеют разные пути/осадку. Это вскрыло ошибку инструмента: arrival-такт сравнивался с FEED-полом и печатал ложный `BELOW FLOOR`. Теперь раннер сохраняет входной `plan.log`, floor проверяется по нему, arrival остаётся только описательной метрикой производительности. Сам D→B timing старый Lunchbox-промах не объясняет |
| 2026-07-14 | `main@9eb5ca5` | Повтор штатного `scripts/smoke_multi_item.sh` после закрытия конкурентных Gazebo-сессий; проверка открытого go/no-go non-touching multi-item на стыке touching-split и `feed_timeout_s=8` | WSL `ozon`, Humble + Fortress, свободный стенд, `cell_diverter.sdf`, два `box_300x200x200`, зазор 0.65 м; команда `bash scripts/smoke_multi_item.sh` | **PASS, exit 0, 58.4 с:** simultaneous frame `ids=[1,2]`; controller: `item 1: B`, `item 2: B`; обе модели в B (`x=5.918/6.040`, `z=0.400/0.400`). В логах нет `FEED JAM`, `E-STOP`, `Traceback`, `QhullError`; предыдущий стартовый отказ не воспроизведён без изменений кода и отнесён к состоянию загруженного стенда |
| 2026-07-14 | `main@09a2e70` / `cv/touching-masks-split@933b3b6` | **Differential Gazebo-прогон плотного 5-товарного потока** после аудита throughput: одинаковый сценарий на смёрженном main и touching-ветке с восстановленным border-gate | WSL `ozon`, Humble + Fortress, `cell_diverter.sdf`, seed=0, orient=0; box_400→C @0 с, pen→C @1 с, bottle→D @4.5 с, lunchbox→B @8 с, detergent→B @9 с; по 2 последовательных прогона на ветку; логи `runs/dense_compare/{main,touching}_valid{1,2}` | **main: 4/5, 4/5; touching: 4/5, 5/5.** Во всех четырёх прогонах perception увидел id 1–5, controller принял пять решений, Bottle получил `pusher_d FIRED`, Lunchbox и Detergent — B; `QhullError`, смерть узла, `FEED JAM` и `E-STOP` отсутствуют. В трёх FAIL Lunchbox вместо B остался в полосе D (`x=3.02..3.26`, `y=−0.96..−0.85`); в одном touching-повторе дошёл до B (`x=5.00`, `y=−0.04`). Наблюдение подтверждает недетерминированность плотного физического потока, но **не воспроизводит аварийную цепочку** seed1 (`QhullError → смерть perception → FEED JAM → E-STOP`) и не даёт основания объявлять общий корень. Эти 5-товарные прогоны не входят в headline throughput 18 шт/мин |
| 2026-07-14 | 793afdc + рабочее дерево | **Frozen real-Gazebo validation Дня 11:** production `measure_item → classify_conservative` на Bag oi1/oi2, Helmet oi2, Plate oi1 и новых RGB+depth Pen / partial-box; новые кадры сняты `dump_item_frame.sh` с явными `FRAMES=1`, `SPAWN_X` | WSL ozon, финальный `cell_diverter.sdf`; `python3 scripts/measure_validation.py` | **visible recall 5/5; category 5/5; partial reject 1/1.** Max \|Δdim\|: Bag oi1 12.2 мм, oi2 9.9, Helmet 22.1, Plate 2.0, Pen 2.6. K: Bag 0.74/0.00 против ref 0.72, Helmet 0.76 против 0.78, Plate 0.98 против 1.00; **Pen 0.06 против best-section ref 0.99 (\|ΔK\|=0.93)** — не скрывается: top-view и лучший 3D-срез не эквивалентны, категория C всё равно верна по приоритетному габариту 9 мм. Partial-box, заспавненный x=2.3 у края FOV, даёт `measure_item=None`/`measure_items=[]`, а не фантомный маленький товар. Ограничение: 6 срезов — regression set, не статистика; старые 4 кадра depth-only, RGB есть у новых Pen/partial |
| 2026-07-14 | (ветка `cv/touching-masks-split`) | **Разделение соприкасающихся масок**: тюнинг детектора пиков и заливки на синтетике + прогон реальных одиночных кадров через `measure_items` (WSL cv2). Сравнивались: наивный подсчёт пиков EDT / h-maxima; евклидов Voronoi / geodesic grassfire; порог prominence 2..8 px, solidity fast-path 0.97 | хост numpy+scipy 1.17; WSL `ozon` cv2 5.0; синтетика двух коробов (угловой/смещённый контакт) + `day{1,4,11}_*` реальные кадры | **h-maxima+grassfire выбраны числом.** Наивный подсчёт: одиночный повёрнутый короб → 21 кусок на yaw 20°/70° (реграсс). h-maxima: одиночные 1, все touching-пары 2, стабильно при prominence 2..8 px. Grassfire против Voronoi на смещённых коробах: **260 mm против 299..322** (истина 260); угловой контакт 208 mm обоими. `watershed_ift` отвергнут (осколок 16 px). Fast-path solidity на реальных кадрах: короб 1.02 / мешок 1.01 / шлем 0.98 / тарелка 1.01 / ручка 1.04 → быстрый путь; бутылка 0.887 → медленный, но 1 товар. Стоимость: fast-path ~3 ms, полный split ~25 ms (bbox-кроп). 33 pytest хост + 45 WSL зелёные, ruff чисто. `smoke_multi_item.sh` 0.65 м не прогнан: параллельный `run_stream` активен, а cleanup smoke делает глобальный `pkill ign gazebo` — снёс бы чужой эксперимент; отложено на свободный стенд |
| 2026-07-14 | 67e84b0 | **E2e smoke после watchdog'а подачи**: `scripts/ci_e2e.sh` (полный контур, box_300→B) в docker на Arch-хосте — регрессия объявления `/infeed/fed` в `run_skeleton.sh` | docker-dev (Humble+Fortress), headless, `cell_diverter.sdf` | **PASS**: box_300→B, body-scored PASS, cycle 21.4 c; ложных FEED JAM/E-STOP нет (grep по логу — 0). Просрочка дедлайна и очистка при reset закрыты node-тестами (симулировать реальный feed_jam в smoke дёшево нельзя — застревание стохастично). 214 pytest, ruff чисто |
| 2026-07-14 | 63db672 | **Overlay с id и состоянием агрегации** на реальном кадре `day11_bag_oi1_depth.png`: measure_items → агрегат одного кадра → classify | docker-dev, cv2/numpy | Кадр подписан «id=1 214x175x168mm K=0.74 / B conf=0.20» — conf 0.20 честно отражает один кадр из N_CONFIDENT=5; картинка `docs/report/img/day9_overlay_ids_state.png`, синтетика двух товаров закрыта тестом |
| 2026-07-13 | (рабочее дерево после `650d3b9`) | **Целевой replay Helmet oi0 ×4 после единственного FAIL финального census + проверка сохранения траектории**: каждый запуск — свежий Gazebo, `seed=0`, `N=1`, `item=10`; четвёртый с `SAVE_DYNAMICS=1` | WSL `ozon`, финальный `cell_diverter.sdf`; логи `runs/helmet_oi0_replay_{1,2,3}_20260713` и `runs/helmet_oi0_replay_trace_20260713` | **4/4 PASS**; классификация B, финальный y от −0.10 до +0.34. Вместе с двумя body-scored census та же ячейка наблюдалась как **5 PASS из 6**, поэтому единичный боковой сход не воспроизведён и физику не меняем. Диагностический запуск сохранил отдельный pose-trace (935082 байт) рядом с логом ячейки и посчитал peak 1.611 м/с, 61.426 м/с², 1.289 Н·с. Финальное имя `dynamic_pose_helmet_0.log` намеренно не попадает под служебный glob `matrix_*.log`. Новый контракт `SAVE_DYNAMICS=1`: отдельная трасса на ячейку вместо перезаписываемого `/tmp/dyn_trace.log`; `bash -n`, ruff и 9 целевых pytest зелёные. |
| 2026-07-10 | d518687 | `scripts/build_item_models.py` — генерация SDF всех 11 товаров | хул на сетке 5 мм | 11/11 сгенерированы; хулы 22–974 треуг.; 35 pytest зелёные. В Gazebo пока не загружались (среда — день 1 P1, проверка — день 2) |
| 2026-07-10 | (рабочее дерево) | `scripts/check_sdf.sh` в WSL: парсер SDF по миру и 11 моделям | Fortress 6.18, headless | 12/12 OK |
| 2026-07-10 | (рабочее дерево) | `scripts/smoke_belt.sh`: короб на призматической ленте | скорость 1.0 м/с из констант | **PASS**: dx = 2.42 м за ~2.42 с — ровно 1.0 м/с; короб доехал до конца рабочей секции |
| 2026-07-10 | (рабочее дерево) | мир + ros_gz мост + `dump_camera.py`: кадры RGBD | box под камерой, 2 кадра | PNG в `docs/report/img/day1_camera_*.png`; depth проверен числом: центр 1300 мм = камера(1.9) − лента(0.4) − короб(0.2), пол 1889–1900 мм |
| 2026-07-11 | d219ebd | `src/perception.measure_dims_mm` на day1 depth-кадре Короба 300 | fx=fy=552 из hfov=1.05, belt_depth=1.5 м | **PASS ±10 мм**: [296.5, 200.0, 197.7] мм против эталона 300/200/200 (ошибки 3.5/0/2.3 мм); оверлей `docs/report/img/day2_perception_overlay.png` |
| 2026-07-11 | c02fb89 | `scripts/smoke_pusher.sh`: толкатель v0 сталкивает Короб с ленты | команда 1.2 м/с на `/pusher/cmd`, спавн у x=2.5 | **PASS**: y 0.12 → 0.69 м (край ленты 0.25) — короб ушёл на C-сторону. Ограничение: теряет контакт при падении с края (0.4 м), не доезжает до патча C (y=1.2) — доводка день 3 |
| 2026-07-11 | 570a230 | `scripts/check_stability.sh`: 11 SDF на ленте (спавн z=0.5, отстой 2 с, езда 2 с) | мир рестартится на товар, критерий 0.35≤z≤1.0 и dx>1.0 | **8/11 стабильны**. FAIL: Бутылка (z_end 0.06), Шлем (z_end 0.03, dx 0.91), Пуфик (z_end 0.59, dx 0.95) — круглые/скруглённые скатываются с безбортовой ленты. Бортики отклонены (конфликт с камерой и толкателем, см. decisions.md) → день 3 |
| 2026-07-11 | 595bb36 | `ros2 launch launch/skeleton.launch.py` в WSL: bridge + classifier | overlay `ros_msgs`, Gazebo не запущен | **PASS**: подняты `/camera_bridge`, `/classifier`; топики `/camera/{image,depth_image,camera_info}`, `/item/measurement`, `/item/classification`; classifier подписан на measurement, публикует classification. perception_node/controller_node — день 3 |
| 2026-07-11 | 015cf51 | `check_stability.sh` v2: мягкий пуск (рамп 8×0.125), прекат ленты в −3.2 (ход 6.4 м), ретраи позы, pkill сирот | шаг 0.002; сравнение: kp/kd ленты и шаг 0.001 — без эффекта | **10/11** (было 8/11): Бутылка и Пуфик с рампом едут 6+ м. FAIL только Шлем (z_end 0.17, dx 1.2) — «качалка»-оболочка, детерминированно укатывается в +y → геометрия удержания, день 3 |
| 2026-07-11 | 631bb66 | Проверка пиксель→мир: кадр с коробом в (1.8, 0.1) + `measure_item` на обоих кадрах | `day2_offset_x1.8_y0.1_depth.png`, fx=fy=552 | Отображение подтверждено: смещённый кадр → позиция (1.791, 0.100), ошибка 9/0 мм; центр — (1.501, 0.001), K=0.56 (аналитика 0.5547). Габариты на внеосевом ракурсе завышены до +15 мм (боковые грани в маске) — ограничение v0 |
| 2026-07-11 | faa377b | **Живая цепочка**: gz + `skeleton.launch.py` (bridge+perception+classifier), Короб 300 под камерой | intrinsics из /camera/camera_info | **PASS**: /item/measurement = 296×200×198 мм, K=0.56, поз. (1.501, 0.001, 0.500), id=1, stamp кадра; /item/classification = **category B**. Контур камера→классификация работает end-to-end; остался controller (день 3) |
| 2026-07-11 | d80a1a5 | `smoke_pusher.sh` v2: оба толкателя, выброс 2.5 м/с | pusher_c x=2.5, pusher_d x=3.0 (разнесены), зоны придвинуты к ленте (y=±0.9) | **PASS**: Короб → патч C (y=1.12), Тарелка → патч D (y=−0.92, центр патча). Лицом к лицу на одном x не работает: паркованная лопатка перегораживает путь выброса (короб «пробило» насквозь, тарелку зажало) |
| 2026-07-11 | d80a1a5 | Регрессия нового мира: `check_sdf.sh` + `check_stability.sh` + 2 одиночных прогона Шлема | после разноса толкателей | SDF 13/13 OK; sweep 11/11 PASS, **но** Шлем в 2 контрольных одиночных прогонах упал (z 0.15/0.21) — PASS в sweep был удачей хаотичной раскачки, статус Шлема честно: **флейки**, нужен эксперимент удержания (день 3, п.5) |
| 2026-07-11 | 81a9682 | **Сквозной скелет** `scripts/run_skeleton.sh`: спавн → камера → perception → classifier → controller → толкатель, без ручных команд | спавн x=−1.5, детерминирован (случайности ещё нет — seed войдёт с ориентациями дня 4) | **PASS все маршруты**: Короб 300 → B (24.5 с), Тарелка → D (23.8 с), Короб 400 → C (23.8 с); cycle time — от старта launch до товара в зоне, включает ~10 с подъёма узлов и рампу 2.4 с |
| 2026-07-11 | 9095fa4 | Геометрия удержания Шлема: рельсы подачи + `check_stability.sh` (спавн −5.0) + Шлем через полный контур ×3 | рельсы x −6.0..0.7, низкий бортик 15 мм — отклонён (шлем взбирается по касательной) | **11/11 стабильны**; Шлем → B **3/3 PASS** (26.5/26.8/27.8 с — дольше остальных: подтормаживает о рельсы, dx 6.05 против 6.44) — флейк дня 2 снят. Ограничение: после толкателей Шлем едет с остаточным покачиванием (z до 0.65), но с ленты не сходит |
| 2026-07-11 | 96e8c74+ | e2e `run_skeleton.sh` после фиксов ультраревью (маска 5 мм, флипы категории) | те же 3 эпизода, что в вехе | Короб 300 → B PASS (25.4 с); Тарелка → D сначала **FAIL** (маска 5 мм захватила дно тарелки, медиана дала высоту 8 мм → C) → фикс: высота = 1-й перцентиль глубины; после фикса Тарелка → D PASS (28.3 с, мерит 28 мм), Короб 400 → C PASS. 66 pytest зелёные |
| 2026-07-11 | (рабочее дерево) | День 4, P4: `src/aggregation.py` + `classify_conservative()` — юнит-тесты на синтетических последовательностях кадров | WSL `ozon`, `pytest -q` + `ruff check .` | **PASS**: 70 passed, 3 skipped (rclpy-ноды — в ROS-CI), ruff чист. Медиана отбрасывает K-спайк 0.85 у Цилиндра (per-frame → D) и +30 мм внеосевой габарит → категория не флипает; confidence растёт с кадрами (0.2→1.0 за N_CONFIDENT) и падает при разбросе; пограничная четвёрка через конс-политику в правильные зоны. e2e-эффект (нет флипов в логах прогона) — прогон в WSL, день 4 P1+P2 |
| 2026-07-11 | 9965aef | Проверка нод + инъекции ориентации: rclpy-тесты с оверлеем; 1 ячейка матрицы box_300 с неидентичным кватернионом (seed 0, item 1, orient 1) | WSL `ozon`, source install/setup.bash; `run_skeleton` с ORIENT_* | **Ноды 8/8 PASS** (классификатор через живой граф, флип-тесты контроллера целы). Ориентация инъектится: Fortress принял поле `orientation:`, box_300 мерится 341×315×200 (identity — 296×200×198), маршрут B сохранён (conf 0.88, n=13). **Находка: perception мерит осевой AABB → под поворотом раздувается** (кадр 341×322×200, 322>лимит 320); медиана вытащила B, но впритык и зависит от ориентации. Фикс — OBB (`cv2.minAreaRect`) вместо пиксельного bbox, задача P3 (устойчивость perception к ориентациям) |
| 2026-07-11 | (рабочее дерево) | OBB-фикс perception (поворотные калиперы по hull): та же ячейка box_300 (seed 0, item 1, orient 1) до/после + `test_obb_dims_invariant_to_yaw` | WSL `ozon`, `pytest -q` + повтор ячейки в Gazebo | **PASS**: осевой bbox 341×322 → OBB **302×228** (длинная сторона +2 мм против +41, короткая ушла с 322 под лимит 320 → устойчивый B). Юнит: OBB инвариантен к yaw 0/20/45/70° (±4%), осевой дал бы ~226×226 на 45°. 84 passed (0 skipped с оверлеем), ruff чист, real-frame ±10/±20 мм целы (+1 px конвенция). Остаток короткой стороны ~30 мм — наклон/перспектива, не yaw (ограничение v0) |
| 2026-07-11 | (рабочее дерево) | Вертикальное сечение K (фикс Bottle) + расширение вердикта зон. Валидация: 82 pytest, **16 реальных top-down кадров** (3 seed-позы граничных товаров) через `measure_item`, 3 сквозных прогона | WSL `ozon`; кадры дампнуты `scripts/dump_item_frame.sh`, разбор — `debug_section` (скретчпад) | **Классификация по реальным кадрам:** Bottle 3/3 K=1.000 → **D** (был 0.29–0.30 → B); Cylinder 3/3 K 0.11/0.20/0.11 → **B** (ловушка стяжек цела); Helmet сечение молчит (K=силуэт 0.76/0.76/0.82); Box 0.56, Lunchbox 0.63, Detergent 0.72 → B; Bag K=0.88 → D (силуэт, известный дефект недели 2, сечение НЕ сработало — rms 0.086); Pouf/Pen → C по габаритам. Сырьё гейтов (tau=peak/R, rms/R) в decisions.md. **Сквозной контур:** Bottle→D PASS (k=1.0), Cylinder→B (k=0.111, n=13), Helmet→B PASS (**агрегат k=0.780**, эталон 0.78). Регрессий нет: сечение флипает ТОЛЬКО Bottle. `ruff` чист |
| 2026-07-11 | (рабочее дерево) | Сквозной box_400 (oi0, expect C) — валидация расширенных допусков зон + P2-хендофф по застреваниям | WSL `ozon`, `run_skeleton box_400x400x300 C` | **PASS**: C (k=0.607), сел y=1.298 z=0.010 (пол), pusher_c FIRED, cycle 25.2 с. В этом прогоне НЕ заклинил — значит застревания box_400/detergent из отброшенной матрицы **стохастичны по ориентации** (P2: развести рельсы подачи под крупные габариты / больше пред-осадки). **P2-тулза:** `run_skeleton.sh` может зависнуть намертво (pouf oi0 висел ~1ч40м: gz жив, вердикт не отдаётся) — матрице нужен per-cell timeout (`timeout 180 bash run_skeleton …`), иначе один зависший товар вешает весь прогон |
| 2026-07-11 | (первый запуск, отброшен) | `run_matrix.sh 0 3` после первой версии фикса — **зависла на pouf oi0** (~1ч40м, gz жив, вердикт не отдаётся), убита на 18/33. Bottle показал K=0.30 | WSL `ozon`, headless | **Ложный сигнал из-за устаревшего кода:** матрица гоняла старую перцепцию (Bottle 0.30 → B), хотя правка уже в дереве — воспроизведено дампом того же кадра: текущий код даёт K=1.0. Урок (Карпаты №1): офлайн-рендер STL был слишком чист (bottle tau 1.99, Helmet 1.31) — реальные кадры Gazebo дали tau bottle 2.2 / helmet 2.98, что и вскрыло helmet-регрессию одногейтовой версии. Перешли на валидацию дампом реальных кадров вместо флаки-матрицы: моё изменение трогает только K, физику/толкатели — нет. Box_400 в этом прогоне 0/3 «no measurement» — заклинил на подаче (P2), не дошёл до камеры → расширенный вердикт зон этим прогоном не проверялся (геометрия сверена с cell.sdf) |
| 2026-07-12 | (рабочее дерево) | `tools/precision_sweep.py` — офлайн-свип: STL→top-down depth (trimesh-растеризация тем же pinhole, что perception) → `measure_item` на всех 11 в flat-rest позе | Windows, без Gazebo; `pytest tests/test_precision_sweep.py` (box_300+plate) зелёный | **Cross-check 9/11** (SYNTHETIC, чище Gazebo — не для тюнинга порогов): подтвердил замки на реальной STL-геометрии — box_300 299×200×200 K=0.57→B (Δ2), bottle 301×91×90 K=0.91→**D** (section-K ловит лежащий бок), cylinder K=0.11→B (ловушка стяжек), plate/pouf→D/C, box_400/lunchbox/detergent/helmet→верно. **2 промаха-лида: (1) bag K=0.832→D** против эталона B — воспроизводит straddle 0.8 из Gazebo-матрицы **офлайн** ⇒ дефект в силуэт-K скруглённого блоба, не артефакт симулятора; **(2) pen — no measurement**: 13-мм ширина рендерится в 182 px < спек-фильтра 200 (артефакт разрешения рендера, не баг перцепции; в Gazebo depth покрыл бы больше — тонкие товары у порога). Крупнейшая ошибка габаритов — helmet Δ30 мм (подтверждает флаг «perception слаб на Шлеме», день 4). Промахи — лиды для Gazebo-прогона, не вердикты |
| 2026-07-12 | cbdf3be | `run_matrix.sh 0 3` — **полный census 11×3** со seed-ориентациями и per-cell timeout (5d620c7). Дошёл до конца, все 33 ячейки | WSL `ozon`, headless, лог `/tmp/census_seed0.log` + позы `/tmp/matrix_*.log` | **routing 19/33.** Разбор 14 отказов по корню: собственно классификация/маршрут — **6** (мисмаршрут bag oi0/oi2 →D агрегат 0.87/>0.8, helmet oi0 →D агрегат 0.82, plate oi1 →B ребром K=0.15; промах перцепции pen 0/3 не измерен→дефолт B). Исполнительная часть — **8**: застревание на подаче box_400 oi2 / detergent oi1 / helmet oi2 (по 1/3, x≈спавн, нет measurement); перелёт пушера bottle oi2 / box_400 oi1 (класс верен, x за окном зоны); заклин физики pouf oi1/oi2 → **TIMEOUT (фикс 5d620c7 сработал в бою — не завис, ~1ч40м→180с)**. Чисто 3/3: box_300, lunchbox, cylinder. **Подтверждает офлайн-лиды предыдущей строки в Gazebo:** bag straddle 0.8 → 2/3 в D; pen no-measurement → 0/3. Числа сведены в `report/methodology_and_limitations.md` §2.0/2.1/2.4 |
| 2026-07-12 | (рабочее дерево) | **Bag→D фикс:** дамп реальных кадров мешка+тарелки (`dump_item_frame.sh`), замер solidity, гейт плоскостности в `measure_item`, сквозной прогон | WSL `ozon`, кадры `day4_{bag,plate}_depth.png`, `pytest -q` + `run_skeleton bag B` | **solidity опровергнута реальными данными:** мешок solidity=1.0 = тарелка solidity=1.0 (осевший мешок выпуклый, не вогнутый) → дискриминатор нулевой, отвергнут (Карпаты №1/№3). **Принят гейт плоскостности:** силуэт→D только если `K>0.8` И `dz>0.7·диаметр` (плоский диск). Мешок (dz/диам=0.85, K_sil=0.88) → K=0.000 → B; тарелка (0.14, 0.98) → D цела; Бутылка через сечение цела; Шлем (0.78<0.8) доказуемо не задет. **100 pytest зелёные** (+2 real-frame теста bag/plate, 0 регрессий), ruff чист. Сквозной **Bag→B PASS** (k=0.000, cycle 26.2 с). Пороги по реальным кадрам, не синтетике. `decisions.md` 2026-07-12 |
| 2026-07-12 | 94d8504 (контур = 2b3fba0) | `run_matrix.sh 0 3` — **повтор census 11×3 после Bag→D фикса**, на другой машине: свежая WSL `Ubuntu-22.04` (Fortress 6.18.0, ROS 2 Humble), развёрнутая с нуля по `provision_wsl.sh` | seed=0, N=3, headless, 33/33 ячеек | **routing 22/33** (было 19/33). **Фикс Bag подтверждён в Gazebo: bag 1/3 → 3/3** (K_sil 0.80/0.79/0.47 — гейт плоскостности не пускает в D), регрессий нет: box_300 3/3, lunchbox 3/3, cylinder 3/3, bottle 2/3, plate 2/3 целы. Остальная дельта — **не заслуга фикса, а стохастика исполнения**: helmet 1/3→2/3, box_400 1/3→2/3, **pouf 1/3→0/3** (в этот раз без TIMEOUT — обычный FAIL), у bottle отказ переехал oi2→oi1. Неизменны: **pen 0/3** (не измерен → дефолт B), plate oi1 →B ребром (207×107×34, K=0.15). Воспроизводимость среды: `check_sdf` 12/12, `smoke_belt` PASS, `run_skeleton box_300 B` PASS (cycle 28.5 с) |
| 2026-07-12 | (рабочее дерево) | **Ручка: фикс `_MIN_ITEM_PX` 200→24** — диагностика по реальным кадрам (`dump_item_frame.sh` × 3 seed-позы), замер шумового пола пустой ленты, сквозной прогон строки pen | WSL Ubuntu-22.04, Fortress 6.18; `run_matrix.sh 0 3 7 7`; фикстура `day4_pen_depth.png` | **pen 0/3 → 3/3 → C.** Корень: маска Ручки **155–179 px** лёжа (2.72 мм/px при 148×13×9 мм) — ниже отсечки 200 px, `_find_item` отдавал `None`, контур дефолтил в B. Шумовой пол пустой ленты измерен: **0 px** ⇒ отсечка 200 не защищала ни от чего; новый порог 24 px = на порядок выше шума, в 6× ниже Ручки. **9 мм не разрешается камерой** (3.3 px → меряется 10.7 мм, верхом на пороге 10) — тай отдан `classify_conservative` (габарит ближе 5 мм к границе → C), пороги классификации НЕ трогали. Контур: перцепция 148×12×11 мм K=0.05 → классификатор **C** (conf 0.96, n=19) → `pusher_c FIRED`. **102 pytest** зелёные (+2), ruff чист, регрессий нет. Ограничение: Ручка стоймя (15 px) ниже детекции — на ленте заваливается плашмя (oi0 PASS) |
| 2026-07-12 | 94d8504 (контур = 2b3fba0) | Переспрос строки pouf (`run_matrix.sh 0 3 6 6`) — проверка, просадка ли `pouf 0/3` из строки выше | seed=0, те же 3 ориентации, логи вне `/tmp` | **Просадка не подтвердилась: pouf снова 1/3**, как в census ⇒ `0/3` был стохастикой, а не регрессией. Корни отказов — **исполнение, не классификация**: oi0 **PASS** (489×421×265 мм → C верно, `pusher_c FIRED`, сел x=2.40 y=1.26); oi1 — **застревание на подаче** (финальная поза x=−1.26 при спавне −1.5, до камеры не доехал, нет measurement); oi2 — **перелёт пушера** (`pusher_c FIRED` at t=20.8 с, но товар ушёл на x=4.75, z=0.50 — мимо окна зоны). TIMEOUT не повторился. Вывод: две стохастические болячки исполнения (подача крупных габаритов + окно срабатывания пушера) — это и есть цена pouf/box_400/detergent/helmet, задача P2 |
| 2026-07-11 | de58141 | `run_matrix.sh 0 3` — матрица маршрутизации 11×N со seed-ориентациями (OBB в контуре). **Прервана на 7/11** (pouf/pen/plate/cylinder/helmet не дошли) | WSL `ozon`, headless, 21 ячейка | **Разбор по причине (не по verdict):** чисто верно 8 (box_300 3/3, lunchbox 3/3, detergent 2/3). Ошибки классификации 5: **Bottle 0/3** — лежит на боку, top-view K=0.30 (эталон 1.00) → B вместо D (ограничение одной проекции, см. decisions.md); **Bag 2/3** — измеренный K 0.79–0.87 (эталон 0.72) straddle порога 0.8 → флип в D (конс-политика K не трогает by design). Не-классификация 4: **box_400 3/3** классифицирован C **верно**, но verdict FAIL — 2 приземления на границе патча (y=1.30012, z=0.21, мм за допуском) + 1 застрял на спавне; **detergent oi1** застрял на подаче. OBB держит: box_300 3/3 B несмотря на повороты (наклонная oi2 — 341×274, всё ещё fits→B). Итоги: (а) top-view K — главный провал классификации, кандидаты фикса — неделя 2; (б) допуски зон в `run_skeleton` тесноваты (box_400 маршрутизирован верно, FAIL по мм) — расширить; (в) физика под случайной ориентацией: застревания на подаче — P2; (г) прогнать хвост 4 товаров захарденным `run_matrix` |
| 2026-07-12 | 079de1a | Диверт-мир: дамп depth + сквозной box_400→C (блокер дня 5) | Docker (Arch-хост), headless, HOLD_S=2.5 FIRE_LEAD_S=0.5 | **PASS**: до фикса перцепция молчала (лопасть C в кадре, блоб касался границы → None каждый кадр); после сдвига пивотов C=3.25/D=3.75 дамп чист — box_400 меряется 401×394×299 мм, K=0.61, поза (1.50, 0.00). Первый прогон после видимости: товар улетел на −y с 134 м/с² — кончик лопасти прошибал товар при развороте (тайминг толкателя); с fire_lead_s=0.5 — box_400→C PASS, сел (3.35, 0.80), cycle 22.1 с. 113 pytest в Docker зелёные |
| 2026-07-12 | bd1783b (контур = 9584db2) | **Census 11×3 через ШИБЕР** — тот же `run_matrix.sh 0 3`, что гонялся на толкателе, но `WORLD=sim/worlds/cell_diverter.sdf` (тайминги диверта подставляет сам `run_skeleton.sh`). Разбор — `scripts/triage_matrix.py` | WSL Ubuntu-22.04, Fortress 6.18, headless, seed=0, 33/33 ячеек | **routing 22/33 — РОВНО столько же, сколько толкатель (22/33), при бережности в 3.5×.** Построчно: box_300 3/3, lunchbox 3/3, bag 3/3, cylinder 3/3, plate 2/3, detergent 2/3, helmet 2/3, bottle 1/3, box_400 1/3, pouf 1/3, pen 1/3. **Разбор по корню (11 отказов): исполнение 8, классификация 3.** Исполнение: `feed_jam` 4 (box_400 oi2, detergent oi1, helmet oi2, pouf oi1 — не доехали до камеры, финальный x ≈ спавн; болячка ПОДАЧИ, общая для обоих миров, механизм ни при чём); `mech_overshoot` 2 (**bottle oi1/oi2: класс D верен, товар сошёл с ленты на пол, но лёг на y=−0.48/−0.44 при кромке патча −0.5** — недолёт 2–6 см); `no_fire` 2 (**pen oi0/oi1: классифицирован C, строки FIRED в логе нет вообще** — новый лид, всплыл только после фикса детекции Ручки). Классификация: `no_detect` 2 (box_400 oi1, pouf oi2 — проехали камеру, перцепция молчит), `misroute` 1 (plate oi1 ребром: 209×107×32, K=0.169 → B). **Оговорка честности:** census толкателя (22/33) гонялся ДО фикса `_MIN_ITEM_PX` (там pen 0/3 по no-detect), поэтому равенство 22=22 — это равенство итога, а не строк; сравнимы разборы по причине, а не сами суммы. **Артефакты прогона утеряны:** логи писались в `/tmp`, WSL выключился после прогона и вычистил его — числа сняты до потери, но эпизоды не переспросить (фикс — следующая строка) |
| 2026-07-12 | ac4847e | `compare_mechanisms.sh`: одинаковый поток B/C/D через оба механизма | Docker, CAPTURE_DYNAMICS=1, box_300→B, box_400→C, plate→D | **6/6 PASS.** Толкатель: box_400→C peak_accel=381 м/с² impulse=16.0 Н·с, plate→D 168.7/1.5; шибер: box_400→C **109/7.1** (3.5× бережнее по ускорению, 2.3× по импульсу), plate→D 111.4/1.0. B-строки идентичны (9.8/1.5) — механизм не трогает проходящий товар. Разбор пиков шибера по трассе: максимум — **приземление с кромки ленты** (z 0.4→0, t=22.53), контакт со стенкой 68–77 м/с²; смоук-цифра 9.06 из шага 2 — артефакт сломанного прогона (без выстрела трасса мерила ровную езду по ленте). Cycle сопоставимы (19.5–22.1 с оба) |
| 2026-07-12 | (рабочее дерево, контур = 3b8bf71) | Переспрос строки Ручки через шибер (`run_matrix.sh 0 3 7 7`, `WORLD=cell_diverter`) — проверка диагноза `no_fire` из census. **Логи впервые сохранены в `runs/`** | WSL Ubuntu-22.04, Fortress 6.18, seed=0, 3 ячейки | **pen 0/3 (в census было 1/3 — стохастика). Диагноз `no_fire` ОПРОВЕРГНУТ:** oi0 — товар на ПОЛУ (x=3.36, y=0.464, z=0.015) при кромке патча y=0.5 ⇒ механизм сработал, недолёт 3.6 см; строки `FIRED` нет лишь потому, что kill launch обрезает stdout нод. oi2 — `FIRED at t=24.01`, но Ручка осталась НА ЛЕНТЕ (z=0.405, y=0.008) и уехала на x=4.94 — стенка её не коснулась. **Таймлайн oi2 вскрыл корень:** последнее измерение x=2.04 → выстрел через 0.67 с ⇒ товар был уже на x≈2.7, ВНУТРИ зоны разворота лопасти (2.45–2.61). Ручка тонкая (маска ~160 px), классификатор набирает уверенность к n=18–19 кадрам (conf 0.00 на n=18 в oi0!) — точка выстрела x=2.0 к тому времени пройдена. **Вывод: узкое место — задержка классификации против длины ленты, а не механизм** (раздел «синхронизация»). **Отвергнутая гипотеза:** «контроллер планирует по координатам толкателя 2.5/3.0, а лопасти на 3.25/3.75» — расхождение реально, но 2.5 это ТОЧКА ПЛАНИРОВАНИЯ, а не геометрия: планирование по пивоту сдвинуло бы выстрел в зону разворота и вернуло удар лопастью (134 м/с²). Фикс откачен, калибровка закреплена тестом-замком (decisions.md) |
| 2026-07-12 | (рабочее дерево) | **Склиз (chute) от кромки ленты к зоне** в диверт-мире + одиночные прогоны 5 товаров через полный контур | WSL Ubuntu-22.04, Fortress 6.18, `CAPTURE_DYNAMICS=1` | **5/5 PASS, оба собственных дефекта шибера закрыты.** Бутылка **D PASS** (села y=−0.903 — центр патча; в census застревала на −0.48, недолёт), Ручка **C PASS** (y=0.768; в переспросе лежала на 0.464), box_400 **C PASS**, Тарелка **D PASS**, box_300 **B PASS** (y=−0.0004 — склиз не трогает проходящий товар). **Бережность выросла ещё раз:** box_400→C peak_accel **62.3** м/с² (было 109 без склиза, у толкателя 381 ⇒ разрыв **6.1×**), Тарелка→D **58.3** (было 111.4), Бутылка→D 54.2. Импульс box_400 7.04 (было 7.1) — импульс держит контакт со стенкой, а падение с кромки убрано, что и подтверждает разбор трассы дня 5. **Первый вариант склиза дал регрессию** (плита до высоты ленты z=0.4): лопасть паркуется на y=±0.28 — ВНУТРИ y-полосы склиза, плита резала её насквозь и дефлектор не мог повернуться (bottle уехала по ленте, FAIL). Фикс — верхняя кромка z=0.34, под нижней кромкой лопасти (z=0.35): товар делает шаг 6 см с ленты вместо падения 40 см. Геометрия закреплена тестами (`tests/test_diverter_world.py`, 6 шт., проверены мутацией) |
| 2026-07-12 | 69ce940 | **Census 11×3 через ШИБЕР СО СКЛИЗОМ** — тот же `run_matrix.sh 0 3`, `WORLD=cell_diverter` | WSL Ubuntu-22.04, Fortress 6.18, seed=0, 33/33 ячеек, логи `runs/matrix_20260712_113511_seed0` | **routing 26/33** (было 22/33 и у шибера без склиза, и у толкателя). **Отказов механизма НОЛЬ:** `mech_overshoot` 2→0, `no_fire` 2→0. Построчно выросли ровно те товары, что недолетали: **bottle 1/3 → 3/3**, **pen 1/3 → 3/3**. Целы: box_300 3/3, lunchbox 3/3, bag 3/3, cylinder 3/3, plate 2/3, helmet 2/3, detergent 2/3. Остаток 7 отказов — **не механизм**: `feed_jam` 4 (box_400 oi2, detergent oi1, pouf oi1, helmet oi2 — застревание на подаче, болячка общая для обоих миров), `no_detect` 2 (box_400 oi1, pouf oi2 — проехали камеру, перцепция молчит), `misroute` 1 (plate oi1 ребром: 209×113×35, K=0.185 → B). Узкие места теперь ровно два: **подача крупных габаритов** и **перцепция на нестандартных ракурсах** — исполнительная часть свою цену больше не платит |
| 2026-07-12 | aa55c97 | **Census 11×3 с высотой спавна по позе** (`WORLD=cell_diverter`, тот же seed=0) | WSL Ubuntu-22.04, Fortress 6.18, логи `runs/matrix_20260712_144200_seed0` | **routing 26/33 — сумма та же, но состав отказов сменился.** Выигрыш фикса: **box_400 1/3 → 3/3**, **detergent 2/3 → 3/3**; класс отказов `feed_jam` практически исчез (4 → 1). Просели ровно на столько же: lunchbox 3/3→2/3 (`physics_wedge`), bag 3/3→2/3 (`no_detect`), helmet 2/3→1/3 (`misroute`: перцепция раздула шлем до 336×307 мм → габарит >320 → C вместо B). **Главный сдвиг — не в сумме, а в природе отказов: исполнение 8 → 2, классификация 3 → 4.** Исполнительная часть (30 б) практически вычищена; остаток — перцепция и правило. Просадка трёх ранее стабильных товаров может быть шумом (pouf уже скакал 1/3→0/3→1/3 между одинаковыми прогонами) — переспрос строк запущен |
| 2026-07-12 | eebc95a (ветка `feat/wider-belt`, НЕ мержится) | Census 11×3 на **широкой ленте 700 мм** — проверка гипотезы «застревают товары шире прохода рельсов (504 мм)» | seed=0, лента 700 мм, проход 604 мм, лопасти/склиз/зоны пересчитаны | **Гипотеза ОПРОВЕРГНУТА: routing 26/33 — ровно как на узкой ленте, при этом бережность упала вдвое** (box_400→C peak_accel 62 → 120 м/с²: товар едет дальше поперёк, разгоняется и жёстче садится). Застревания остались (box_400 oi2 снова замер на спавне x=−1.49) ⇒ корреляция «ширина > прохода» была ложной. Настоящий корень — спавн товара ВНУТРИ ленты (следующая строка). Ветка оставлена в репозитории как отрицательный результат: расширять ленту не нужно |
| 2026-07-12 | 07cef4c | **Переспрос просевших строк** (lunchbox, bag, helmet) после фикса спавна — шум или регрессия? | seed=0, те же ориентации, `LOGDIR=runs/recheck*` | **Регрессия НЕ подтвердилась — это шум прогона.** lunchbox **3/3 PASS** (в census был `physics_wedge`), helmet oi0/oi1 **PASS** (в census oi0 дал `misroute` — перцепция раздула габариты; при повторе не воспроизвёлся). Подтвердилась только просадка bag: 2/3 (oi1 ушёл в D вместо B). **Скорректированная оценка фикса спавна: ≈28/33, а не 26/33.** Следствие для методики: **одиночный census шумит на ±2–3 ячейки** (pouf 1/3→0/3→1/3, lunchbox 3/3→2/3→3/3, helmet 2/3→1/3→PASS на тех же ориентациях) ⇒ сравнивать варианты по ОДНОМУ прогону нельзя, метрику вехи брать как повтор ≥2 прогонов. Это же ответ жюри по критерию воспроизводимости: seed фиксирует ориентации, но не физику Gazebo |
| 2026-07-12 | aff3add | **Census #2 (повтор для вехи)** — тот же seed=0, тот же коммит, тот же мир; проверка воспроизводимости метрики | WSL Ubuntu-22.04, логи `runs/milestone_run2` | **25/33** против 26/33 первого прогона. **Стабильно чистые 6 товаров (18 ячеек, 3/3 в обоих):** bottle, box_300, box_400, detergent, pen, cylinder. **Стабильно проблемные:** plate 2/3 (oi1 ребром, K=0.167 — единственный полностью ДЕТЕРМИНИРОВАННЫЙ дефект), bag 2/3, pouf 1/3, helmet 1/3. **Тревога прогона: зависания физики выросли вчетверо** (4 `physics_wedge`/TIMEOUT против 1) — регрессия от фикса спавна, разобрана ниже |
| 2026-07-12 | (рабочее дерево) | **Зазор спавна 20 → 5 мм**: диагностика зависаний + переспрос трёх проблемных строк | `runs/probe_clearance.sh`, `runs/clear5_*` | **Корень зависаний — мой же зазор:** товар падал на ленту с 20 мм, и удар раскачивал скруглённые тела до взрыва контактов. Переспрос pouf oi1 с изменением ТОЛЬКО этого числа: 20 мм — **FAIL** (заклин на склизе, cycle 123 с), 5 мм — **PASS** (cycle 69 с). После фикса: **зависаний нет ни одного** (все 9 ячеек дали вердикт), **lunchbox 1/3 → 3/3**, **pouf 1/3 → 2/3**, helmet без изменений (1/3). Оценка контура после фикса — **≈27–29/33**; точное число вехи требует полного census (следующий прогон). **Остаток крупных дефектов:** pouf oi2 и helmet oi2 замирают на спавне (x≈−1.45) — обе ячейки шире прохода рельсов (528 мм против 504) ⇒ гипотеза «узкий проход», отвергнутая ранее, могла быть верной для ЭТИХ ячеек: тогда её маскировал спавн внутри ленты (два дефекта накладывались). Проверять на исправленном спавне, ленту при этом НЕ расширять (см. отрицательный результат `feat/wider-belt`) |
| 2026-07-12 | e67cb52 (формула — 64fef00) | **Физический replay box_400 после согласования bottom-origin и `min_z` повёрнутой сетки**: `run_matrix.sh 0 3 2 2` | WSL Ubuntu-22.04, Fortress 6.18, финальный мир шибера, `runs/recheck_origin_box400` | **3/3 PASS, застреваний и TIMEOUT нет.** Все позы дали C и физически попали в зону: oi0 `(3.378, 0.792, 0.160)`, oi1 `(3.462, 0.834, 0.210)`, oi2 `(3.414, 0.953, 0.010)`; cycle **16.9–17.1 с**. Проблемная oi2 при корректном origin спавнится на z≈0.754: нижняя точка ровно на 5 мм выше ленты, а не внутри неё. Исправление подтверждено полным контуром, но итоговую метрику 11×3 по этому replay не заявляем — нужен отдельный полный census. |
| 2026-07-12 | 64fef00 | **Верификация фикса origin спавна** (height/2 → нижняя точка позы) в симуляторе: helmet+pouf, `WORLD=cell_diverter`, seed=0, N=3 | WSL Ubuntu-22.04, логи `runs/verify_helmet`, `runs/verify_pouf` | **Геометрия подтверждена в WSL до миллиметра:** зазор до ленты = РОВНО +5 мм во всех позах (helmet oi2 spawn_z 0.6597, pouf oi2 0.7467, box_400 oi0 0.4050). **Спавн-пенетрация устранена:** ранее проваливавшиеся/падавшие ячейки уходят со спавна и доезжают до камеры — helmet oi1 **PASS**, pouf oi0 **PASS**, pouf oi1 near-miss (доехал в зону C, y=1.426 при пороге 1.4 — промах на 26 мм, FIRED, K=0.994), helmet oi0 доехал до x=3.30 (порог B x≥3.5 — недолёт 0.2 м). Счёт helmet 1/3, pouf 1/3 — **внутри задокументированного шума ±2–3 ячейки, одиночный прогон не метрика** (decisions «метрика по ≥2 прогонам»). **Остаток — ДВА РАЗНЫХ дефекта, не мой спавн:** (1) **pouf oi2 TIMEOUT** — поперечный габарит **528 мм > 504** прохода рельсов ⇒ подтверждена rail-width (отдельная болячка, ленту НЕ расширять — см. `feat/wider-belt`); (2) **helmet oi2** застрял x=−1.485, но поперечный габарит **391 мм < 504 ⇒ В РЕЛЬСЫ ВЛЕЗАЕТ** — значит НЕ ширина (правка к прошлой строке: «обе шире рельсов» неверно для helmet), скорее форма/кувырок или шум; нужен повтор. **Вывод: фикс сделал ровно то, что заявлено (товар ложится на ленту, не в неё и не падает); маршрутизацию он не «чинит» — оставшиеся заклины лежат в проходе рельсов и перцепции.** |
| 2026-07-12 | (рабочее дерево, мир до фикса) | **Первый мульти-товарный поток в Gazebo** (`run_stream.sh`): box_400→C, Ручка→C в 1 м, Бутылка→D в 3.5 м | WSL Ubuntu-22.04, Fortress 6.18, seed=0, orient=0, логи `runs/stream_v2` | **2/3, такт 1.6 с (37 шт/мин).** ГЛАВНОЕ ПОДТВЕРЖДЕНИЕ: **два товара в ОДНУ зону вплотную проходят на физике** — box_400 и Ручка оба легли в C, перцепция удержала 3 раздельных ID, контроллер дал 3 независимых выстрела (снята оговорка `b10baf7` «проверено только на узле»). Бутылка ушла в C вместо D. Разбор: зазор ни при чём — **6.0 м тоже FAIL** (`runs/stream_e1`), а в зеркальном порядке (бутылка первой) лопасть D увела за собой ОБА следующих товара, ехавших в 3.5 и 4.5 м (`runs/stream_e2`). Бисект: одиночная бутылка в том же мире — **PASS** (и с мягким пуском, и сброшенная на движущуюся ленту) ⇒ дефект живёт только в потоке |
| 2026-07-12 | (рабочее дерево) | **Прямое измерение угла сустава шибера** — впервые за проект (`JointStatePublisher` + `ign topic -e`) | те же условия, логи `runs/blade_c.log`, `runs/joint_*.log` | **Лопасть НЕ ВОЗВРАЩАЕТСЯ.** C: выстрел t=23.42 → 0.95 рад за 0.4 с → **стоит 0.950 до конца записи (31.3 с)**, хотя по `hold_s`=2.5 обязана вернуться на 27.2. Команда доходит: в логе `RETRACT at t=27.09`, на топике `data: -2.5` — сустав игнорирует. Изоляция без контура и товаров: то же самое. На копии мира с пределом ±3 рад — слушается идеально (1.45 → −0.45) ⇒ **сустав залипает, когда его вдавливают в жёсткий упор**. Под позиционным (силовым) управлением стопорится на **0.040 рад** ⇒ вторая причина: **низ лопасти z=0.35 при верхе ленты z=0.40 — лопасть на 50 мм внутри конвейера**, скоростная команда (кинематический сервопривод) продавливала её сквозь ленту и прятала это. Инструментальный урок: `dynamic_pose` не публикует поворот ЗВЕНА — оба шибера в трассе выглядели неподвижными, сортируя товары |
| 2026-07-12 | (рабочее дерево, мир после фикса) | **Верификация фикса шибера**: лопасть поднята на 2 мм над лентой, сустав на `JointPositionController` | WSL, Fortress 6.18, seed=0, логи `runs/stream_fixed`, `runs/verify.log` | **Изолированный механизм: два полных цикла подряд** (0 → 0.895 → 0 → 0.895 → 0), залипания нет. **Регрессия одиночных эпизодов 3/3:** box_300→B, box_400→C, plate→D — все PASS на исправленном мире. **Поток: 2/3, но природа отказа сменилась** — cross-fire исчез, бутылка теперь уходит В СВОЮ сторону (y=−0.917, зона D), но перелетает посадочную полосу по x (легла на 4.36 при границе 4.1): цилиндр скатывается до дальнего конца склиза D и выкатывается из ролл-кейджа. Такт 1.5 с (40 шт/мин). **Оговорка честности: census 26/33 и бережность (62 vs 381 м/с²) сняты со сломанного механизма и подлежат перемеру — это следующая задача, а не установленный факт** |
| 2026-07-12 | e704669 + рабочее дерево | **`smoke_multi_item.sh`: два Короба 300 одновременно в окне камеры через полный контур** | финальный мир шибера, интервал спавна 0.65 м, один запуск мира | **PASS.** Probe получил два разных ID в одном и том же camera stamp `20.990000000`: `[1, 2]`; оба независимо классифицированы B и остались на ленте до зоны B, финальные позы `x=5.125` и `x=4.791`, `z=0.400`. Дополнительно node-тест одновременно планирует C для ID30 и D для ID31: оба механизма срабатывают ровно один раз без cross-fire. Ограничение baseline: соприкасающиеся depth-маски пока сливаются в одну компоненту. |
| 2026-07-12 | d4ba416 (механизм после `2ac9617`) | **Повтор `compare_mechanisms.sh` после исправления шибера**: одинаковые box_300→B, box_400→C, plate→D | WSL Ubuntu-22.04, Fortress 6.18, свежий Gazebo на эпизод | **6/6 PASS, cycle time сопоставим.** Толкатель: box_300 17.9 с / 8.714 м/с² / 1.500 Н·с; box_400 15.7 / **331.085** / **13.021**; plate 16.8 / **204.190** / **1.489**. Шибер: box_300 17.3 / 7.555 / 1.500; box_400 16.4 / **81.849** / **6.740**; plate 18.5 / **55.729** / **0.776**. На box_400 шибер мягче в **4.0× по ускорению и 1.9× по импульсу**, на plate — 3.7× и 1.9×. Выбор шибера подтверждён уже исправной механикой; старые headline 62.3/381 и census 26/33 остаются историческими и требуют перемера. |
| 2026-07-12 | 6279fac (ДО harness-фикса `22f8d96`) | **Независимая пара census на исправленном шибере** — снята параллельно с прогонами P1/P2, на том же коммите механизма, но на СТАРОМ runner'е (Gazebo ещё оставался между эпизодами) | WSL Ubuntu-22.04, Fortress 6.18, seed=0, 33×2, логи `runs/census_fixed_1`, `runs/census_fixed_2` | **29/33 и 28/33.** Числа НИЖЕ канонических 30–30/33 ровно потому, что этот runner ещё ловил ложные TIMEOUT от недобитых Gazebo-серверов (`physics_wedge` у cylinder — тот самый артефакт, который P1 диагностировал и починил). **Метрикой считать 30–30/33, эти два прогона — независимое подтверждение, а не конкурирующая цифра.** Что они добавляют сверх: (а) у Тарелки исчез ДЕТЕРМИНИРОВАННЫЙ misroute «ребром» (K=0.167 → B), державшийся во всех прошлых census — plate 3/3 в обоих ⇒ фикс лопасти лечил не только поток; (б) triage прогона 1 дал **НОЛЬ ошибок классификации**, все отказы исполнительные; (в) подтверждён шум ±2–3 ячейки на одних и тех же ориентациях (box_400 3/3→2/3, bag 3/3→2/3, cylinder 2/3→3/3) — то есть требование «метрика по двум прогонам» не формальность |
| 2026-07-13 | 25cfa56; replay после `22f8d96` | **Post-origin/post-shiber census #1**, `run_matrix.sh 0 3`, затем replay единственной технической ячейки | WSL Ubuntu-22.04, Fortress 6.18, `runs/fixed_census_run1_20260712`, replay `runs/replay_fixed_census_run1_cylinder_oi2.log` | **Сырой итог 29/33; скорректированный по заранее принятому правилу replay — 30/33.** 3/3: bottle, box_300, box_400, lunchbox, detergent, pen, plate, cylinder (после replay), helmet; Bag 2/3, Pouf 1/3. Причины трёх реальных отказов: Bag oi1 — `misroute` B→D (`K=1.0`); Pouf oi1 — `mech_overshoot` `(x=3.65,y=0.58,z=0.29)`; Pouf oi2 — `feed_jam` у спавна `x=-1.47`. Cylinder oi2 не создался (`Service call timed out`) из-за двух оставшихся Gazebo-серверов; точный seeded-replay после cleanup — **PASS B** `(4.930,0.003,0.420)`, 18.0 с. Runner исправлен: cleanup на EXIT, TERM→KILL с ожиданием; два последовательных B/C эпизода дали PASS и `CLEAN_EXIT`. Это первый из обязательных двух census, не финальная одиночная метрика. |
| 2026-07-13 | fbfb758 (код `d7edf8c` только уточняет triage) | **Post-origin/post-shiber census #2**, тот же `seed=0, N=3`, без replay | WSL Ubuntu-22.04, Fortress 6.18, `runs/fixed_census_run2_20260713` | **30/33 напрямую, без TIMEOUT и технических замен.** Состав совпал с corrected census #1 во всех 33 ячейках: девять товаров стабильно 3/3; Bag 2/3 (`oi1 misroute`: D вместо B, `K=0.979`); Pouf 1/3 (`oi1 mech_overshoot`, `(3.52,0.52,0.32)`; `oi2 feed_jam`, `x=-1.466`). Cylinder oi2, ранее потерянный из-за старого Gazebo, прошёл B штатно. **Итоговый диапазон двух полных прогонов: 30–30/33; стабильные причины: классификация 1, исполнение 2.** Cleanup завершился `CLEAN_EXIT`. Критерий baseline Дня 8 «два census + triage» закрыт. |
| 2026-07-12 | (рабочее дерево) | **E-stop на ЗАНЯТОЙ ячейке** (`smoke_estop_stream.sh`): два Короба 400 на ленте, лопасть C уже выдвинута стеной, затем аварийная остановка | WSL Ubuntu-22.04, Fortress 6.18, финальный мир шибера, логи `runs/estop_stream_*` | **PASS.** После остановки: оба товара замерли (смещение **0.000 м за 2 с** на устоявшемся интервале), лопасть **осталась на 0.90 рад** (парковка = 0.0) — то есть не ушла из-под товара, поздних `FIRED` нет. **Первый прогон был FAIL и вскрыл два факта:** (1) обнуление топика по E-stop уводило позиционную лопасть В ПАРКОВКУ — движение во время аварийной остановки (фикс: `estop_hold_mechanism`, контроллер переотправляет текущую команду); (2) **лопасть податлива** — Короб 400 отжал её с 0.90 до **1.24 рад**, ЗА предел сустава 0.95, который физика не удержала. Второе принято как ограничение (см. decisions), первое исправлено. Регрессия одиночного `smoke_estop.sh` — PASS (до остановки dx=0.037 м/с, после 0.000) |
| 2026-07-12 | a9e41a4 | **Регрессия детектора затора на ЗДОРОВОМ трафике** (он глушит всю ячейку, поэтому ложное срабатывание дороже пропуска): одиночный эпизод + полный поток | WSL Ubuntu-22.04, Fortress 6.18, финальный мир шибера, логи `runs/stream_jamcheck` | **Ложных срабатываний НЕТ** (0 строк `JAM` в логе), box_400→C PASS. **Поток впервые 3/3:** box_400→C, Ручка→C, Бутылка→**D PASS** (x=3.93, y=−0.818 — внутри лотка при границе 4.1). Такт: 1.3 с между товарами в ОДНУ зону и 6.7 с при смене зоны (итого 15 шт/мин на этом сценарии) — асимметрия ровно та, что предсказывал `stream_plan.py`: одну зону можно грузить вплотную, смена зоны стоит удержания лопасти. **Перелёт Бутылки за лоток (x=4.36 в прошлых прогонах) не воспроизвёлся ⇒ это пограничная посадка цилиндра, гуляющая от прогона к прогону, а не детерминированный дефект.** Метрику потока по одному прогону не заявляем — правило «≥2 прогонов» действует и здесь |
| 2026-07-13 | c552513 | **ДВА census на ЦЕНТРИРОВАННОМ спавне** (товар кладётся телом на середину ленты, а не своим origin): `run_matrix.sh 0 3`, `WORLD=cell_diverter` | WSL Ubuntu-22.04, Fortress 6.18, seed=0, 33×2, логи `runs/census_centered_1`, `runs/census_centered_2` | **31/33 и 32/33 ⇒ метрика 31–32/33** (было 30–30/33 на прежнем спавне). **Шлем 2/3 → 3/3** (его отказ и был этим багом: спавнился внутри рельса), **Пуфик 1/3 → 2/3**, Цилиндр/Мешок/Короб 400 — 3/3 в обоих. **Ошибок классификации НОЛЬ в обоих прогонах.** Единственный стабильный отказ — **pouf oi=1, `mech_overshoot`**: товар отведён механизмом верно, но ложится мимо зачётного окна на СЕНТИМЕТРЫ (прогон 1: y=1.41 при границе 1.40; прогон 2: x=3.64 при границе 3.60). **Причина не в ширине ленты, а в геометрии зоны:** патч зоны C идёт по x до 3.6, а склиз, который в неё сваливает товар, тянется до **3.9** — склиз доставляет крупное тело туда, где зоны уже нет. Плюс «ролл-кейдж» имеет ЕДИНСТВЕННУЮ стенку (дальнюю по y) и ни одной по x, поэтому катящийся цилиндр (Бутылка, x=4.35 в прогоне 1) просто выкатывается наружу. **Следствие: 4 ячейки, ранее объявленные «шире ленты», после центрирования едут и отводятся нормально ⇒ довод за расширение ленты сильно ослаб** |
| 2026-07-13 | (рабочее дерево) | **Census #1 на исправленной приёмке** (зона перекрывает склиз: патч 1.2 → 1.6 м; у ролл-кейджа появилась торцевая стенка): `run_matrix.sh 0 3`, `WORLD=cell_diverter` | WSL Ubuntu-22.04, Fortress 6.18, seed=0, 33 ячейки, логи `runs/census_zones_1` | **32/33 — но это ОДИН прогон, не метрика вехи** (правило «≥2 прогонов» действует; второй — завтра). Десять товаров из одиннадцати 3/3; **ошибок классификации ноль**. Переспрос двух ранее падавших строк дал **Пуфик 3/3 и Бутылку 3/3** — перелёт Бутылки за лоток (x=4.35) закрыт торцевой стенкой, промах Пуфика мимо края зоны — удлинением патча. **Остался ровно один отказ, и он НЕ в зоне: pouf oi=1 застревает на САМОМ СКЛИЗЕ** — поза (3.591, 0.524, **z=0.323**) при пороге z<0.25, то есть товар съехал до середины склиза и встал, не свалившись в лоток. Гипотезы на завтра: крупное тело (473 мм на склизе 605 мм) мостит 6-см ступеньку кромка-склиз и повисает на двух опорах; кандидаты — круче склиз, ниже трение, либо дольше удержание лопасти, чтобы она дотолкнула товар через кромку |
| 2026-07-13 | c243e61 | **Census #2 на исправленной приёмке** (зона перекрывает склиз + торцевая стенка ролл-кейджа): тот же `run_matrix.sh 0 3`, `WORLD=cell_diverter`, seed=0 | WSL ozon (Fortress 6.18, ROS 2 Humble), 33/33 ячеек, логи `runs/census_zones_2` | **routing 33/33 — ПЕРВЫЙ полностью чистый полный прогон.** Все 11 товаров 3/3; `triage_matrix`: **классификация 0, исполнение 0**. Вместе с census #1 (32/33 на ТОМ ЖЕ контуре c243e61) двухпрогонный диапазон вехи — **32–33/33** (было 30–30/33). Единственный отказ census #1 (pouf oi=1 застрял на склизе, z=0.323) **НЕ воспроизвёлся**: pouf oi=1 → C PASS (K=0.97) ⇒ стохастика ±2-3 ячейки, не детерминированный дефект (подтверждает правило «метрика по ≥2 прогонам»). **Латентные риски, НЕ сработавшие в этом прогоне (не устранены — просто не выпали):** helmet oi2 средний габарит **317 мм в 3 мм от лимита 320** (OBB-раздутие, 2.3); plate oi1 сел плашмя (213×211×28, K=0.98→D), «ребром» не выпал; bag oi1 per-frame K=1.00, но агрегат→B. 2 ячейки PASS с «no measurement in log» (bottle oi1, plate oi0) — вердикт по позе, строка перцепции обрезана kill-launch. Код не менялся — `ruff`/pytest не в этом прогоне |
| 2026-07-13 | (рабочее дерево) | **Диагностика + частичный фикс pouf oi=1**: переспрос ×2 до фикса и ×2 после; вердикт-полоса C/D → внутренняя грань кейджа 1.475 | WSL ozon, `runs/pouf_replay_*`, `runs/pouf_postfix_*` | **pouf oi=1 — МНОГОМОДОВЫЙ стохастический отказ, не один баг.** До фикса ×2: `mech_overshoot` детерминированно x=3.07 y=1.433 z=0.14 (в контейнере у стенки 1.5, но за старой чертой 1.4). Фикс полосы (ТОЛЬКО скоринг, не физика) снимает эту моду. После фикса всплыла ВТОРАЯ мода: `chute-stick` x=3.61 y=0.535 **z=0.315** (застрял на склизе, мостит 6-см ступеньку — как census #1 z=0.323). Итог pouf oi=1: 1/3 и 2/3 (моды чередуются); oi=0/oi=2 в основном 3/3. **164 pytest зелёные, ruff чист.** Вывод: полоса-до-стенки — честный самостоятельный correctness-фикс (снимает overshoot-моду у любого крупного товара), НО не решает pouf oi=1 целиком; chute-stick — отдельная геометрия склиза у физ. предела ячейки, отложен под ревалидацию |
| 2026-07-13 | f161e5b | **Census #3 (валидация band-фикса)**: полный `run_matrix.sh 0 3`, `WORLD=cell_diverter`, seed=0 | WSL ozon, 33/33 ячеек, `runs/census_bandfix` | **routing 31/33** (было 33/33 в #2, 32/33 в #1). **Band-фикс подтверждён БЕЗ регрессий** (полоса только расширяет скоринг, уронить ячейку не может). Два отказа — РОВНО заявленные латентные риски, выпавшие в этот раз: **bag oi=1 misroute** (классификация: агрегат K=1.0 → D вместо B, K-straddle порога 0.8), **pouf oi=1 chute-stick** (исполнение: x=3.67 y=0.58 z>0.25 — застрял на склизе; overshoot-мода полосой снята, chute-stick осталась). Триаж: классификация 1, исполнение 1. **Трёхпрогонный диапазон 31–33/33; 33/33 из #2 был ВЕРХОМ шума ±2-3.** Детерминированных регрессий нет — оба отказа на известных хрупких ячейках |
| 2026-07-13 | 05a7b2a | **Валидация elongation-фикса bag oi=1** (маршрут сечение→D теперь требует вытянутости следа ≥2×). Собранный single-pass census всех 11 товаров + bag oi=1 ×2, `WORLD=cell_diverter`, seed=0 | WSL ozon, поячеечные `run_skeleton` (полный `run_matrix`-цикл в этой среде НЕ идёт — SIGKILL после ~4 boot'ов Gazebo, и в фоне, и на переднем плане, рестарт distro не поднимает лимит) | **11/11 маршрутизируются верно, регрессий классификации нет.** **bag oi=1 → B, 2/2** (было D): K 1.00→**0.74/0.76**, dims 213×174×167 — совпадают с census #3; агрегат classifier B conf 0.96–0.98, товар доехал в зону B. Разбор на замороженном реальном кадре: K=1.0 шёл от `_section_roundness_k` (гребень блоба лёг на касательную окружность rms/R=0.015 ≈ как у лежащей Бутылки 0.009), силуэт был 0.74<0.8 — гейт плоскостности ни при чём. Остальные: bottle oi0/1/2 → D (3/3); box_300 → B (K=0.54); box_400 → C (416×400×299); lunchbox → B (K=0.63); bag oi2 → B; detergent → B (K=0.72); **pouf → C (489 мм)**; **pen → C (147×12×9)**; plate oi1 → D; **cylinder → B (K=0.11, ловушка стяжек цела)**; **helmet oi2 → B (0.77)**. Погран-четвёрка (Цилиндр/Шлем/Ручка/Пуфик) подтверждена. Фикс детерминированно меняет РОВНО одну ячейку матрицы (bag oi1 D→B): bottle всегда вытянут (не гейтится), сечение Цилиндра уже 0 по rms ⇒ снятие `k_section` может только увести D→B, ни один B не регрессирует. **Диапазон вехи 31–33/33 → 32–33/33** (в 31 падали bag oi1 + pouf oi1; bag oi1 устранён, pouf oi1 — стохастика исполнения, фиксом не затронута). 187 pytest зелёные (реальный кадр `day11_bag_oi1_depth.png` + validation-набор oi2/helmet/plate), ruff чист. **Ограничение честности: полный автоматический 33×2 census в этой среде не прогнан — цикл Gazebo убивается через ~4 ячейки; поячеечное покрытие собрано вручную. Заявляемую метрику 32–33/33 нужно подтвердить полным census на машине без этого лимита.** |
| 2026-07-13 | 38fb6e7 | **Первый полный автоматический census после elongation-фикса bag oi=1**: `run_matrix.sh 0 3`, `WORLD=cell_diverter` | WSL `ozon`, ROS 2 Humble, Gazebo Fortress, seed=0, 33/33 ячеек, `runs/census_post_bagfix_20260713_134500` | **routing 32/33. Классификация 33/33, исполнение 32/33.** Фикс Bag подтверждён в полном контуре: `bag oi=1 → B`, K=0.75; все остальные товары, включая Цилиндр, Шлем, Ручку и Тарелку, 3/3. Единственный отказ — известный `pouf oi=1`, `mech_overshoot`: маршрут C и срабатывание шибера правильны, но итоговая поза `(x=3.720, y=0.653, z=0.257)` вне окна зоны. Автоматический `triage_matrix.py`: classification 0, execution 1. **Ограничение:** это один полный повтор после фикса; второй census нужен до подтверждения диапазона 32–33/33. |
| 2026-07-13 | d512659 | **Второй полный автоматический census после elongation-фикса Bag**, тот же `seed=0, N=3`, без изменений physics/perception/mechanism | WSL `ozon`, ROS 2 Humble, Gazebo Fortress, 33/33 ячеек, `runs/census_post_bagfix_repeat_20260713` | **routing 32/33 повторно; post-Bag-fix baseline = 32–32/33. Классификация 33/33 в обоих полных прогонах.** Bag 3/3 (`oi1`: 214×174×170 мм, K кадра 0.75, агрегат 0.774 → B); остальные 9 стабильных товаров 3/3. Единственный отказ снова `pouf oi1`, правильная C и FIRED, но `mech_overshoot/chute-stick` в `(x=3.515,y=0.542,z=0.311)`. Triage: classification 0, execution 1; `CLEAN_EXIT`, TIMEOUT нет. Фикс Bag подтверждён повторно; текущий потолок — одна воспроизводимая физическая ячейка склиз→зона, а не CV. |
| 2026-07-13 | 5abe909 + рабочие прототипы, **отвергнуты** | Два локальных варианта перехода для Pouf oi1, каждый replay `WORLD=sim/worlds/cell_diverter.sdf`, seed=0, строка Pouf 3 позы. A: короткий склиз с верхом почти заподлицо (фактическая ступень ~7 мм, уклон 37.6°). B: тот же верх + удлинение внутрь зоны (уклон 28.8°). | Логи `runs/pouf_chute_replay_diverter_20260713` и `runs/pouf_chute_gentle_replay_20260713` | **Оба варианта 2/3 и оба отвергнуты; baseline SDF восстановлен.** A исправил oi1 (`z=0.139`), но дал новую регрессию oi2: правильная C + FIRED, затем `mech_miss` на высоте `z=0.430`. B исправил oi1/oi2, но сломал ранее рабочий oi0 (`mech_miss`, `z=0.462`). Значит простое поднятие или удлинение общей пластины переносит опору/опрокидывание между ориентациями, а не устраняет корень. Нужна локальная геометрия стыка, не изменение всей плоскости. Один предварительный запуск без `WORLD` оказался в pusher-baseline `cell.sdf` и **полностью исключён** из метрик; runner после этого печатает выбранный мир и предупреждает о дефолте. |
| 2026-07-13 | 73183dc | **Переспрос 4 ячеек с ЛОГОМ ОРИЕНТАЦИИ ПОКОЯ (её не писал ни один прошлый прогон) — проверка новой линейки вердикта: тело вместо origin** | WSL ozon (Fortress 6.18, Humble), `WORLD=cell_diverter`, seed=0, поячеечный `run_skeleton`, каждая ячейка печатает ОБА вердикта (legacy origin-scored и body-scored) | **«Chute-stick» Пуфика оказался артефактом линейки, а не физикой.** `pouf oi=1` (3/3 прогона, детерминированно): origin x≈3.65 y≈0.58 **z≈0.29** — та самая подпись «застрял на склизе» из census #1/#3 — но нижняя точка ТЕЛА **z=+0.011, на полу лотка**, центр (3.44, 0.68) внутри следа зоны C. **legacy FAIL / body PASS.** Товар был доставлен всё это время. **Регрессий новая линейка не даёт:** `box_400 oi=2` (одна из 4 теоретически «непроходимых» ячеек) садится плашмя, origin z=0.16 ⇒ legacy **PASS** / body PASS — то есть предсказание «непроходима» — это ВЕРХНЯЯ оценка при условии, что товар сохранит позу спавна, а реально он кувыркается; детерминированно кусается только Пуфик. `lunchbox oi=0 → B` (x=6.76) и `bag oi=1 → B` (x=7.84, K-фикс IR630 держит) — PASS по обеим линейкам, новая полоса B (x≥4.2) настоящие belt-ride ячейки не роняет. Побочно: первая версия строки сравнения ВРАЛА (звала новый вердикт с origin-позой вместо старого правила) и приписала фиксу box_400 oi=2 — поймано сверкой с числами руками, закрыто `in_zone_legacy` + тестом. 180 pytest зелёные, ruff чист. **Ограничение: полный census новой линейкой НЕ снят — метрика 32–33/33 измерена сломанным инструментом и подлежит пересъёмке** |
| 2026-07-13 | d3ec235 | ⚠️ **НЕГОДЕН — МИР НЕ ТОТ.** Ре-census ×2 на починенной линейке, но `run_matrix` не задаёт мир, а скрипт не экспортировал `WORLD` ⇒ прогон молча ушёл на дефолт `cell.sdf` — **баллистический толкатель**, отвергнутый механизм. Числа 31/33, 31/33, 30/33 — метрика ТОЛКАТЕЛЯ, не вехи; отозваны. **Ре-census ×2 на починенной линейке** (вердикт считает тело товара, полоса B — за пределом досягаемости механизмов): `run_matrix.sh 0 3`, `WORLD=cell_diverter`, seed=0, каждая ячейка печатает ОБА вердикта | WSL ozon (Fortress 6.18, Humble), 33×2, отдельный worktree на зафиксированном коммите, логи `runs/census_body2_1`, `runs/census_body2_2` | **31/33 и 31/33 — и между прогонами НЕ РАЗОШЛАСЬ НИ ОДНА ячейка.** «Шум физики ±2-3 ячейки», из-за которого введено правило «мерить по ≥2 прогонам», в этих прогонах равен НУЛЮ: часть его была не физикой, а линейкой, по-разному читавшей случайную ориентацию покоя. Отказы одинаковы в обоих прогонах, оба исполнительные, оба подтверждены И старой линейкой (⇒ не артефакт фикса): **box_400 oi=1 `feed_jam`** (застрял на спавне, x=−1.50, до камеры не доехал) и **helmet oi=0 `false_divert`** (B-товар ушёл с ленты, лёг на пол у лотка D). **Классификация — 0 ошибок в обоих прогонах.** **ВАЖНО про helmet oi=0: в прежних census он ПРОХОДИЛ (x=5.12). Сейчас падает дважды подряд — реальный НОВЫЙ отказ исполнения, требует отдельного разбора** (родственный симптом: bag oi=1 тоже сползает с ленты вбок и замирает на полу, пройдя механизмы). **Предыдущая попытка ре-census (31/33 и 30/33) ПРИЗНАНА НЕГОДНОЙ:** все её 5 отказов были `physics_wedge` — не физика, а вес самой линейки: вердикт импортировал trimesh на каждом из 60 опросов (замер: голый python 0.035 с, numpy 0.51 с, trimesh 1.33 с ⇒ +86 с к эпизоду), и ячейки без запаса вылетали за таймаут 180 с. После сведения меша к выпуклой оболочке один раз за эпизод вердикт стоит 0.065 с, и `physics_wedge` исчез полностью. **Заявляемая метрика вехи: 31/33 (два совпавших прогона). Прежние 32–33/33 сняты сломанной линейкой и негодны.** |
| 2026-07-13 | 688346f | **Census ×2 на ФИНАЛЬНОМ механизме и починенной линейке** — первый прогон, где обе оси верны: `run_matrix.sh 0 3`, `world=cell_diverter.sdf` (теперь дефолт и печатается в итоговой строке), seed=0, каждая ячейка печатает ОБА вердикта | WSL ozon (Fortress 6.18, Humble), 33×2, отдельный worktree на зафиксированном коммите, логи `runs/census_diverter_1`, `runs/census_diverter_2` | **routing 33/33 и 32/33 ⇒ диапазон вехи 32–33/33. Классификация — 0 ошибок в обоих прогонах.** **Фикс линейки доказан на полном прогоне:** на ТОЙ ЖЕ физике старая origin-линейка даёт 32/33 и **29/33** против 33/33 и 32/33 по телу — расходятся ровно **три ячейки, и все три — Пуфик**. Пример `pouf oi=0`: origin z=0.273 (старый гейт z<0.25 → FAIL), нижняя точка ТЕЛА **z=0.000**, центр в следе зоны C — товар лежит на полу лотка, а линейка валила его как недоставленный. Ровно то, что было предсказано замером меша (origin у pouf oi=1 на 268 мм выше точки контакта). Единственный отказ — **helmet oi=0 `false_divert`** во втором прогоне: скатился с ленты на x=1.33, **y=+1.27**, ещё ДО механизмов. Это историческая «качалка» (шлем-оболочка детерминированно укатывается в +y, День 2), обе линейки согласны ⇒ реальный стохастический отказ исполнения, а не артефакт скоринга. В прогоне 1 не выпал. **Разбор «helmet останавливается на ленте на x≈3.5», из-за которого и вскрылась подмена мира: у ленты ТОЛКАТЕЛЯ ход 6.4 м против 20 м у шибера (фикс Дня 10 внесли только в мир шибера) — лента встаёт на середине эпизода, и B-товар замирает. Старая полоса B начиналась с x>=3.5, то есть засчитывала PASS РОВНО в точке, где лента умирала: дефект жил под линейкой, которая его маскировала.** 187 pytest зелёные, ruff чист |
| 2026-07-13 | db5368e | **Census ×2 после фикса ПОРЯДКА ПОДАЧИ** (товар кладётся на уже идущую ленту, а не лента трогается под ним): `run_matrix.sh 0 3`, `world=cell_diverter.sdf`, seed=0 | WSL ozon, 33×2, отдельный worktree на зафиксированном коммите, логи `runs/census_feed_1`, `runs/census_feed_2` | **routing 33/33 и 33/33 — ДВА ПОЛНОСТЬЮ ЧИСТЫХ прогона подряд. Классификация 0, исполнение 0. Между прогонами не разошлась ни одна ячейка.** Закрыт последний отказ — **helmet oi=0**, и он оказался дефектом СТЕНДА, а не механизма: `run_skeleton` спавнил товар и ПОТОМ трогал ленту под ним. Шлем стоит на макушке своей выпуклой оболочки (пятно контакта **62×64 мм** при центре масс **175 мм**) ⇒ опрокидывается при 10°, и лента, тронувшаяся под ним, его опрокидывает. Замер по ТЕЛУ (с кватернионом; origin здесь врёт на треть метра): старый порядок — шлем КУВЫРКАЕТСЯ всю ленту (своя ось 0°→67°→125°), боковое положение блуждает случайно, **4 падения из 19**; новый — **8/8, y=0.0109 м каждый раз** (разброс в 5-м знаке). Ни одна лопасть не срабатывала ⇒ не misroute. `run_stream.sh` всегда подавал на ходу — поэтому поток дефекта не видел, а одиночный раннер видел; историческая «качалка-Шлем» (День 2, рельсы подачи) — следствие того же артефакта. **Цена линейки видна и здесь:** на той же физике старая origin-линейка дала бы **31/33 и 32/33** вместо 33/33 — расходятся 2 ячейки, обе Пуфик (лежит на полу лотка, но origin выше гейта z<0.25). **Заявляемая метрика вехи: routing 33/33, два совпавших прогона на финальном механизме, починенной линейке и физичном порядке подачи.** 189 pytest зелёные, ruff чист |
| 2026-07-14 | e59d5f9 | **ТРЕТИЙ независимый полный census перед фиксацией вехи** — страховка: оба чистых прогона 13.07 были сняты сразу ПОСЛЕ большого фикса измерительного инструмента, третий снят на HEAD после аудита класса origin-бага (decisions.md 2026-07-14) | `run_matrix.sh 0 3`, `WORLD=sim/worlds/cell_diverter.sdf` (напечатан в сводке), WSL ozon | **33/33, все 11 товаров 3/3.** Три полностью чистых census подряд на финальном мире и body-scored линейке — 33/33 фиксируется как метрика вехи. Логи: `runs/matrix_20260714_065253_seed0` |
| 2026-07-14 | рабочее дерево после e59d5f9 | **compare_mechanisms box_400→C + plate→D с границей вращения** — проверка числом, что множители бережности не артефакт точки измерения (трасса = origin модели, не COM) | WSL ozon, `CAPTURE_DYNAMICS=1`, трассы сохранены в `runs/compare_20260714_072732`, r из `<inertial><pose>` (box_400 0.200 м, plate 0.105 м) | **4/4 PASS.** Толкатель: box_400 peak_accel=318.5 rot_bound=142.7 impulse=17.2, plate 172.4/126.4/1.22; шибер: box_400 107.2/27.5/7.74, plate 68.2/22.7/0.75. Диапазоны COM: короб [176;461] против [80;135] — не пересекаются, **шибер ≥1.3× бережнее в худшем случае (3.0× точечно, 2.2× по импульсу)**; тарелка [46;299] против [46;91] — пересекаются, по ускорению не разрешимо → множитель по тарелке в отчёте отозван. \|ω\| в пике: толкатель 10.6/15.3 рад/с против шибера 3.3/0.5 — закрутка сообщена ударом |
| 2026-07-14 | рабочее дерево (ветка `int/throughput-metrics`) | **Steady-state такт и throughput потока**: `scripts/measure_throughput.py` по пяти успешно завершённым 3-товарным прогонам (box_400→C, Ручка→C, Бутылка→D): seeds 0/2/3/4 + отдельный validation, `ORIENT_INDEX=0`; отдельно разобрана аварийная попытка seed1; добавлен tee сводки arrivals в `stream.log`, разбор `skeleton.log`+`stream.log` | WSL ozon, шесть попыток: `runs/stream_thr3_seed{0,1,2,3,4}` + `stream_validate_122633_seed0`; 5 timed | **throughput медиана 18 шт/мин (p95-медленный такт 11 шт/мин), 5 валидных прогонов / 6 попыток.** Такт между прибытиями (по ТЕЛУ, не origin): та же зона (nose-to-tail C→C) медиана **1.6 с** / p95 1.78 с; смена зоны C→D медиана **5.5 с** / p95 5.58 с (n=10 зазоров). Задержки (n=15 товаров): решение→команда медиана **1.90 с** / p95 2.27 с (обе метки от контроллера — один процесс, без cross-node джиттера), камера→команда 1.91 с / 2.27 с. **Камера→решение ≈0.01 с — один кадр камеры, НИЖЕ cross-node джиттера эмиссии логов ~0.2 с (три ноды — разные процессы), отдельно не измеряется; см. methodology §2.7.** Расчёт↔сим: мин. feed-зазор против cross-fire (HOLD 2.5 + RETRACT 0.6)/1 м/с = **3.1 с**; наблюдаемый такт смены зоны 5.5 с ≥ пола (соблюдён), +2.4 с — D-лоток ниже по потоку, чем C (дифф. транзит + осадка), не feed-ограничение. **Seed1 в числа НЕ вошёл и их не исказил:** 0 arrivals/0 стадийных цепочек; perception упал в экспериментальном `_split_touching` с `QhullError`, затем `FEED JAM → E-STOP`, а box_400 без команды доехал до x=8.018. Это не «класс helmet-oi0»: поток подаёт на уже идущую ленту, старый census-механизм неприменим. Пяти-товарная попытка дала 1/5 и не включена в throughput; её лог не содержит FEED JAM/E-STOP, тогда как smoke `cv/touching-masks-split` с плотной парой записан как FEED JAM→E-STOP. Наблюдения связаны в methodology §2.7 без вывода об общем корне; потолок >3 остаётся открытым. `test_measure_throughput.py` 7 кейсов на синтетике, ruff чист |
| 2026-07-14 | 6060961 | **Census с ЧИСТОГО КЛОНА как приёмка чистого checkout** (не пересъёмка метрики, а проверка, что README+скрипты воспроизводят её у нового участника): свежий `git clone` с ремоута, шаги строго по README (`colcon build --packages-select ros_msgs` → `build_item_models.py` → `check_sdf.sh`), затем `run_matrix.sh 0 3` | WSL `ozon`, ROS 2 Humble, Gazebo Fortress, `WORLD=sim/worlds/cell_diverter.sdf` (напечатан в сводке), seed=0, N=3, 33 ячейки, клон `/mnt/d/vano/ozon-verify`, логи `runs/matrix_20260714_204450_seed0` | **routing 33/33, 0 FAIL/TIMEOUT, все 11 товаров 3/3** — базлайн трёх прошлых чистых census воспроизведён с нуля незнакомым checkout'ом. Отдельно на том же клоне: Docker-раздел README целиком (colcon → build_item_models → check_sdf 13/13 → `run_skeleton box_300x200x200 B` PASS обеими линейками → `run_stream` `pusher_d FIRED`), WSL-раздел целиком (+`smoke_belt`, `smoke_multi_item` PASS), `pytest -q` **216 passed / 21 skipped** из PowerShell на Windows+WSL, ruff чист. **Ограничение честности:** прогон снят ПОСЛЕ того, как первая попытка шла параллельно с контейнером `census` на `network_mode: host` (общий ign-transport) — тот `smoke_belt` упал, на тихом хосте проходит; хост под этим census был единственным потребителем Gazebo, конкурентов нет (`docker ps` пуст, `pgrep ign` = 0) |
| 2026-07-14 | a470aa7 | **Pose-sweep правила классификации по УСТОЙЧИВЫМ позам** (`scripts/pose_sweep.py`): каждая грань выпуклой оболочки, на которой товар может покоиться (центр масс проецируется внутрь грани), x 8 случайных yaw, замер мешевым рендером `render_depth` (доменный разрыв с Gazebo измерен ранее: 3 мм / 0.01 K) | Windows, seed=0, 11 товаров, 16-64 поз на товар, вес позы = доля площади грани | **9/11 товаров — 100% по всем укладкам.** Ломаются двое, оба реальные: **Бутылка** — лёжа (69% веса укладок) мерится 305x90x90, K=0.00 -> **B при эталоне D**; D даёт только стоячая поза. Census (3 ориентации) лежачую позу не пробовал — это слепое пятно вехи 33/33, а не её опровержение. **Шлем** — 2% укладок дают 355x350x293 -> C (предел 320 превышен на 30 мм). Заявленные риски вехи теперь ИЗМЕРЕНЫ: K Шлема действительно касается порога 0.8 (запас 0.00), но категорию не сломал ни разу; Мешок держит запас 0.02 по K, Цилиндр — 12 мм до предела габарита, все трое 100%. Оговорки: развёртка НЕ моделирует доукладку на ленте (неустойчивую позу Gazebo опрокинет до камеры) — это худший случай «если товар так предстанет», не частота census; веса — доли площади, не вероятности. Равномерно случайные ротации отвергнуты и закреплены тестом: они дают фантомные отказы (box_300 16/40, Бутылка 6/40) на позах, которых физика не держит |
| 2026-07-14 | (этот коммит) | **Переспрос главной находки pose-sweep НА РЕАЛЬНОМ КАДРЕ: вывод про Бутылку ОТОЗВАН, рендер оправдан, валидация расширена на путь K-сечения.** `dump_item_frame.sh bottle` с ориентацией 90° о Y (лёжа), замер реального кадра и рендера в ОДНОЙ позе покоя | WSL Gazebo, мир cell.sdf, кадр заморожен в `tests/fixtures/frames/bottle_side` | **Утверждение «census лежачую Бутылку не пробовал — слепое пятно» НЕВЕРНО:** census oi=1/2 мерил её лёжа (299×103×93) и дал **K=1.00 → D** через K вертикального сечения (perception.py: K = max(силуэт, сечение) — контур это умеет с Дня 4). Расхождение развёртки с census — не доменный разрыв рендера: в одинаковой позе покоя реальный кадр и рендер совпадают (разрыв ≤5.3 мм, dK=0.00, оба → D) — **путь K-сечения теперь ИЗМЕРЕН и закреплён пятой ячейкой валидации** (прежние 4 ячейки решали K силуэтом, лицензия рендера молча распространялась на оба пути). Корень: ИДЕАЛЬНО горизонтальная поза (69% веса граней у развёртки) в Gazebo не реализуется — hull-коллизия подпирает бутылку конусом у горлышка, три независимых оседания легли с наклоном ~8.5° (tau=2.22, полка гейта). На идеальной горизонтали tau=1.89 — на кромке гейта из-за систематики биннинга гребня (max бина приписывается его центру ⇒ r_fit завышен на ~2.6 мм): известное ограничение, production не трогаем (census 33/33 заморожен, K-запас Мешка 0.02). Урок = сам pose_sweep: статически устойчивая грань оболочки ≠ поза, в которую физика оседает; промах развёртки — наводка для проверки реальным оседанием, а не приговор (вписано в докстринги). Риск Шлема (2% укладок → C) остаётся как worst case |
| 2026-07-14 | (этот коммит) | **Разбор последнего риска pose-sweep — Шлем 2% укладок → C: риск ПОДТВЕРЖДЁН кадром, но сквозной прогон переквалифицировал его из классификации в ИСПОЛНЕНИЕ.** (1) `dump_item_frame.sh` в ломающей позе (наклонная грань оболочки, кватернион из развёртки); (2) `run_skeleton.sh helmet B` ×2 в мире шибера с той же позой | WSL Gazebo; кадр заморожен шестой ячейкой валидации `helmet_tilt`; логи прогонов разовые | Истинные габариты Шлема **352×298×282** (запас средней оси 22 мм), но на наклонной грани покоя (вес ~2.3%) проекция сверху — **373×336×291**: обе оси за пределом 320, B-товар мерится как C. Реальный кадр и рендер согласны (разрыв 6 мм / dK=0.03, оба → C) — ячейка `helmet_tilt` закрепляет и позу, и лицензию рендера на ней. НО сквозной прогон ×2: поза не доживает до механизмов — на ДВИЖУЩЕЙСЯ ленте шлем раскачивается и падает с неё на пол у **x=2.098, y=−0.567** (финальная поза идентична в обоих прогонах до 6-го знака — отказ детерминированный), классификатор по кувыркающимся кадрам даёт B (верно), но товар не доезжает ни в один лоток. Итог: остаточный риск Шлема — не «B уедет в C», а «B упадёт с ленты до механизмов» (~2% подач), та же семья, что историческая «качалка» и «bag oi=1 сползает вбок». Оговорка: вес 2.3% — доля площади граней, не частота подачи; в двух census (66 ячеек) поза не выпадала |
| 2026-07-14 | ветка `sim/verdict-scores-the-body@a128743` (в main — 37a33c7+ace8a59) | **Ре-census ×2 после закрытия риска наклонного Шлема** (прозрачные ограждения камерной секции + телесные габариты 3D-OBB): `run_matrix.sh 0 3`, мир печатается в итоге. Строка восстановлена: при приёмке ветки пересадкой она не переносилась | WSL, отдельный worktree на измеряемом коммите, логи `runs/census_bodydims_1`, `runs/census_bodydims_2` | **routing 33/33 и 33/33 — два полностью чистых прогона, классификация 0, ни одна ячейка не разошлась.** Фиксы не сдвинули ни одну из 66 ячеек относительно вехи 33/33 (`census_feed`) — а закрытие целевого риска доказано ПРИЦЕЛЬНО, потому что seed-0 census наклонную грань не вытягивает: наклонный Шлем сквозно **B PASS ×2** (x=7.35/7.10) против детерминированного падения с ленты на x=2.10 до фикса и увода B→C с одними ограждениями (позже подтверждено приёмкой на main: 3/3 B PASS). Полная дуга риска: развёртка (наводка, 2.3% веса укладок) → реальный кадр (373×336 при истинных 352×298×282, обе оси > 320) → сквозной прогон (падение) → ограждения (доезжает, но C) → телесная OBB (B). Оговорки: мерился коммит ветки ДО touching-masks; слитое состояние main валидировано приёмкой (пограничная матрица, multi-item smoke) и подлежит своему census перед следующей вехой; вес позы 2.3% — доля площади граней оболочки, не частота подачи |
| 2026-07-15 | `main@59d8fd4` | **Census ×2 СЛИТОГО main** — первый полный census состояния, где сошлись touching-masks, прозрачные ограждения, телесная 3D-OBB и unicode-фикс overlay (приёмка pose-ветки проверяла его только пограничной матрицей 5×3 и multi-item smoke): `run_matrix.sh 0 3`, мир печатается | WSL, отдельный worktree на 59d8fd4, логи `runs/census_merged_1`, `runs/census_merged_2` | **routing 33/33 и 33/33 — два полностью чистых прогона, классификация 0, ни одна ячейка не разошлась.** Слитое состояние подтверждено той же метрикой, что и вехи по отдельности; телесная OBB видна в бою (helmet oi=0: 333×280×279 против ~356×350 у прежней проекции). Артефакт «no measurement in log» — 5 ячеек из 33 в ОБОИХ прогонах, ровно как в census ветки: известное обрезание stdout нод при kill launch, не регрессия. Оговорка: первый запуск цепочки умер молча в первые минуты (лог 0 байт, процессов нет) — причина не установлена, повторный запуск той же командой прожил оба прогона; на будущее у длинной цепочки теперь сторож живости, потому что монитор «только отказов» неотличим от мёртвого прогона |
| 2026-07-15 | (этот коммит) | **Validation-набор расширен последним открытым CV-риском — тонкие/частично видимые:** +4 реальных среза (`dump_item_frame.sh`, финальный мир, кроме оговорённого) | WSL Gazebo; кадры в `docs/report/img/validation_{pen_diag,pen_standing,partial_helmet,partial_pen}`, воспроизводство — в validation.md | **visible recall 8/8, category 8/8, expected reject 4/4.** Находки замера: (1) дефолтная поза STL Ручки — ВЕРТИКАЛЬНАЯ, поэтому «yaw-развороты» её не укладывают — для лежачих поз нужен pitch 90°; (2) документированный предел «стоячая Ручка = 12 px < гейта 24» в ФИНАЛЬНОМ мире нереализуем: анизотропная лента шибера (mu2=0.2) роняет её даже неподвижной, и она классифицируется C лёжа (149×14×8) — предел заморожен кадром из cell.sdf как замок на случай смены гейта; (3) диагональная тонкая маска (45°, ~180 px) мерится 150×14×10 при истине 148×13×9 — погрешность ≤2 мм; (4) частичная Ручка у кромки даёт 31 px фона — ВЫШЕ speck-гейта, отклонение делает правило границы, а не фильтр размера; частичный Шлем (1833 px на кромке) отклоняется тоже. Итоговая строка скрипта переименована: «partial reject» → «expected reject» (стоячая Ручка — не частичная видимость). Попутно: /tmp WSL стирается при простое дистрибутива — этим же объяснилась молчаливая смерть первого census-запуска (см. память wsl-idle-stop-kills-detached) |
| 2026-07-15 | рабочее дерево после `main@c36f683` | **Повторный офлайн-аудит сохранённых stream-артефактов после фикса знаменателя и рекурсивного discovery:** `python scripts/measure_throughput.py runs` | Windows host, существующие локальные каталоги `runs/`; новый Gazebo-прогон не выполнялся | Найдено **10 структурно завершённых эпизодов** вместо прежних 8: all-pass **6/10 (60%)**, routed **27/36 (75%)**, terminal FAIL 9, incomplete 0; маршруты: bottle→D 8/10, box→C 9/10, detergent→B 2/3, lunchbox→B 0/3, pen→C 8/10. Из latency исключён несвязанный skeleton-only E-stop probe, вложенные `dense_compare/main_valid{1,2}` включены ровно по одному разу. Это аудит смешанных исторических попыток, а не контролируемый soak и не новая оценка физической надёжности; корректность typed takt через FAIL и pairing latency проверяется следующими отдельными задачами |
| 2026-07-15 | рабочее дерево после `main@91ef1ee` | **Контрольный офлайн-перерасчёт после разделения output-такта и feed-adjacent типизации:** `python scripts/measure_throughput.py runs` | Windows host, те же 10 существующих локальных эпизодов; новый Gazebo-прогон не выполнялся | Throughput сохранил все **18** положительных хронологических PASS-интервалов: median **3.90 с / 15 шт/мин**, p95-медленный **14.745 с / 4 шт/мин**, 8 timed runs. Из 18 только **16** имеют честный маршрутный тип: 8 floor-0 (median 1.50 с) + 8 zone-change (median 5.55 с); две пары через terminal FAIL больше не склеиваются. Неразрешимых nonpositive интервалов в этих артефактах 0; синтетический equal-timestamp случай закреплён и не делит на ноль. Надёжность не изменилась (6/10, 27/36). Ограничение то же: это аудит смешанной истории, не контролируемый soak; latency pairing ещё проверяется отдельно |
| 2026-07-15 | рабочее дерево после `main@2fe1fc7` | **A/B офлайн-перерасчёт правила pairing `route commit → FIRED` на тех же 10 каталогах:** кандидат «последний совместимый commit» против «первый commit непрерывной route-эпохи» | Windows host, существующие `runs/`, синтетические B→C/C↔D replans; новый Gazebo-прогон не выполнялся | Кандидат «последний» дал **n=26, median 0.101 с, p95 1.716 с** и был ОТВЕРГНУТ чтением raw-логов: одна зона печатает countdown каждый кадр, последний лог — не момент решения. Route-epoch правило вернуло **n=26, median 1.909 с, p95 4.821 с**, camera→command осталось n=26 / 1.924 / 4.821 с; это совпадает с прежним baseline, потому что сохранённая когорта не содержит replan, влияющего на FIRED-пары. Ценность фикса — будущая корректность: отменённый B/C/D больше не может стать commit другой зоны, а несовместимый FIRED исключается из latency вместо ложного числа |
| 2026-07-15 | рабочее дерево после `main@0798093` | **Backward-compatibility прогон нового matrix termination ledger на последнем сохранённом полном census:** `python scripts/triage_matrix.py --logdir runs/matrix_20260714_204450_seed0`; отдельно synthetic rc truth-table | Windows host, legacy-каталог без новых `.status`; новый Gazebo-прогон не выполнялся | Legacy terminal verdicts сохранены без пересчёта: **routing 33/33, classification 0, execution 0**. Синтетика различает rc124→TIMEOUT/physics_wedge, rc7/137 без terminal→RUNNER_ERROR, PASS+rc1 и прочие contradictions→HARNESS_ERROR, malformed status→HARNESS_ERROR, старый незавершённый лог без status→UNKNOWN_TERMINATION. Ограничение намеренное: старые оборванные артефакты нельзя ретроспективно назвать timeout без сохранённого rc; новые matrix-запуски пишут его атомарно |
| 2026-07-15 | рабочее дерево после `main@538103b` | **Повтор pose-sweep после исправления population bias:** все 11 STL ×1 yaw; затем четыре проблемных товара ×8 yaw (`seed=0`) | Windows host, production `render_depth→measure_item`, stable supports из полного convex hull; новый Gazebo-прогон не выполнялся | Инструментальный census: икосаэдр **0/20→20/20 rests**; реальный набор после cap — 114 rests (старый выбор 61, uncapped 593), сумма весов каждого товара 1. Sweep ×1 занял 62.2 с и дал 100% у 7/11; лиды: **Bottle D 81.2%, Box400 C 70.9%, Pouf C 68.8%, Helmet B 93.8%**. Переспрос этих четырёх ×8 yaw (432 кадра, 143 с) дал те же доли и симптомы: Bottle 305×90×90 K=0→B; Box400 часто 401×303×169…280→B; Pouf часто скрыто ужимается ниже габаритного порога→B; Helmet одна support-страта 356…359×329×261→C. Это ОТМЕНЯЕТ актуальность headline «9/11 по всем укладкам» для текущего sampler/production-кода, но НЕ является Gazebo-частотой или основанием менять пороги: Bottle-horizontal уже известен как нереализуемый settle, Helmet требует real-frame переспроса, новые Box/Pouf лиды указывают на возможный underfit 3D body-OBB и идут в отдельную диагностику |
| 2026-07-15 | рабочее дерево после `main@2d6c4c9` | **Дифференциальная диагностика Box400/Pouf-лидов и A/B touching-discriminator:** exact failing quaternions, Box ×8 yaw, 45 unequal/offset rectangle contacts, полный 11-STL sweep ×1, frozen validation | Windows host, deterministic render seed 0; новый Gazebo-прогон не выполнялся | Гипотеза «3D-OBB режет скрытое тело» ОПРОВЕРГНУТА: split одного товара создавал Box 3/2 и Pouf 10 sub-masks, после чего single-item API мерил крупнейший фрагмент. Unsplit восстанавливает Box `402×401×303` и Pouf `482×476×256`, обе C; Box relief/dz≈0.475 оставляет body-OBB выключенным. Защита `n==2` + compactness≥0.20 дала Box **48/48 C** на 8 yaw (было 41/48); stress сохранил все **24/24** различимых touching-пары против 12/24 у piece-solidity. Итоговый sweep ×1 за 68.3 с: **Box400 100%, Pouf 100%**, Bottle 81.2% и Helmet 93.8% без изменений; frozen real validation: visible/category **8/8**, expected reject **4/4**. Ограничение: synthetic touching проверяет маски, штатный физический smoke имел зазор; тонкое реальное касание может консервативно остаться одним ID |
| 2026-07-15 | рабочее дерево после `main@6f3c3d2` | **Backward replay `census_ruler_diff.py` после подключения canonical termination parser:** `matrix_20260714_065253_seed0` против `matrix_20260714_204450_seed0`; отдельно synthetic status truth-table | Windows host, два существующих legacy-census без `.status`; новый Gazebo-прогон не выполнялся | Исторические terminal-факты неизменны: body **33/33 и 33/33**, legacy 31/33 и 32/33; линейка объясняет Pouf0/Pouf1 (**2**), физический PASS↔FAIL drift **0**. Synthetic sidecars теперь различают rc124→TIMEOUT, rc7/137→RUNNER_ERROR, PASS+rc1→HARNESS_ERROR, legacy без terminal→UNKNOWN_TERMINATION; ни infra-исход, ни отсутствующая ячейка не увеличивают `MOVED BY PHYSICS`, настоящий PASS↔FAIL увеличивает |
| 2026-07-15 | рабочее дерево после `main@570fa9d` | **Пересчёт frozen validation с dimension budgets:** `python scripts/measure_validation.py`; отдельно same-category gross-error probe | Windows host, 8 сохранённых real-Gazebo visible depth + 4 expected rejects; новый Gazebo-прогон не выполнялся | До фикса B против B проходил даже при `max|Δdim|=260 мм`; теперь такой кейс FAIL. Реальные ошибки восьми кадров: median **10.97 мм**, p95 **20.66 мм**, max **22.07 мм**; явные лимиты 3/5/20/25 мм сохраняют visible recall **8/8**, category **8/8**, новый visible contract **8/8**, expected reject **4/4**. K выводится, но не гейтится из-за несопоставимости top-view и best-section для Ручки (|ΔK| до 0.93) |
| 2026-07-15 | рабочее дерево после `main@c5c98bd` | **Windows Unicode A/B записи depth PNG из реального `render_depth.main()`:** ASCII, `данные/глубина.png`, missing parent; по 3 повтора encode-path | Windows host, mocked только тяжёлый mesh render; настоящий OpenCV uint16 PNG codec и filesystem | Старый `cv2.imwrite`: ASCII 3/3, Unicode **0/3**, missing parent 0/3, но CLI во всех неуспехах печатал `wrote`. `cv2.imencode + Path.write_bytes`: Unicode **3/3**, decoded pixels побитно совпали; missing parent теперь nonzero `FileNotFoundError`, ложного `wrote` нет. Это проверка artifact I/O, не perception/Gazebo |
| 2026-07-15 | рабочее дерево после `main@9bc1433` | **Unicode round-trip legacy single overlay после унификации writer:** `save_overlay` на synthetic measured box в `данные/оверлей.png`; регрессия existing multi-item overlay | Windows host, настоящий OpenCV PNG codec; synthetic depth geometry | До фикса single API возвращал dims, но Unicode-файла не было. После общего `_write_png` single PNG существует и декодируется как 480×640×3; single Unicode + multi-item pixel overlay + Unicode depth loader **3/3**. Физика/измерительные числа не менялись |
| 2026-07-15 | рабочее дерево после `main@f961fa3` | **A/B multi-item perception hot path:** 2 и 3 раздельных товара 640×480, touching fixtures; счётчик `_find_items` и wall-time | Windows host, synthetic deterministic depth, одинаковая production-геометрия до/после | До фикса N товаров запускали **N+1** полных сегментаций и N копий кадра; 2 товара 44.1 мс, 3 товара 54.3 мс. Прямая передача найденной маски даёт всегда **1** сегментацию: 25.3 мс (-43%) и 33.8 мс (-38%); touching экономит около 19 мс. Все Measurement на проверенных кадрах совпали, целевые perception/touching 58/58. Это host-производительность, не ROS/Gazebo latency |
| 2026-07-15 | (этот коммит) | **Плечо камера→решение теперь измеряется НАПРЯМУЮ (Раздел 5.2, синхронизация)** — контроллер логирует `(cam->decision Xs)` = `now()` в момент коммита маршрута минус стамп кадра камеры из хедера сообщения, обе величины в sim-time из общего `/clock`; throughput-скрипт читает токен дословно | WSL Gazebo, батарея seeds 0/2/3/4, дефолтный 3-товарный поток, логи `runs/stream_lat_seed*` | **routing 12/12, камера→решение медиана 0.094 с, p95 0.186 с (n=12).** До этого плечо считалось неизмеримым: throughput-скрипт вычитал wall-время эмиссии логов ДВУХ процессов (перцепция и контроллер), а между ними ~0.2 с джиттера планировщика — сегмент уходил даже в минус, и старый гейт `decide ≥ detect` молча выбрасывал инвертированные товары. Корень был не в измерении, а в том, ЧТО вычиталось: стамп кадра уже ехал в хедере, контроллер уже считал `age_s` как sanity-guard — оставалось залогировать его. Находка от прямого измерения: латентность ~0.2 с на ПЕРВОМ коммите (агрегация под soft-start), созревает до ~0.09 с — прежняя косвенная оценка «≈ один кадр, ~0.05 с» (1.91−1.90=0.01 с) была шумом двух cross-node разностей, а не сигналом (Карпаты №1: прямое измерение бьёт догадку). Токен встроен в эпоховую логику коммитов (влита из main): берётся со строки старта финальной совместимой эпохи. Тест парсинга + обратная совместимость со старыми логами без токена; полный набор зелёный |
| 2026-07-17 | `main@0324a10` | **Первый сырой футаж сквозного прогона (P1 видео, «первое дело» недели 5):** `scripts/record_skeleton_video.sh bottle D` — спектатор-камера в мир, кадры через ros_gz_bridge в mp4 | WSL Ubuntu-22.04 Gazebo Fortress под llvmpipe (`LIBGL_ALWAYS_SOFTWARE=1`), `RUN_SKELETON_POLL_ITERS=300` | Конвейер записи РАБОТАЕТ end-to-end: **254 кадра → mp4 980K + постер 55K**, вердикт `bottle → D: PASS` (body-scored), classifier `D k=1.000 conf=0.98`. Опасение «cv2.VideoWriter mp4v тихо не откроется без ffmpeg-CLI» НЕ подтвердилось — libav из opencv-wheel пишет mp4 сам (ffmpeg-CLI в WSL отсутствует). Вскрытая дыра для P1: ракурс спектатор-камеры непрезентабелен — половина кадра пустой серый пол, товар в кадре не вычленить, нет оверлея (товар/категория/зона). mp4 гитигнорен, не коммитится (GIT.md). Это проверка конвейера записи, не новая физика/perception |
| 2026-07-17 | рабочее дерево после `main@3429959` | **Hero-ракурс спектатор-камеры (P1 видео):** новый `scripts/frame_spectator.sh` спавнит мир+камеру и снимает ОДИН кадр без прогона эпизода; 6 поз-кандидатов оценены глазами против геометрии `cell.sdf` | WSL Ubuntu-22.04 Gazebo Fortress под llvmpipe | Прежняя поза `1.2 -3.6 2.9 …` давала полкадра пустого пола, действие вбок. Выбран **b3 `-1.0 -3.0 3.0 0 0.545 0.753`** — приподнятый 3/4 от угла подача-D вниз по линии: лента ведёт взгляд подача→камера-станция→диверторы→все 3 зоны, робастно для любого товара. Полный прогон `record_skeleton_video.sh bottle D` на новой позе: **244 кадра, bottle→D PASS**, бутылка и дивёрт в D читаются (кадры из mp4 через cv2). Задокументирована tighter «action»-поза `0.8 -2.6 2.2 0 0.544 0.880` для D-крупняка (клипит зону C, потому не дефолт). Это framing-инструмент и композиция, не физика/perception |
| 2026-07-17 | `main@8a728f9` | **Честный sustained-замер throughput на ПОЛНОЙ матрице (P3/P5, поток #1):** `run_stream_suite.sh 0 0 2` (все 11 моделей, 6 эпизодов) → `measure_throughput.py` | WSL Ubuntu-22.04 Gazebo Fortress под llvmpipe, чистый стенд (зомби-сервер cell.sdf от съёмки камеры сначала контаминировал spawn — устранён, прогон перезапущен) | **6/6 all-pass, 33/33 routed, 0 FAIL.** Steady-state **медиана 10 шт/мин** (p95-медленный такт 4), n=27 интервалов; поэпизодно 15/15/15/4/6/10 — микс-5 держит 15, плотный 6-товарный same-zone B деградирует до 4–6 (насыщение хода ленты). Камера→решение медиана **0.030 с** (созрело лучше 3-товарных 0.094). Feed-floor смены зоны: расчёт 3.10 с ↔ принято ровно 3.10 с (margin −0.00). **Это опровергает headline «18 шт/мин» как sustained:** 18 — благоприятный короткий 3-товарный поток; честный sustained all-model — **~10 шт/мин**. Заменяет прежний недоказанный «3–12» воспроизводимой медианой. Основа для calc↔sim якорь 5 и переписи заявки вниз |
| 2026-07-17 | `main@e768745` | **Hero-футаж финального механизма (P1, поток #5):** новый `scripts/record_stream_video.sh` (обёртка run_stream по паттерну record_skeleton) + пересъёмка на `cell_diverter.sdf` (финальный шибер, не baseline-толкатель) | WSL Ubuntu-22.04 Gazebo Fortress под llvmpipe, hero-поза b3 | Три клипа: `hero_diverter_bottle_D.mp4` (262 кадра, D), `hero_diverter_box400_C.mp4` (крупный короб, C), **`hero_stream_mixed_cd.mp4` — 5/5 PASS непрерывная сортировка** (bottle→D, plate→D, box→C, pouf→C, pen→C; money shot под §5 throughput). Все mp4 гитигнорены, сырьё для монтажа человеком. Ракурс b3 хорошо ложится на шибер (наклонные лопасти-стенки читаются). Это framing/сырьё, не физика |
| 2026-07-17 | `main@c8ce41a` | **Проверка чистого развёртывания на текущем HEAD (P6, поток #6):** `scripts/check_clean_deploy.sh` — git archive HEAD → пиннутый образ с нуля → headless CPU `ci_e2e.sh` в контейнере | WSL Ubuntu-22.04 + docker, база `osrf/ros:humble-desktop@sha256` тянется с нуля | **PASS:** образ собран из чистого checkout, colcon ros_msgs ок, e2e поднял контур (perception `329×200×198 K=0.52 → B`). Первый прогон упал ложно («controller did not come up») из-за остаточного host-`ign gazebo` от съёмки: контейнер идёт `--network host`, зомби-сервер коллизил ROS-граф; после kill — зелено. Развёртывание на ЧУЖОЙ машине остаётся за человеком (ledger H5) |
| 2026-07-19 | `main@0dfe501` | **Сверка правила K с ответами жюри (`scripts/compare_k_rules.py`, 11 моделей docs/Stl, оценщик `_roundness_k` для обеих метрик):** проекции XY/XZ/YZ против сечений вдоль осей OBB — **расхождений по категории 0/11**, max ΔK = 0.306 (Ручка: сечение 0.986, проекция 0.680, обе → C по габаритам). Побочно: замена центроидного радиуса вписанной окружности на центр Чебышёва переворачивает Шлем 0.779→0.843 и Мешок 0.716→0.878 (оба B→D, порог 0.8); Цилиндр устойчив 0.738→0.739. Правила не менялись |
| 2026-07-20 | `main@cc14eea` (worktree) | **Перегон переписи на текущем main — закрытие открытого гейта (session-next.md):** `LOGDIR=runs/baseline_main_seed0 bash scripts/run_matrix.sh 0 3`, world=cell_diverter.sdf (дивёртер, не пушер) | WSL Ubuntu-22.04 Gazebo Fortress под llvmpipe, seed 0, N=3, 33 ячейки | **routing correctness 33/33** (все 11 товаров ×3 ориентации). Впервые перепись гоняется на ПОТОМКЕ `fa4d30e` («K из общего центра»): Шлем → B 3/3 и Мешок(bag) → B 3/3 держатся под Gazebo — заявленные 33/33 в отчёте теперь стоят на отгруженном ядре, а не на ветке-не-потомке (`git merge-base --is-ancestor fa4d30e HEAD` → истина). Осталось для полного снятия оговорки: мульти-seed 165/165 (seeds 0–4, ~5 ч). Это routing-перепись, не perception-числа |
| 2026-07-20 | `main@c12ba2b` (worktree) | **Мульти-seed перепись на текущем main (seeds 0–4) — снятие второй оговорки session-next:** seed 0 = `runs/baseline_main_seed0`, seeds 1–4 = `run_matrix.sh $s 3`, агрегатор `census_seed_sweep.py` по пяти | WSL Ubuntu-22.04 Gazebo Fortress под llvmpipe, world=cell_diverter.sdf, 165 ячеек | **routing 164/165** (НЕ 165/165). По seed: 33/33/33/33/**32**. Единственный промах — `bag(Мешок) oi=2` на seed 4: перцепция `195×183×159 K=0.80`, классификатор прочитал K≥0.8 → **D** вместо B (**misroute, классификация, не физика**; execution failures 0). Это задокументированная хрупкость Мешка (K=0.8004 на самом пороге 0.8): политика удержания Мешка в B через силуэтный порог не покрывает эту ориентацию. **Вывод для отчёта: заявленное «165/165» правится на 164/165 с честной оговоркой** — одиночный промах это by-design coin-flip у порога, а не отказ механизма. Фикс потребовал бы правки замороженного `perception.py` + перевалидации; за ~10 дней до сдачи цена/выгода не в пользу правки |
| 2026-07-20 | `main@9448b9e` (worktree) | **Тест эквивалентности STL-путь↔камера (`compare_mesh_vs_perception.py`) на 11 моделях:** чистый рендер identity-позы → настоящий `measure_item` → допуск организаторов + согласие категории против OBB-эталона `analyze_models` | Windows host, чистые офлайн-рендеры (без шума кадра), identity-позы | **organizer tolerance 8/11, category agreement 7/11** на identity-позах. Мисматчи категории (Бутылка/Тарелка/Ручка → B) — артефакт ПОЗЫ (в переписи они садятся в стабильные ориентации и маршрутизируются верно), Мешок D→B — документированная политика прода, не ошибка. Позо-инвариантная эквивалентность держится на жёстких телах: Короб300/Короб400/ЛанчБокс/Моющее/Цилиндр — все in-tolerance И совпадение категории (закреплено `tests/test_mesh_vs_perception.py`, 5/5). Точность round/thin-товаров — в переписи (164/165) и `measure_validation` (реальные кадры), НЕ в этой identity-диагностике. Ограничение: чистый рендер без шума измеряет алгоритм, не реальный кадр |
| 2026-07-20 | `main@8bf3bac` (worktree) | **Проверка детерминизма промаха Мешка (seed4 oi2):** перепрогон ТОЙ ЖЕ ориентации 8× через `run_skeleton.sh bag B` (quat из `spawn_orientations.py 4 4 2 bag`) | WSL Ubuntu-22.04 Gazebo Fortress под llvmpipe, 8 прогонов одной ячейки | **B 8/8** (K=0.80, габариты `197×174×166` против `195×183×159` в переписи-провале). Промах в переписи — run-to-run оседание физики на пороге K=0.8004, НЕ детерминированное свойство ориентации. Итог по Мешку: B в **22/23** наблюдённых прогонов (14/15 перепись + 8/8 зонд). Вывод: классификатор устойчив, недетерминирована физика оседания; гнаться за 165/165 перепрогоном = черри-пик монетки, честнее 164/165 с этой оговоркой. Фикс дисперсии (сигнал мягкости `k_spread`, `aggregation.py:97`) — принципиальный путь, но трогает замороженное ядро |
| 2026-07-21 | `estop-trial@4df6c05` (worktree `Ozon-estop`) | **Ре-верификация ядра после вливания safety-пакета Stage 26 (`int/estop-engaged-recovery`):** `LOGDIR=runs/estop_merge_census_seed0 CELL_TIMEOUT=180 bash scripts/run_matrix.sh 0 3`, world=cell_diverter.sdf | WSL Ubuntu-22.04 Gazebo Fortress под llvmpipe, seed 0, N=3, 33 ячейки | **routing correctness 33/33**, triage classification 0 / execution 0, 0 FAIL/TIMEOUT/RUNNER_ERROR. Feedback-gated E-stop, стартовый интерлок и латч по потере углов C/D **не ломают штатный маршрутный контур** — все 11 товаров ×3 ориентации на месте. Хост-регрессия того же состояния: ruff чист, 453 теста. Замечание по среде: свежий worktree требует `colcon build --packages-select ros_msgs` — без него все 33 ячейки дают мгновенный `RUNNER_ERROR: ROS workspace is not built` (раннер падает громко, а не тихо мерит пустоту). Осталось для полного снятия: мульти-seed 0–4 (~5 ч) и resource/latency-gate двух новых feedback-топиков на окружении организаторов | 
| 2026-07-21 | `main@bf891af` (worktree `Ozon-estop`) | **Мульти-seed перепись на ядре С E-stop (seeds 1–4, seed 0 = `estop_merge_census_seed0`)** — вторая половина ре-верификации Stage 26 | WSL Ubuntu-22.04 Gazebo Fortress под llvmpipe, world=cell_diverter.sdf, 165 ячеек | **routing 162/165** (33/32/33/33/31). Три промаха, ВСЕ классификация (execution 0): helmet oi=1 seed1 (K=0.80→D), bag oi=2 seed4 (K=0.80→D, знакомый), helmet oi=1 seed4 (K=0.80→D). **НЕ регресс от E-stop:** safety-пакет трогает `controller_node.py`/`constants.py`, `perception.py` байт-в-байт тот же — классификация K выше контроллера по потоку, а seed 0/2/3 дали чистые 33/33. Все промахи — околопороговые по K товары (Шлем K≈0.78, Мешок K=0.800) у порога 0.8; печать «K=0.80» прячет цифру, решающую порог. **Перепрогон helmet seed1 oi1 ×8 → B 7/8** (`replay_helmet.sh`, тот же метод, что дал Мешок B 8/8): промах недетерминирован, это run-to-run оседание Gazebo, а не свойство позы. Честная заявка — диапазон **162–165/165 run-to-run** (19.07 давал 165 на ядре до фикса K, 20.07 — 164, 21.07 — 162), инвариант: execution 0, все промахи — K-монетка Шлем/Мешок у 0.8 | 
| 2026-07-21 | `main@bf891af` | **Зонд боковой камеры на ПОЗАХ ПОКОЯ переписи** (`probe_side_camera.py --from-census runs/estop_merge_census_seed0`): слияние top-down+бок против одноракурсного, вердикт `within_measurement_tolerance` | Windows host, офлайн-рендеры мешей (400k сэмплов, z-буфер на ракурс), 33 позы покоя из логов переписи `resting rpy` | **допуск организаторов: top-down 14/33 → слитое 30/33.** Починены Бутылка 0/3→3/3, Короб300 1/3→3/3, Короб400 1/3→3/3, ЛанчБокс 0/3→3/3, Моющее 0/3→3/3, Цилиндр 0/3→3/3, Ручка 2/3→3/3, Шлем 2/3→3/3; Тарелка и Пуфик 3/3 без изменений. **Единственный регресс — Мешок 2/3→0/3** (высота 170→155 и 175→131 мм: мягкий ком даёт лишние точки, минимально-объёмная коробка наклоняется). **ВАЖНО не путать метрики:** это точность ИЗМЕРЕНИЯ против допуска 5 мм/10 %, а НЕ маршрутизация — routing на тех же позах 33/33, потому что у порогов B/C/D есть запас. На сеяных кувырках (не позы покоя, есть физически невозможные) было 19/44 → 25/44 — этот набор с переписью НЕ сопоставим. Ограничения: чистый рендер без шума сенсора и **без ошибки взаимной калибровки двух камер** — это геометрическая верхняя граница, не полевая точность | 
| 2026-07-21 | `main@bf891af` | **Попытка довести слитое двухкамерное измерение с 30/33 до 33/33 — два принципиальных варианта OBB** (разведочные скрипты, в репо не оставлены) | Windows host, те же 33 позы покоя переписи, те же слитые облака | **Оба провалились, итог одинаковый 27/33 против 30/33 базового.** (1) *Вертикальный гард* (бокс обязан перекрывать измеренную высоту тела над лентой): Мешок НЕ чинит вовсе — наклонная коробка перекрывает высоту диагональю, поэтому ограничение на нём не связывает; побочно сломал Бутылку oi=1, Моющее oi=2, Шлем oi=1. (2) *Гравитационно-выровненный бокс* (ось строго вертикальна, рыскание свободно): **Мешок чинит 0/3→3/3** — диагноз подтверждён, — но ломает Бутылку ×2, Тарелку ×3, Цилиндр ×1. **Вывод: наклонный min-volume прав для жёстких тел под углом, гравитационный — для мягкого кома; одно правило обслуживает одну семью и ломает другую.** Свести их можно только селектором «мягкий/жёсткий», подогнанным на 33 чистых рендерах — это ровно та подгонка порога внутрь перекрытия классов, которую проект уже отверг в кампании «K-полоса» (19.07) и которая один раз стоила регрессии Бутылки. Не делаем | 
| 2026-07-21 | `main@bf891af` | **Маршрутизация под разным числом камер — прямой замер, закрытие вопроса «камеры»** (`probe_side_camera` слитые габариты → прод `classify()` при фиксированном истинном K, боковой ракурс K не меняет) | Windows host, 33 позы покоя переписи, конфиги top / +1 / +2 / +3 / +4 бока | **Мисроуты от истинной зоны: одна камера 2/33, +1бок 1/33, +2 2/33, +3 2/33, +4 2/33 — число камер маршрутизацию НЕ улучшает.** Одна камера мисроутит Шлема ×2 (B→C, top-down недомеряет купол); вторая чинит Шлема, но ломает Ручку (слияние переоценивает тончайший габарит 9→10–12 мм, переводит через порог 10 мм в B) — плюс/минус в ноль. Третья-четвёртая насыщаются на 2/33. Причина структурная: пороги B/C/D это обрывы, облако неполное (низ не видит никто), лишняя точность у обрыва переставляет, кто падает; глобального OBB-правила на все формы нет. Прод справляется медианой K/габаритов по кадрам прохода, а не точностью одного замера. **Вопрос камер закрыт числом**: не добавляем — маршрутизация (за неё баллы) не растёт, а цена — вскрытие ядра + калибровка + конфликт с шиберами. В защиту — `qa_prep.md` | 
| 2026-07-21 | `main@0b28a93` | **2 камеры против 3 — расширенный замер с моделью ошибки взаимной калибровки** (`scripts/probe_camera_count.py`; слитые габариты → прод `classify()` при фиксированном истинном K) | Windows host, **165 поз покоя** (`runs/sweep_seed0..4`, все пять переписей), 4 конфигурации от ОДНОЙ верхней головки, свип калибровки 0 / 1 мм·0.1° / 2 мм·0.2° / 3 мм·0.3°, по 3 seed'а | **Две камеры выигрывают маршрутизацию на ВСЕХ уровнях калибровки: мисроуты 8.0 (одна) / 0.0 (две) / 3.0 (три встречные) при идеальной, 8.0 / 4.3 / 9.0 при типичной 2 мм·0.2°.** Три головки хуже двух везде, а при типичной калибровке хуже ОДНОЙ (9.0 против 8.0). При этом по ТОЧНОСТИ ИЗМЕРЕНИЯ три лучше двух (95% против 81% в допуске при идеальной, 89% против 81% при типичной) — **метрики расходятся**. Механизм в таблице виновников: одна камера мисроутит helmet ×18 + box300 ×6 (недомер купола), две — pen ×13, три — **pen ×27**. Ручка 148×13×9 стоит минимальным габаритом 9 мм под самым порогом 10 мм; лишние ракурсы дают минимально-объёмной коробке больше свободы, она раздувает тончайший габарит за 10 мм и переводит C→B. То есть лишняя точность в среднем, но у обрыва порога она переставляет категорию. Ограничения: не смоделированы шум сенсора, выпадение точек на тёмной/бликующей упаковке и рассинхрон нетриггерованных кадров (до 66.7 мм при 1 м/с) — все три штрафа тоже растут с числом головок, значит числа для трёх камер ОПТИМИСТИЧНЫ |
| 2026-07-21 | `main@026309a` | **Оценщик против числа камер: union-слияние облаков против СТРУКТУРНОГО (след XY только с верхней головки, высота — максимум по видам)** (разведочный скрипт в scratchpad, в репо не оставлен; те же `CONFIGS`, `misregister`, `visible_points`, прод `classify()` при истинном K) | Windows host, 165 поз покоя (`runs/sweep_seed0..4`), 11 товаров, фиксированная типичная калибровка 2 мм·0.2°, 3 seed'а, 495 прогонов на конфигурацию | **Структурное слияние на ОДНОЙ камере — 1.0/165 мисроута и 80 % в допуске — бьёт union на ДВУХ (2.3/165, 81 %) по маршрутизации и повторяет по точности.** Union: 8.0 / 2.3 / 8.0 / 6.7 мисроута (одна / две / три встречные / три вдоль), допуск 47 / 81 / 95 / 84 %. Структурное: 1.0 / 3.7 / 3.7 / 3.7, допуск ровно 80 % везде. Виновники объясняют механизм: union с одной камерой роняет helmet ×18 + box300 ×6 (недомер купола) — структурное на той же камере оставляет helmet ×3, то есть это дефект ОЦЕНЩИКА, а не обзора; боковые головки в структурной схеме уже ничего не чинят (плато 3.7) и лишь раздувают высоту Ручки (pen ×8). **95 % в допуске остаются достижимы только union с тремя головками** — купол Шлема есть единственная информация, которой у верхней камеры физически нет. Ограничения: (а) структурный оценщик считает след по выпуклой оболочке в мировых XY и через прод `measure_item` НЕ проходит — сравниваются концепции оценщиков, баг медианной глубины `perception.py:671` обойдён, а не починен; (б) схема розыгрыша ошибки калибровки здесь иная (rng пересевается на позу), и union-двойка сместилась с 4.3 на 2.3 против прогона `main@0b28a93` — **порядок конфигураций устойчив, абсолютные величины нет**; (в) шум сенсора, выпадение точек и рассинхрон головок не смоделированы |
| 2026-07-21 | `main@9cd6312` | **База B0 — перепись маршрутизации на ТЕКУЩЕМ ядре** (`scripts/run_matrix.sh 0 3`, мир `cell_diverter.sdf`, `CELL_TIMEOUT=180`, логи `runs/b0_census_9cd6312`) | WSL `ozon`, Gazebo Fortress + ROS 2 Humble, 11 товаров × 3 сеяные ориентации = 33 ячейки, полный перезапуск мира на ячейку | **Маршрутизация 33/33, таймаутов 0, триаж чист (classification 0, execution 0).** Все одиннадцать товаров 3/3: bottle→D, box300→B, box400→C, lunchbox→B, bag→B, detergent→B, pouf→C, pen→C, plate→D, cylinder→B, helmet→B. **Зачем прогон:** перепись не гонялась после фикса K `fa4d30e` (с тех пор 45+ коммитов), и заявленные 33/33 были числом с прогона ДО фикса — то есть формально неподтверждённым. Теперь подтверждено на текущем ядре и служит нулём для обеих камерных веток (`docs/plan-branch-2cameras.md`, `docs/plan-branch-3cameras.md`): обе ответвляются от этой точки, обе сравниваются против B0. Мир напечатан явно в логе — защита от повторения дефекта, когда три переписи молча ушли на баллистический толкатель вместо шибера. Ограничения: один seed (33 позы), не мульти-seed 165; допуск организаторов этим прогоном НЕ мерился — это метрика маршрутизации |
| 2026-07-21 | `main@c2aa3dd` | **Перепись маршрутизации на ядре с фиксом масштаба — гейт против B0** (`WORLD=sim/worlds/cell_diverter.sdf CELL_TIMEOUT=180 bash scripts/run_matrix.sh 0 3`, логи `runs/census_depthfix_c2aa3dd`) | WSL `ozon`, Gazebo Fortress + ROS 2 Humble, 11 товаров × 3 сеяные ориентации = 33 ячейки, полный перезапуск мира на ячейку | **Маршрутизация 33/33, таймаутов 0, триаж чист (classification 0, execution 0) — РОВНО B0, обязательное условие выполнено.** Все одиннадцать товаров 3/3. **Зачем прогон:** фикс масштаба (`c2aa3dd`) трогает габаритный тракт, то есть ровно ту величину, по которой принимается решение B/C/D; без переписи «стало точнее» ничем не отличается от «стало точнее и сортирует хуже». Маршрутизация не изменилась — у порогов B/C/D есть запас, и фикс в него укладывается. **Измерения на ТЕХ ЖЕ кадрах Gazebo улучшились** (истина → B0 → фикс): Шлем 352×298×282 → 337×279×278 / 329×313×282 / 338×283×280 → 349×299×279 / 352×304×282 / 341×299×285; Пуфик 489×489×264 → 492×425×267 / 474×470×262 → 484×478×262. Это снимает главное ограничение записи в decisions.md: выигрыш 8/11→11/11 был измерен на чистых офлайн-рендерах, теперь та же поправка видна на реальных кадрах симулятора. Ограничения: один seed (33 позы), не мульти-seed 165; допуск организаторов этим прогоном не мерился — это метрика маршрутизации; число залогированных строк измерения на ячейку у B0 и этого прогона различается (часть ячеек пишет `no measurement in log`), поэтому таблица выше сопоставляет ДИАПАЗОНЫ по товару, а не позу с позой |
