# Ozon Hackathon — трек 3: роботизированная сортировка товаров

**Команда «ИИ в массы»:** Иван Пушкин · Иван Резников · Максим Седов · Михаил Анцев · Владимир Зайцев.

Мы разработали виртуальную ячейку предсортировки товаров. Две камеры глубины
измеряют геометрию объектов на движущемся конвейере, алгоритм относит каждый
товар к одной из трёх категорий, а контроллер ROS 2 синхронизирует ленту и два
поворотных шибера. Полный процесс — от появления товара до его физического
перенаправления — воспроизводится в Gazebo одной командой.

**Маршруты:** `A` — точка подачи; `B` — основной сортировщик; `C` — зона
негабаритных товаров; `D` — зона доупаковки для товаров с кругом в сечении
(`K > 0.8`). Сначала проверяются габариты, затем форма — так требуют правила
задачи.

## Как работает решение

1. Верхняя и боковая камеры глубины наблюдают товар на ленте.
2. Алгоритм определяет его габариты и коэффициент формы `K`.
3. Классификатор выбирает маршрут `B`, `C` или `D`.
4. ROS 2 передаёт решение контроллеру с учётом скорости ленты 1 м/с.
5. Поворотный шибер формирует направляющую стенку, и товар уходит в нужную зону.

## Подтверждённые результаты

| Проверка | Результат |
|---|---:|
| Контрольная перепись: 11 товаров × 3 положения | **33/33** |
| Проверка на пяти наборах случайных положений (seed) | **163/165**, без ошибок исполнительного механизма |
| Производительность финальной двухкамерной конфигурации | **9 товаров/мин** |
| Задержка от кадра камеры до решения | **0,042 с** |
| Аварийная остановка и восстановление | **успешно** |

Известное ограничение: в одном плотном потоке, где шесть товаров подряд должны
уйти в одну зону, до места назначения дошли 2 из 6, поэтому итог потоковой серии
составил 29/33. Классификация этих товаров была правильной; проблема относится к
исполнительной части симуляции и подробно разобрана в
[`final_report.md`](docs/report/final_report.md).

### Почему используются две камеры

Мы сравнили конфигурации с одной, двумя и тремя камерами. Первые плохие результаты
многокамерного варианта оказались следствием нехватки производительности, а не
ошибок измерения. После оптимизации две камеры сохранили точность одной и дали
дополнительный боковой ракурс. Третья камера маршрутизацию не улучшила, но
увеличила время цикла и добавила ещё одну калибровку, поэтому в финальной версии
остались верхняя и боковая камеры. Полное сравнение приведено в
[`camera_tradeoff.md`](docs/report/camera_tradeoff.md).

В диагностических логах одна камера называется `head`, поэтому запись `heads=2`
означает, что в измерении участвовали обе камеры. Это имя программного параметра,
а не отдельный тип устройства.

**Стек:** Python 3.11, ROS 2 Humble, Gazebo Fortress (`ros_gz`), NumPy и SciPy.

## Материалы решения

- **Начать с отчёта:** [`docs/report/final_report.md`](docs/report/final_report.md).
- **Краткое резюме:** [`docs/report/one_pager.md`](docs/report/one_pager.md).
- **Записанная презентация (5:17):**
  [`II_v_massy_pitch.mp4` в Nextcloud](https://disk.ozon-robozon.ru/s/xeToaJw95awTb5C/download?path=%2FII_v_massy_Task3%2F04-presentation&files=II_v_massy_pitch.mp4).
- **Исходник 12 слайдов:** [`deck-c-ozon.html`](docs/report/slides/deck-c-ozon.html)
  и [инструкция локального просмотра](docs/report/slides/README.md). HTML-файл
  нужен для редактирования и воспроизводимой сборки записанной презентации; это
  не отдельный файл, который требуется открывать при оценке решения.
- **Текст выступления:** [`defence_speech.md`](docs/report/defence_speech.md).
- **Облачные материалы:** [Nextcloud команды](https://disk.ozon-robozon.ru/s/xeToaJw95awTb5C).

**Видеодемонстрация:**
[`census_reel.mp4` в Nextcloud](https://disk.ozon-robozon.ru/s/xeToaJw95awTb5C/download?path=%2FII_v_massy_Task3%2F01-video-demo%2Fdocs%2Freport%2Fvideo&files=census_reel.mp4)
— 6:03, H.264, все 33 контрольных случая. Каждый фрагмент подписан названием
товара, положением, ожидаемой зоной и фактическим результатом. Копия ролика также
лежит [в репозитории](docs/report/video/census_reel.mp4).

Записанная презентация `II_v_massy_pitch.mp4` содержит все 12 слайдов и озвучку.
Она размещается в разделе `04-presentation` и используется как ссылка на
презентацию в форме сдачи. Если позднее будет подготовлен PDF, его можно добавить
в тот же раздел, не меняя видеодемонстрацию решения.

### Что уходит в Nextcloud организаторов

Объёмные материалы по правилам сдачи хранятся в облаке, а не в git. Текущий
комплект без необязательных логов собирается одной командой:

```bash
python scripts/make_nextcloud_bundle.py --without-runs
# результат: build/II_v_massy_Task3/ + MANIFEST.md
```

В Nextcloud загружается **распакованная папка**, не ZIP/RAR. Финальные логи не
входят в обязательный набор; если команда решит их приложить, они добавляются
отдельной папкой `05-run-artifacts`.

| папка пакета | категория организаторов | что внутри |
|---|---|---|
| `01-video-demo` | видеодемонстрация решения | **главный файл — `census_reel.mp4`** (6:03, все 33 ячейки), остальные файлы — дополнительные клипы и постеры |
| `02-cad-and-models` | CAD-файлы и 3D-модели | STEP/STL ячейки, размерный чертёж, изометрия; исходные модели 11 товаров (STEP + STL) |
| `03-simulation` | файлы симуляций | миры Gazebo для разных конфигураций камер, настройки моста `ros_gz`, сгенерированные SDF-модели товаров |
| `04-presentation` | презентация | `II_v_massy_pitch.mp4` (5:17, 12 слайдов с озвучкой); PDF можно добавить позднее |
| `05-run-artifacts` | дополнительные доказательные материалы | необязательные финальные логи; раздел добавляется только при их наличии |

`MANIFEST.md` внутри пакета объясняет назначение разделов и перечисляет файлы с
размерами. Собранный каталог (`build/`) в git не попадает — он воспроизводится
этой же командой.

| Документ | Что внутри |
|---|---|
| `docs/report/final_report.md` | ★ Итоговый отчёт: задача, обе части ПАК, связка, результаты и ограничения |
| `docs/report/one_pager.md` | ★ Резюме «прочти за 3 минуты» — точка входа для жюри |
| `docs/report/criteria_coverage.md` | Карта: каждый пункт `criteries.md` → артефакт репо |
| `docs/md/task.md` | Условия задачи трека |
| `docs/md/criteries.md` | Критерии оценки жюри |
| `docs/md/models.md` | Анализ 11 тестовых моделей |
| `docs/md/organizer_faq_2026-07-16.md` | Официальные уточнения о приватном наборе, Docker и физических допущениях |
| `docs/report/classification.md` | Правила классификации, K по проекциям, пограничные случаи, 33/33 |
| `docs/report/mechanism.md` | Выбор и обоснование исполнительного механизма (шибер), бережность |
| `docs/report/architecture.md` | Единый контур: обработка изображения → классификация → контроллер → шибер |
| `docs/report/camera_tradeoff.md` | Сравнение вариантов с одной, двумя и тремя камерами |
| `docs/report/path_to_line.md` | Что уже доказано, исследовано только в ветках и не покрыто для реальной линии |
| `docs/report/calc_vs_sim.md` | Сравнение инженерного расчёта с результатами симуляции |
| `docs/report/layout.md` | Компоновка + сверка с официальной схемой участка |
| `docs/report/soak.md` | Длинные серии, надёжность, производительность и потребление ресурсов |
| `docs/report/safety.md` | E-stop, разделение зон, граница программного прототипа |
| `docs/report/physics_assumptions.md` | Что задано организаторами, а что принято по массе, трению и физике |
| `docs/report/validation.md` | Результаты проверки в Gazebo: полнота распознавания, ошибки габаритов и ограничения |
| `docs/report/failure_scenarios.md` | Нештатные ситуации: сигнал, безопасная реакция, восстановление и команды проверки |
| `docs/decisions.md` | Журнал инженерных решений с обоснованиями |
| `docs/experiments.md` | Журнал прогонов и метрик |
| `PLAN.md` | Стратегия и календарь этапа |
| `CLAUDE.md`, `GIT.md` | Правила работы над кодом и git-процесс |

## Требования

- **Docker** с плагином Compose — рекомендуемый путь: воспроизводит окружение
  целиком, включая ROS 2 и Gazebo, без установки их в систему.
- Либо **Ubuntu 22.04** (нативно или в WSL2) для запуска без Docker.
- Для запуска только Python-части (классификация, генерация моделей, тесты)
  достаточно Python 3.11+ на любой ОС — ROS 2 и Gazebo не нужны.

**Если разворачиваете на Windows, проверьте версию ОС до всего остального.**
Нужна Windows 10 версии 21H2 (build 19044) или новее, либо Windows 11. На
**LTSC 2019 (build 17763) два пути из трёх недоступны**, и это ограничение
редакции, а не настройки: WSL2 требует build 18362, а LTSC 2019 до него не
обновляется в принципе (отдельная редакция с десятилетним циклом); актуальный
Docker Desktop требует 21H2 и новее. Проверено развёртыванием 02.08 на такой
машине — Python-часть прошла целиком, Docker и WSL2 не поднялись, отчёт
в `docs/deploy-report-2026-08-02.md`. На такой ОС остаётся только Python-часть,
то есть ядро восприятия и классификации; исполнительная часть не запустится.
Проверить эту половину из ЧИСТОГО checkout, а не из рабочего дерева, можно так:

```bash
bash scripts/check_clean_deploy.sh --python-only
```

Он делает `git archive HEAD` во временный каталог, поднимает там свой venv,
ставит `requirements.txt`, гоняет линт, тесты и измерительные гейты — и в конце
прямо перечисляет, что осталось непокрытым (ROS 2, Gazebo, colcon, сквозной
прогон), чтобы зелёная половина не читалась как зелёное целое.

**Отдельно от `requirements.txt`** ставятся инструменты сборки медиа, они нужны
только для пересборки роликов и проверки вёрстки деки, но не для решения:
`ffmpeg` (или `pip install static-ffmpeg`), `playwright` + `playwright install
chromium`, и `gh` для проверки CI после push. Сами артефакты лежат в
репозитории, поэтому без этих инструментов всё остальное работает.

Все команды ниже выполняются из корня репозитория.

## Запуск в Docker (рекомендуется)

Полностью воспроизводимое окружение — то же, что разворачивается на сервере.

```bash
docker compose -f docker/docker-compose.yml build
docker compose -f docker/docker-compose.yml run dev
```

Внутри контейнера доступен весь контур. `install/` и `sim/models/items/` —
build-артефакты, в гит они не попадают, поэтому на свежем checkout первые две
команды обязательны и идут именно в этом порядке:

```bash
colcon build --packages-select ros_msgs          # ROS 2 контракты -> install/
python3 scripts/build_item_models.py             # SDF-модели 11 товаров -> sim/models/
bash scripts/check_sdf.sh                         # валидность мира и моделей
bash scripts/run_skeleton.sh box_300x200x200 B   # один товар, финальная конфигурация
bash scripts/run_stream.sh                        # поток без перезапуска мира
```

`WORLD` и `BRIDGE_CONFIG` здесь не задаются намеренно: скрипты и
`launch/skeleton.launch.py` по умолчанию используют финальную двухкамерную конфигурацию
(`cell_diverter_2cam.sdf` + `bridge_2cam.yaml`), поэтому команда без переменных
запускает именно то, что сдаётся. Однокамерный режим включается явно
(`WORLD=sim/worlds/cell_diverter.sdf BRIDGE_CONFIG=bridge.yaml`).

GUI Gazebo пробрасывается через X11 (Linux) или WSLg (Windows); для проверок
без экрана используется headless-режим (`ign gazebo -s -r <world>`).

Проверка развёртывания одной командой (чистый checkout `HEAD`, сборка
запиненного образа, headless CPU сквозной прогон — тот же путь, что e2e-smoke
в CI; непроверенные локальные правки в неё не попадают — сначала коммит):

```bash
python scripts/check_submission.py   # preflight полного контура, артефактов и ссылок
bash scripts/check_clean_deploy.sh
```

Перед сдачей `python scripts/check_submission.py` должен завершиться сообщением
`SUBMISSION PREFLIGHT: PASS`: ссылка на облачное видео уже указана выше.

Версии зафиксированы двумя замками: базовый образ — по digest в
`docker/Dockerfile`, python-зависимости образа — `docker/pip-constraints.txt`
(контракт — `tests/test_deploy_pins.py`).

## Запуск Python-части (без ROS 2 / Gazebo)

Классификация, анализ моделей и генерация SDF работают на чистом Python.

```bash
python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install -r requirements.txt
pytest -q                        # unit-тесты; ROS-node тесты — Docker/ручной CI
python scripts/build_item_models.py
python scripts/measure_validation.py
python scripts/measure_hard_scenes.py
python scripts/measure_private_shapes.py
```

### Классификация своей 3D-модели (STL)

Требование экспертной сессии (`docs/md/expert_session_qa.md`, `[35:29]`): загрузить
СВОЮ модель и прогнать полный контур — определение параметров → классификация по
правилам → вердикт. Точка входа:

```bash
# один или несколько путей к STL — печатает вердикт, эталон docs/md/models.md НЕ трогает
python scripts/analyze_models.py path/to/model.stl [другой.stl ...]

# на эталонном наборе организаторов (11 моделей из docs/Stl/):
python scripts/analyze_models.py docs/Stl/*.stl
```

Реальный вывод на наборе организаторов:

```
Бутылка: OBB [305.   91.3  91.2] K=0.996 watertight=True faces=6522 -> D: доупаковка (круг в сечении)
Короб 300х200х200: OBB [301.  200.5 200. ] K=0.720 watertight=True faces=592 -> B: подходит для сортировки
Короб 400х400х300: OBB [401.  400.  300.5] K=0.716 watertight=True faces=536 -> C: не подходит по габаритам
ЛанчБокс: OBB [201.  152.4  62.3] K=0.637 watertight=True faces=11574 -> B: подходит для сортировки
Мешок: OBB [201.6 176.2 169.6] K=0.800 watertight=False faces=21228 -> D: доупаковка (круг в сечении)
Моющее средство: OBB [279.6 259.8 179.2] K=0.708 watertight=True faces=72752 -> B: подходит для сортировки
Пуфик: OBB [488.9 488.9 264. ] K=0.994 watertight=False faces=12880 -> C: не подходит по габаритам
Ручка: OBB [148.5  13.1   9. ] K=0.591 watertight=False faces=40926 -> C: не подходит по габаритам
Тарелка: OBB [209.5 209.4  26.5] K=0.999 watertight=True faces=2504 -> D: доупаковка (круг в сечении)
Цилиндр: OBB [435.  50.  43.] K=0.749 watertight=True faces=2152 -> B: подходит для сортировки
Шлем: OBB [352.1 297.6 281.6] K=0.783 watertight=False faces=55159 -> B: подходит для сортировки
```

> Мешок `K=0.800` показан с 3 знаками — фактическое значение **0.8004 > 0.8**
> (порог строгий), поэтому маршрут → D корректен (`experiments.md`, c12ba2b).

Под каждым товаром печатается **передача в исполнительную часть** — тот же контракт
категория→актуатор, что и на живом пути камера→классификатор→контроллер (`src/controller_node.py`):

```
    -> исполнительная часть: категория D — доупаковка, дивёртер зоны D (/pusher_d/cmd)
    -> исполнительная часть: категория C — дивёртер зоны C (/pusher_c/cmd)
    -> исполнительная часть: категория B — едет до конца ленты, без дивёрта
```

Так контур замыкается сквозь: загрузка STL → параметры → классификация → **команда
исполнительной части** (имитация, `docs/md/expert_session_qa.md` `[35:29]`–`[36:12]`).

Габариты — по OBB (мм, по убыванию), K — максимум `r_впис/R_опис` по трём проекциям
тем же алгоритмом, что и основной контур (`src.perception._roundness_k`); категория — по правилам
`task.md` из порогов `src/constants.py`. Без аргументов скрипт вместо печати
**перегенерирует** `docs/md/models.md` (эталонный прогон) — для разовой проверки
всегда передавайте пути.

> **Расхождение STL-путь ↔ камера (честно).** Этот путь считает категорию по чистой
> геометрии сетки. Камера в основном контуре может решать иначе там, где действует
> дополнительная политика: например, **Мешок** здесь `K=0.800 → D`, а основной
> контур сознательно оставляет его в **B** (мягкий ком не
> катится; провенанс в `docs/decisions.md`). Это задокументированное расхождение, а не
> рассинхронизация: STL-путь показывает чистую геометрию, основной контур — итоговую политику.

## Запуск в WSL2 (нативно, без Docker)

Альтернатива для локальной разработки на Windows. Подставьте свои имя дистрибутива
и путь к репозиторию (`<distro>`, `<repo-path>`).

ROS 2 Humble `rosidl_cmake` может неверно разобрать смонтированный Windows-путь
с кириллицей: например, из `C:\Users\Максим\...` пропадает часть пути, после чего
`colcon build` сообщает, что сгенерированный `.idl` не существует. Для WSL-сборки
используйте checkout с ASCII-путём. Разовый проверочный worktree можно создать из
PowerShell (работать и коммитить продолжайте в основном checkout):

```powershell
git worktree add --detach C:\tmp\ozon-wsl HEAD
```

После проверки: `git worktree remove C:\tmp\ozon-wsl`. Docker-проверка
`scripts/check_clean_deploy.sh` уже собирает временный checkout в ASCII-пути
`/tmp` и этой особенности не подвержена.

```powershell
wsl --import <distro> <install-dir> <ubuntu-22.04-rootfs>.tar.gz --version 2
wsl -d <distro> -- bash <repo-path>/scripts/provision_wsl.sh   # ROS 2 + Gazebo + deps
wsl --terminate <distro>                                       # перезапуск для systemd
```

```bash
# внутри WSL, из корня репозитория. Первые две команды собирают build-артефакты
# (install/, sim/models/items/) — без них check_sdf.sh нечего проверять,
# а прогоны не находят ROS-контракты:
colcon build --packages-select ros_msgs  # ROS 2 контракты -> install/
python3 scripts/build_item_models.py     # SDF-модели 11 товаров -> sim/models/items/
bash scripts/check_sdf.sh                # валидность мира и 11 моделей
bash scripts/smoke_belt.sh               # короб физически едет по ленте 1 м/с
bash scripts/smoke_multi_item.sh         # два товара одновременно видны и имеют разные ID
```

Свежий WSL-дистрибутив иногда не разрешает DNS-имена через WSL-шлюз (`apt-get update`
отдаёт сплошные `Ign`). Исправляется один раз, до установки окружения:

```bash
printf '[boot]\nsystemd=true\n\n[network]\ngenerateResolvConf = false\n' > /etc/wsl.conf
printf 'nameserver 8.8.8.8\nnameserver 1.1.1.1\n' > /etc/resolv.conf
# затем из PowerShell: wsl --terminate <distro>
```

Headless-запуски Gazebo на программном GL требуют `LIBGL_ALWAYS_SOFTWARE=1`
(обоснование — `docs/decisions.md`); скрипты выставляют это сами.

## Воспроизведение прогонов

Вся случайность заведена через один явный seed, поэтому любой результат
повторяется одной командой. Команды ниже — Gazebo-прогоны: они запускаются
внутри среды из разделов выше (Docker или WSL2) и после её build-шагов
(`colcon build --packages-select ros_msgs`, `python3 scripts/build_item_models.py`).

```bash
# один товар, полный контур на финальном шибере без ручных вмешательств
bash scripts/run_skeleton.sh box_300x200x200 B

# несколько товаров без перезапуска Gazebo; план подачи печатается до старта
bash scripts/run_stream.sh

# матрица: все товары × N воспроизводимых ориентаций от заданного seed
bash scripts/run_matrix.sh 0 3

# та же матрица в однокамерном режиме — только для сравнения конфигураций
WORLD=sim/worlds/cell_diverter.sdf BRIDGE_CONFIG=bridge.yaml \
  bash scripts/run_matrix.sh 0 3

# медиана и 95-й перцентиль производительности и задержек по сохранённым прогонам
python3 scripts/measure_throughput.py runs/stream_run_1 runs/stream_run_2

# проверка аварийной остановки и восстановления:
# занятый механизм → E-stop → ручная очистка → подтверждённая парковка → новый товар B
bash scripts/smoke_estop_stream.sh

# запись видео сквозного прогона (камера-«зритель» подсаживается на время записи)
bash scripts/record_skeleton_video.sh plate D
```

Ячейка матрицы однозначно задаётся тройкой `(seed, item_index, orient_index)`,
поэтому отдельный эпизод можно переиграть независимо. Крупные бинарники (видео,
веса) в репозиторий не кладутся — уходят в облако организаторов (`GIT.md`).

`run_stream.sh` сначала проверяет, что расстояния между товарами совместимы с
ходом ленты и временем удержания шибера. Каждый спавн объявляется контроллеру:
если товар не доехал до камеры за 8 с, система защёлкивает E-stop вместо тихого
пропуска. Каждый эпизод также сохраняет `resources.csv` и `resources.json` с
профилем CPU/RAM деревьев Gazebo, ROS и feeder. Подробная схема состояния и топиков — в
`docs/report/architecture.md`. Регрессионные серии, полные знаменатели,
ограниченный ресурсный прогон и признаки загрязнённого Gazebo-стенда сведены в
`docs/report/soak.md`.

Начиная со Stage 26 позиционный шибер удерживается по измеренному углу только
при наличии свежего `JointState`, полученного после последней команды. Если
такого подтверждения нет, лента остаётся остановленной, контроллер сохраняет
последнюю цель и явно сообщает, что физическое удержание не подтверждено.

### Воспроизведение заявленных результатов (одна команда на заявку)

Каждая цифра из «Текущий результат» воспроизводится одной командой + seed. Все
команды — внутри среды (Docker/WSL2) после build-шагов.

| Заявка | Команда | Проверка результата |
|---|---|---|
| Классификация и маршрутизация: `33/33` на seed 0, `163/165` на пяти seed | `bash scripts/run_matrix.sh 0 3` (один seed); `for s in 0 1 2 3 4; do bash scripts/run_matrix.sh $s 3; done` + `python3 scripts/census_seed_sweep.py` | Матрица 11×3; распределение результатов — `scripts/census_seed_sweep.py`. Результат `163/165` получен на двухкамерной конфигурации и совпал с однокамерным контрольным прогоном |
| Потоковая серия всех моделей: исторически `33/33`, финальный перезамер — **29/33** | `bash scripts/run_stream_suite.sh 0 0 2` | Пять из шести эпизодов прошли полностью. В плотном потоке одной зоны четыре товара сошли с ленты; классификация была правильной |
| Устойчивая производительность: **9 товаров/мин** для финальной двухкамерной конфигурации | `python3 scripts/measure_throughput.py runs/stream_suite_<...>` | Медиана по полной потоковой серии; RTF 0,44. Пиковое значение 18 товаров/мин за устойчивую скорость не выдаётся |
| Задержка камера → решение: **0,042 с** | тот же `measure_throughput.py` | 33 измерения в единой шкале времени симуляции, 95-й перцентиль — 0,088 с |
| Чистое развёртывание без графического интерфейса | `bash scripts/check_clean_deploy.sh` | Чистая копия `HEAD` → Docker-образ → сквозная проверка `ci_e2e.sh` |

Тройка `(seed, item_index, orient_index)` однозначно задаёт ячейку матрицы —
любой отдельный эпизод переигрывается независимо.

### Основные входные параметры прогонов

| Сценарий | Входы | Назначение |
|---|---|---|
| Один товар | `run_skeleton.sh <slug> <B\|C\|D> [spawn_x]`; `WORLD`, `BRIDGE_CONFIG`, `SPAWN_Y`, `SPAWN_Z`, `ORIENT_{X,Y,Z,W}` | Один сквозной прогон. По умолчанию используется финальная двухкамерная конфигурация; переменные нужны только для сравнительных режимов |
| Матрица | `run_matrix.sh [seed] [N] [start_item] [end_item]`; `LOGDIR`, `CELL_TIMEOUT=180` | Последовательная проверка 11 товаров × N положений; диапазон индексов позволяет продолжить прерванный прогон |
| Поток | `run_stream.sh [slug:zone:gap_m ...]`; `SEED`, `ORIENT_INDEX`, `LOGDIR` | `gap_m` — расстояние позади предыдущего товара; первый gap равен 0. Небезопасный план отвергается до старта Gazebo |
| Потоковая матрица | `run_stream_suite.sh [seed] [start_orient] [end_orient]`; `SAME_ZONE_GAP_M` | Все 11 моделей двумя эпизодами на положение; параметр задаёт интервал между товарами одной зоны, по умолчанию 1,0 м |
| Диагностика | `MATRIX_DRY_RUN=1`, `STREAM_DRY_RUN=1`, `SAVE_DYNAMICS=1` | Показать точный план без Gazebo либо сохранить траектории ячеек матрицы |

`slug` должен совпадать с каталогом в `sim/models/items`, а ожидаемая зона — с
правилом B/C/D. `LOGDIR` лучше задавать явно для приёмочных серий: каталог
содержит план, терминальные результаты и данные для triage. Параметры механизма
(`HOLD_S`, `FIRE_LEAD_S`, `ENGAGE_CMD`) являются инженерной калибровкой, а не
обычными параметрами оператора; их изменение требует отдельного повторного
полного матричного прогона и проверки безопасности.

## Структура репозитория

```
src/          ядро perception / классификации / tracking и ROS 2 узлы
ros_msgs/     ROS 2 пакет с контрактами ItemMeasurement и ItemClassification
launch/       запуск полного контура одной командой
sim/          мир Gazebo, модели товаров, конфиг моста ros_gz
scripts/      генератор моделей, сквозные прогоны, утилиты анализа и записи
docker/       воспроизводимое окружение сборки и запуска
tests/        unit-тесты ядра и узлов
docs/         условия, решения, эксперименты, разделы отчёта
```

### Структура решения по частям

Разбиение по требованию сдачи (`task.md` §4): что относится к классификации, к
исполнительной части, к связке и к сценариям проверки.

| Часть | Код / артефакты | Раздел отчёта |
|---|---|---|
| **Определение и классификация** | `src/perception.py` (габариты, K по проекциям из depth), `src/aggregation.py` (стабилизация по кадрам), `src/classification.py` (правила B/C/D), `src/constants.py` (пороги), узлы `perception_node`/`classifier_node` | `classification.md`, `validation.md` |
| **Исполнительная часть** | `sim/worlds/cell_diverter.sdf` (шиберы, склизы, зоны), `src/controller_node.py` (тайминг, возврат, E-stop, jam/feed-watchdog), `src/tracking.py` (dead-reckoning выстрела) | `mechanism.md`, `layout.md`, `safety.md` |
| **Связка (классификация → воздействие)** | контроллер подписан на `ItemClassification` → `plan_push` (зона→шибер, упреждение по скорости ленты) → команда механизму; контракты в `ros_msgs/` | `architecture.md`, `calc_vs_sim.md` |
| **Сценарии проверки** | `run_matrix.sh` (полная матрица), `run_stream_suite.sh` (поток), `check_clean_deploy.sh` (чистое развёртывание), `smoke_estop*.sh` (безопасность), `measure_*.py` (метрики) | `soak.md`, `failure_scenarios.md`, `criteria_coverage.md` |
