# План недели 3 — устойчивость и веха `v0.4-robust`

> Детализация этапа 5 из `PLAN.md`. Официальное окно — 24–29.07; команда начала
> часть работ раньше после закрытия `v0.3-pipeline`. Источник требований —
> `docs/md/task.md`, критериев — `docs/md/criteries.md`, фактов —
> `docs/experiments.md`. Этот файл не заменяет фактический журнал экспериментов.

## Исходная точка

- Сквозной multi-item контур и чистое развёртывание закрыты тегом
  `v0.3-pipeline`; полный census воспроизведён 33/33.
- Штатный трёхтоварный поток имеет измеренный throughput 18 шт/мин по пяти
  успешным прогонам.
- Плотный пяти-товарный поток пока недетерминирован: 4/5, 4/5, 4/5 и 5/5.
  В трёх промахах Lunchbox правильно получил решение B, но физически остался в D.
- Safety-контур уже умеет E-stop, затор в окне камеры и feed-watchdog; нужны
  сценарные доказательства и длинный горизонт, а не новые флаги ради чекбоксов.
- Ветка `sim/verdict-scores-the-body` исследует pose/ограждения/3D-OBB отдельно.
  До review её нельзя смешивать со стабилизацией плотного потока.

## Gate недели

Веха готова, когда одновременно выполнено:

1. Для каждого длинного прогона считаются **все** терминальные PASS/FAIL, отдельно
   от throughput успешных arrivals; опубликованы числитель, знаменатель и причины.
2. Плотный сценарий повторён не менее 5 раз на чистом стенде. B→D промах либо
   исправлен и не воспроизводится на контрольной серии, либо локализован и честно
   записан как ограничение с безопасным режимом подачи.
3. Нештатные сценарии из рубрики имеют проверяемую реакцию: близкие товары,
   заклинивание/неподача, остановка конвейера и задержка/пропуск решения.
4. Пограничные модели и ориентации проходят замороженную проверку; изменения из
   экспериментальных веток принимаются только сравнением на одном наборе.
5. Размерная компоновка, безопасность, README и чистое развёртывание соответствуют
   фактической финальной сцене. Полный набор тестов и SDF-проверок зелёный.

## День 15 — длинный горизонт и физический B→D промах

- [x] Расширить `measure_throughput.py`: надёжность считает PASS и FAIL по эпизодам,
  товарам и маршрутам, тогда как throughput по-прежнему использует только arrivals.
- [x] На свободном Gazebo-стенде выполнить ≥5 одинаковых плотных эпизодов:
  `box_400→C, pen→C, bottle→D, lunchbox→B, detergent→B`.
- [x] Проверить минимальную различающую матрицу: одиночный B закрыт census 33/33;
  B после D на расчётном минимуме 3.1 с — 4/4; с зазором 3.5 с внутри плотной
  серии — 5/5. Старый промах не воспроизведён, поэтому динамику/физику не меняем.
- [x] Зафиксировать контрольную серию без «фикса»: 5/5 эпизодов, 25/25 товаров,
  точные каталоги и прежние FAIL вместе записаны в `docs/experiments.md`.

Готово: текущий безопасный режим подтверждён серией; старый B→D исход не выдан
за исправленный и остаётся ограничением до воспроизводимого различающего trace.

## День 16 — нештатные ситуации как сценарии

- [x] Таблица «сбой → сигнал → реакция → восстановление» в отчёте.
- [x] Близкие товары: simultaneous-frame, стабильные `item_id`, нет двойной команды.
- [x] Заклинивание в окне и неподача до камеры: E-stop защёлкивается, поздние
  команды отменяются, reset не переиспользует старые watchdog-якоря.
- [x] Остановка ленты при занятой ячейке: товар и позиционный шибер не продолжают
  движение; после reset действует soft-start.
- [x] Задержка/пропуск решения: консервативная реакция названа в логе и не создаёт
  неуправляемого повторного воздействия.

Готово: каждый сценарий воспроизводится одной командой и имеет машинный verdict.

## День 17 — положения, ориентации и пограничные товары

- [x] Review экспериментальной pose-sweep ветки отдельно от main: одинаковые
  frozen frames, регрессии одиночных и multi-item кадров, стоимость кадра.
- [x] Принять только минимальную часть, которая численно улучшает реальный набор;
  синтетический sweep сам по себе не является доказательством Gazebo-качества.
- [x] Повторить Helmet/Cylinder/Pen/Pouf и спорные ориентации с телесным verdict.
- [x] Обновить карту ограничений: ошибка восприятия, правила или механики.

Готово: пограничный случай имеет ожидаемую категорию, наблюдаемый результат и owner.

Статус review: ветка проверена в отдельном worktree; frozen validation сохранил
категории, медианная цена кадра выросла примерно на 9%. Исходная наклонная поза
Шлема прошла полный контур 2/2 в worktree и 1/1 после переноса на main; multi-item
smoke сохранил два ID и доставил оба товара. Приняты рендер/pose-sweep,
collision-only ограждения и 3D body-OBB; старый merge-коммит ветки не переносился.
Контрольная матрица после переноса (`runs/week3_boundary_main_20260715`) дала
15/15: Pouf/Pen/Plate/Cylinder/Helmet — по 3/3; triage classification=0,
execution=0. `no measurement in log` в краткой строке трёх Pouf и Plate oi1 —
не пропуск perception: episode-логи содержат classifier/controller и телесный PASS;
у Pouf legacy-origin дал бы ложный FAIL, что ещё раз подтверждает body-verdict.

## День 18 — soak и регрессия полного контура

- [x] Длинные серии по нескольким seed/ориентациям без параллельных Gazebo-сессий.
- [x] Сводка: all-pass эпизоды, routed items, per-model/per-route PASS, причины FAIL,
  медиана/p95 такта и задержек. Нельзя исключать FAIL из надёжности.
- [x] Повторить census и safety-smoke после последнего изменения физики/CV.
- [x] Зафиксировать ресурсные требования и признаки загрязнённого стенда.

Готово: опубликована воспроизводимая сводка с сырыми каталогами прогонов.

Статус: два post-change census дали 33/33 + 33/33 (66/66 routed, каждая
модель 6/6), четыре seed-потока — 12/12, плотная контрольная серия — 5/5
эпизодов и 25/25 товаров. После синхронизации `main@66f17e4` локально повторены
multi-item и occupied E-stop — оба PASS. Отдельные знаменатели, исторические
FAIL, длительность и dirty-stand checklist опубликованы в `docs/report/soak.md`.

## День 19 — инженерный комплект и чистое развёртывание

- [x] Сверить SDF, размерный план, зоны ограждения и обслуживание по координатам.
- [x] Закрыть спецификацию узлов и честную границу «sim-модель, не production STEP».
- [x] Повторить инструкцию на чистом checkout участником, который не делал правку.
- [x] Сверить README-навигацию и список входных параметров эксперта.

Готово: новый проверяющий разворачивает и запускает сценарии без устных подсказок.

Статус: размерная SVG-схема теперь показывает collision-only бортики точно по
координатам SDF, а тест блокирует рассинхронизацию чертежа и мира. README фиксирует
входы одиночного, matrix- и stream-раннеров; архитектура отделяет исполняемую
sim-модель от будущей STEP/safety-проработки. На временном чистом checkout
`abc8be8` строго с нуля собраны ROS-контракты и 11 моделей: `ruff` чист,
`347 passed / 3 skipped`, оба мира и все модели SDF OK; отдельный сквозной
`box_300x200x200 → B` — PASS. Временные build-артефакты после проверки удалены.

## День 20 — freeze и `v0.4-robust`

- [ ] Полные `ruff`, `pytest`, `check_sdf.sh`, e2e/census/safety на тихом стенде.
- [ ] Обновить архитектуру, методологию, ограничения и `docs/experiments.md`.
- [ ] Проверить оба ремоута, чистое дерево и поставить аннотированный тег.
- [ ] После тега — только упаковка и исправления подтверждённых регрессий.

## Запреты

- Не смешивать параллельные Gazebo-прогоны: общий transport даёт ложные результаты.
- Не лечить нестабильность расширением зоны verdict или ослаблением критерия PASS.
- Не оптимизировать классификационное правило, уже закреплённое тестами задачи.
- Не сливать исследовательскую ветку целиком: только review, одинаковый baseline и
  логические коммиты.
- Не выдавать синтетические/unit-тесты за выполненный физический сценарий.
