# План недели 2: 17.07–23.07

> Черновик после недели 1. Стратегия — `PLAN.md`, факты — `docs/experiments.md`,
> ограничения — `docs/report/methodology_and_limitations.md`. Приоритеты меняются
> только по повторным прогонам, не по одному удачному эпизоду.

## Входное состояние

- Полный контур камера → измерение → правило → контроллер → шибер работает.
- Финальный механизм — исправленный шибер со склизом.
- **Все census до починки вердикта сняты сломанной линейкой и не являются текущей
  метрикой** (числа 30/33…33/33 и «диапазон 31–33/33» не заявлять). Вердикт
  сравнивал с порогом позу **origin'а модели**, а не геометрию самого товара:
  у повёрнутого объёмного товара origin уезжает от точки контакта на 27–35 см.
  Кроме того, полоса B принимала транзиент до лопасти D.
- **Вторая ось, сломавшаяся в тот же день: census не задавал МЕХАНИЗМ.** `run_matrix`
  не выставлял `WORLD`, а `run_skeleton` по умолчанию берёт `cell.sdf` — **баллистический
  толкатель**. Три прогона молча измерили отвергнутый механизм; их числа (31/31/30)
  отозваны. Теперь мир шибера — дефолт матрицы и **печатается** в итоговой строке.
- **Актуальная метрика: `routing 33/33`, ДВА полностью чистых прогона подряд**
  (`runs/census_feed_*`) — на финальном механизме, починенной линейке и физичном
  порядке подачи. Классификация **0**, исполнение **0**, между прогонами не
  разошлась ни одна ячейка.
- **Фикс линейки виден и здесь:** на той же физике старая origin-линейка дала бы
  31/33 и 32/33 вместо 33/33 — расходятся 2 ячейки, обе Пуфик.
- **«Неустойчивый Шлем» ЗАКРЫТ: это был дефект СТЕНДА.** `run_skeleton` трогал ленту
  ПОД лежащим товаром; шлем стоит на макушке (пятно 62×64 мм, центр масс 175 мм)
  и опрокидывается при 10°, дальше катится и выкатывается за кромку (4 падения из 19).
  Подача на уже идущую ленту — 8/8, y=0.0109 м каждый раз. `run_stream` всегда подавал
  так, поэтому поток дефекта не видел. Рельсы подачи (День 2) лечили симптом стенда.
  Диагностический режим `SAVE_DYNAMICS=1` (полная трасса каждой ячейки) остаётся
  в инструментарии для будущих редких сбоев.
- **«pouf oi=1 chute-stick» ЗАКРЫТ: его не существовало** — товар всё это время лежал
  в лотке (нижняя точка тела +11 мм над полом), FAIL ставила линейка. Гипотеза
  «мостит 6-см ступеньку кромка→склиз» опровергнута замером.
- E-stop, зоны безопасности и e2e-smoke в CI готовы.
- Multi-item контур уже поддерживает несколько компонент и устойчивые `item_id`;
  оба долга закрыты 14.07: overlay с id/состоянием агрегации
  (`PERCEPTION_DUMP_DIR` пишет `overlay_NNN.png`) и watchdog затора до камеры
  (`/infeed/fed` + FEED JAM в контроллере).
- Bag oi1 исправлен гейтом вытянутости и закреплён реальным depth-кадром.
- **Остаточные CV-риски держатся везением позы и медианой кадров, а не фиксом**:
  Тарелка на ребре, Helmet OBB-straddle (317 мм в 3 мм от лимита 320), Bag K около 0.8,
  пропуски тонких/необычно ориентированных объектов. В census они не выпали — не устранены.

## Цель и веха

К 23.07 получить `v0.3-pipeline`: непрерывный поток из нескольких товаров с
устойчивыми `item_id`, измеренной пропускной способностью и повторяемой матрицей
качества. Нейросеть и физический узел — условные ветки, не блокирующие веху.

Критерии готовности:

1. [x] В кадре одновременно 2–3 товара; измерения не сливаются, идентификаторы не
   перескакивают, каждому товару соответствует не более одного действия.
2. [x] Сценарии «два близких товара», «E-stop посреди потока», «пропуск измерения» и
   «товар не покинул подачу» имеют явную реакцию и тест. Затор до камеры закрыт
   14.07 watchdog'ом подачи: раннеры объявляют каждый спавн в `/infeed/fed`,
   контроллер защёлкивает FEED JAM, если камера не подтвердила товар за
   `feed_timeout_s` (8 с при ~3 с пути); заодно громко ловится «камера молча
   пропустила товар».
3. [x] Метрика опубликована как **33/33 по двум полностью чистым прогонам подряд**
   на ФИНАЛЬНОМ механизме, починенной линейке и физичном порядке подачи
   (`runs/census_feed_*`), с явным отзывом всех прежних чисел: они снимались либо
   сломанной линейкой, либо не тем механизмом, либо со стендовым дефектом подачи.
   Правило «мерить по ≥2 прогонам» остаётся; к нему добавлено «и НАЗЫВАТЬ, чем мерили».
4. [x] Измерены steady-state такт и товаров/мин без времени запуска Gazebo по пяти
   успешно завершённым потокам: медиана **18 товаров/мин** (p95-медленный такт —
   **11 товаров/мин**), такт одной зоны **1.6 с**, смены зоны **5.5 с**;
   решение→команда **1.90 с** (p95 **2.27 с**). Неуспешный seed1 исключён явно,
   запуск Gazebo и soft-start не входят в T0-относительную метрику.
5. Геометрический baseline остаётся обязательным. ML принимается только если
   побеждает его на замороженном наборе по заранее выбранной метрике.
6. CI, инструкция и разделы отчёта соответствуют фактическому контуру.

## Потоки ответственности

| Поток | Работа | Артефакт |
|---|---|---|
| P1 int | Контракт multi-item, ассоциация, lifecycle товара, потоковый runner, latency/throughput | `ros_msgs/`, `src/tracking.py`, `scripts/` |
| P2 sim | Подача 2–3 товаров, расстояния, jam-сценарии; шибер/склиз не перепроектировать без регрессии | миры и e2e-сценарии |
| P3 cv | Multi-component depth segmentation, ориентации Plate/Bag/Helmet, замороженный validation set | perception + fixtures + метрики |
| P4 cls | Агрегация строго по `item_id`, политика старения/неуверенности; правило категории не заменять сетью | classifier/aggregation + тесты |
| P5 docs | Расчёт↔симуляция, CAD-компоновка, отчёт и таблицы повторных прогонов | `docs/report/`, CAD/схемы |

## План по дням

### День 8 — замок baseline и контракт multi-item

- [x] Выполнить census #1 после исправлений origin и шибера: raw 29/33,
  corrected 30/33 после replay технического TIMEOUT, triage причин сохранён.
- [x] Census #2: 30/33 без replay; диапазон 30–30/33 и общий triage опубликованы.
- [x] Написать тесты контракта: два измерения в одном кадре, уникальный `item_id`,
  исчезновение/повторное появление, запрет двойного срабатывания.
- [x] Зафиксировать lifecycle: `seen → stable → scheduled → fired/done`, TTL и правила
  после E-stop.
- Не менять пороги CV по результату одной ячейки.

Готово: красные contract-тесты описывают будущий multi-item без изменения мира.

**Опережение 12.07:** агрегатор уже держит interleaved ID по LRU; perception
выдаёт несколько раздельных depth-компонент, а pure tracker сохраняет ID при
перестановке порядка и кратком пропуске. Unit/node-контракт зелёный. Два товара
подтверждены в одном Gazebo camera stamp и независимо дошли в B;
node-тест C+D закрывает cross-fire. До закрытия Дня 8 остаётся lifecycle/TTL
завершённых товаров; overlay — отдельный хвост Дня 9.

**Lifecycle закрыт:** terminal-state контроллера имеет TTL 30 с sim-time и
hard cap 256, не удаляя pending-таймеры; E-stop по-прежнему отменяет активные
команды. Два census на исправленном шибере дали 30–30/33; baseline Дня 8 закрыт.

### День 9 — несколько объектов в perception

- [x] Выделять несколько связных компонент depth вместо одного крупнейшего объекта.
- [x] Ассоциировать компоненты между кадрами по предсказанной x-позиции, габаритам и
  допустимому смещению при 1 м/с.
- [x] Выдавать массив/несколько сообщений с устойчивыми идентификаторами.
- [x] Визуальный overlay с ID и состоянием агрегации (14.07): `overlay_NNN.png`
  рядом с каждым depth-дампом — bbox, id, габариты/K и последний вердикт
  классификатора; пример на реальном кадре — `docs/report/img/day9_overlay_ids_state.png`.

Готово: синтетическая последовательность и Gazebo-дамп с двумя товарами сохраняют
два ID до выхода из кадра.

### День 10 — поток и нештатные ситуации

- [x] Спавнер 2–3 товаров с явным интервалом и seed.
- [x] Контроллер хранит независимые таймеры; действие одного товара не отменяет другое.
- [x] Детектор jam по отсутствию продвижения; безопасная реакция — защёлкнуть остановку
  и сообщить причину, без автоматического повторного выстрела.
- [x] E-stop/reset посреди двух товаров.

Готово: e2e-сценарии проходят без cross-fire и старых команд после reset.

**Факт 12.07 (спавнер и cross-fire закрыты, ценой найденного дефекта механизма):**
`scripts/run_stream.sh` подаёт 2–3 товара из одной точки с интервалом и seed
(`stream_plan.py` отказывается строить поток, который сцена не пронесёт). Первый же
поток вскрыл **корневой дефект шибера**: лопасть была на 50 мм внутри ленты и
залипала в упоре, из-за чего первый сработавший механизм забирал ВСЕ следующие
товары. Исправлено (лопасть над лентой + позиционное управление суставом),
регрессия одиночных эпизодов 3/3, поток 2/3, такт 1.5 с (40 шт/мин).
Подтверждено на физике: два товара в одну зону вплотную проходят.

**День 10 закрыт (12.07).** Оставшиеся два пункта тоже сделаны:

- **E-stop посреди потока** (`scripts/smoke_estop_stream.sh`): проверка на ЗАНЯТОЙ
  ячейке вскрыла дефект, которого одиночный smoke показать не мог — для позиционной
  лопасти команда `0.0` означает не «стоп», а «уехать в парковку», то есть аварийная
  остановка уводила лопасть ИЗ-ПОД опирающегося на неё товара. Теперь механизм
  замораживается на текущем угле. После остановки товары стоят (0.000 м за 2 с),
  лопасть держит 0.90 рад, поздних выстрелов нет.
- **Детектор затора**: товар, который камера видит, но который не прошёл и 5 см за
  2 с, защёлкивает безопасную остановку с названной причиной. Ложных срабатываний на
  здоровом трафике нет. Ограничение: заторы ДО камеры (класс `feed_jam`) невидимы —
  нужен датчик на подаче.

**Поток впервые 3/3** (box_400→C, Ручка→C, Бутылка→D). Такт: 1.3 с в одну зону,
6.7 с при смене зоны. Перелёт Бутылки за лоток не воспроизвёлся — это пограничная
посадка цилиндра, а не детерминированный дефект; метрику потока берём по ≥2 прогонам.

### День 11 — CV на нестандартных ракурсах и синтетика

**Статус после фикса порядка подачи:** Bag oi1 исправлен, два полностью чистых
census подряд — routing 33/33 и 33/33, классификация 0 ошибок. Это не отменяет
валидацию редких ракурсов: Helmet держится рядом с габаритной границей, а
Plate/Pen нужны отдельными срезами. Routing-отказов сейчас нет: «неустойчивый
Шлем» (`helmet oi=0`) закрыт как дефект стенда — лента стартовала под товаром.

- [ ] Заморозить validation-набор из реальных Gazebo depth/RGB кадров — частично: сохранены
  depth Bag oi1/oi2, Helmet oi2, Plate oi1; RGB и частичная видимость открыты.
- [ ] Отдельные срезы — частично: Bag/Helmet/Plate закреплены; Pen и частичная видимость открыты.
- Построить генератор синтетических кадров и ground-truth масок/поз.
- Сравнить с реальными кадрами числом; синтетика не задаёт пороги, если доменный
  разрыв не измерен.

Готово, когда: есть baseline-таблица recall/ошибка габаритов/K по каждому срезу.

**Execution-проверка 13.07 (закрыта фиксом подачи):** replay `helmet oi=0`
сначала дали 4/4 PASS без объяснения; затем сравнение двух раннеров показало
корень — `run_skeleton` запускал ленту ПОД лежащим товаром. После подачи на уже
идущую ленту — 8/8 стабильных прогонов и два чистых census. `SAVE_DYNAMICS=1`
сохраняет отдельную полную траекторию каждой ячейки — оставлен для будущих
редких сбоев. Склиз ради Pouf не менять: body-scored вердикт доказал, что товар
уже лежал в зоне.

### День 12 — условная ML-ветка

- Только детекция/сегментация, не категория.
- Обучение в Kaggle/Colab; фиксированные train/validation split и seed.
- Сравнение с классической depth-сегментацией на одном замороженном наборе.
- Если нет улучшения основной метрики либо ломается воспроизводимость — честный
  no-go, классика остаётся финальной.

Готово: таблица baseline vs ML и сохранённое решение go/no-go.

### День 13 — производительность и расчётный слой

- [x] Потоковый runner без перезапуска мира на каждый товар.
- [x] Разделить startup, latency камера→решение→команда, такт механизма и throughput.
  Точное камера→решение ниже ~0.2-секундного шума cross-node логов и честно
  оставлено неизмеримым без общих timestamp'ов; остальные плечи измерены.
- [x] Сопоставить расчёт по расстояниям/1 м/с с sim-трассой.
- [x] Компоновка финального шибера, склизов, ограждения и доступа обслуживания:
  размерный SVG и таблица координат выведены из фактического SDF. Честная граница:
  это 3D-модель симуляции + проектный план, не производственная STEP-сборка с допусками.

Готово: товаров/мин и задержки с медианой/p95; расчёт и симуляция объясняют
расхождение, а не просто дают две цифры.

### День 14 — повторная валидация и веха

- [x] Два повторных потоковых прогона и два census; triage каждой ошибки. Есть
  пять успешно завершённых потоков, аварийный seed1 разобран отдельно и не
  подмешан в метрику; три полных census дали 33/33 ×3.
- [x] Обновить отчёт, README, архитектуру и ограничения. README и схема контура
  теперь описывают фактический multi-item pipeline, финальный шибер, watchdog'и,
  воспроизводимые команды и актуальные метрики, а не однопредметный skeleton.
- [ ] Чистый checkout повторяет другой участник.
- [ ] Тег `v0.3-pipeline` ставится только при выполнении критериев выше.

## Gates и запреты

- Не расширять ленту: ветка `feat/wider-belt` дала ту же маршрутизацию и вдвое
  худшую бережность.
- Не возвращать толкатель: он остаётся baseline, не второй продуктовый механизм.
- Не строить руку параллельно без владельца, доступного железа и отдельного
  решения команды до начала Дня 9.
- Не заявлять детерминизм физики по одному seed: seed фиксирует сцену, не контакты.
- Не ставить `v0.2-all-items` или `v0.3-pipeline` по неполному/одиночному прогону.

## Решения, требующие команды

1. Физический узел: go только если есть конкретный владелец, комплект железа и
   демонстрируемая функция; иначе официальный no-go и усиление CAD+sim.
2. GPU/Colab: кто владеет обучением и где хранятся веса/датасет вне git.
3. Кто является «новым человеком» для проверки чистого развёртывания.
